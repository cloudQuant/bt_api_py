# 迭代 4 报告：适配器正确性

> 日期：2026-08-17

## 目标回顾

修复 OKX 签名、接通错误翻译、补齐插件发现、统一限速/重连/日志脱敏，为 61 仓建立最低测试基线（Spec B-02、B-04~B-24）。

## 任务完成情况

| 任务 | Finding | 子模块 commit | 状态 |
|------|---------|--------------|------|
| 4.1 | B-02 | okx `eda3235` | complete（OKX 时间戳 ISO8601 + 黄金向量，WSS 时间戳同改） |
| 4.2 | B-04 | binance `e8963de`、okx `1ee4ecc`、bybit `5a7c525`、gateio `16add04`、hyperliquid `99097c6` | complete（5 仓 `_raise_if_error` 接通 translate_error） |
| 4.3 | B-05 | 27 仓（bitrue..zaif） | complete（补齐 27 仓 `bt_api.plugins` entry-points + plugin.py，生成脚本 `scripts/fix_plugin_entries.py`） |
| 4.4 | B-07 | binance `4ddfd34` | complete（sign 私钥缺失抛 ConfigurationError） |
| 4.5 | B-13 | base `a39b9a0` | complete（429 读 Retry-After，`_retry_backoff` 按 Retry-After 退避） |
| 4.6 | B-14 | okx `9e7fa77` | complete（登录回执后订阅，重连重订阅，去 sleep(0.3)） |
| 4.7 | B-12 | hyperliquid `6a68053` | complete（分支 A：删除私钥加载死代码，声明 X-API-Key vault/agent 模式，决策记录落盘） |
| 4.8 | B-08 | base `27673a2`（PluginInfo capabilities）、bybit `6a6c5ec` | complete（PluginInfo 加 capabilities 字段，bybit 声明无 WSS，能力矩阵文档 ❌） |
| 4.9 | B-17/23 | hyperliquid `c3dbe3a`、bybit `43a1fdb`、gateio `f9f506b` | complete（URL 日志脱敏 + logger 按仓命名） |
| 4.10 | B-16 | gateio `bb14588`、btbns `ba87c1b` | complete（gateio 删 ctp 残留；btbns 已有实现，补 entry-points） |
| 4.11 | B-10 | binance `c99f951`、zebpay `6effd6e` | complete（基线文档 `docs/code-quality-baseline.md`；binance 自指签名改黄金向量、zebpay 空壳补 normalize） |
| 4.12 | B-24 | — | complete（发布节奏决策文档 + `scripts/bump_all_submodules.py`，dry-run 60 仓一致） |
| 4.13 | B-15/21 | base `6db0c19`（signers/timestamps/rate_limiters）、binance `4333bc8` | complete（工具函数下沉 + 黄金向量测试；测试密钥替换占位常量） |

## 验收清单结果

1. ✅ OKX 黄金向量测试通过（`ymzav0cu8v4AhecjpRnt8sRQ8vOk/6+BT89eeU/sIjQ=`，可复算）
2. ⚠️ 插件发现 smoke ≥61 入口：补 27 仓后总数 58（31 已有 + 27 新补），距 61 差 3（因 5 个 retired 仓已删 + btbns 原先空仓）；本地环境插件未安装故 `entry_points` 返回 0，需 CI 全装后验证
3. ✅ 5 大仓错误翻译测试各 ≥3 用例（binance 4 / okx 3 / bybit 3 / gateio 4 / hyperliquid 4）
4. ✅ 每仓最低测试基线（基线文档 + binance/zebpay 空壳修复；其余仓按 L2/L3 档逐仓落地为 ongoing）
5. ⚠️ URL 配置 env 覆盖测试（B-11）：未做（较大，涉及 okx/hyperliquid exchange_data 重构 + configs yaml），记录为遗留
6. ⚠️ nightly matrix FAIL 清零：未验证（需 GitHub 触发 workflow_dispatch）

## 关键设计决策

- **B-12 hyperliquid 签名**：分支 A（删除私钥加载死代码，仅支持 X-API-Key vault/agent 模式）。EIP-712 另立 backlog（需官方文档）。
- **B-08 bybit WSS**：PluginInfo 加 `capabilities` 字段，bybit 声明 `frozenset()`（无 WSS），能力矩阵文档 ❌。
- **B-16 btbns**：spec 分析时为空仓，实际已由远程更新补全实现，故从"移除或实现"决策改为"补 entry-points"。

## 遗留问题

1. **B-11 URL 配置 env 覆盖**（okx/hyperliquid exchange_data 硬编码 URL 改 yaml+env）未做。
2. **插件发现 smoke ≥61**：实际 58 入口，需 CI 全装后验证；若需严格 61 需补 3 个 retired/新增仓。
3. **nightly matrix FAIL 清零**：需 GitHub 触发验证。
4. **B-15 全量下沉**：三模块已建，但 39 处 hmac/15+ 时间戳/6 限速器**尚未逐仓迁移**到 base 模块（本迭代只建了模块，迁移另立任务）。
5. 母仓库迭代 4 的 commit 尚未 push（本地 master）。

## 时间花费

迭代 4（13 个任务，含大量子模块 commit+push+pin，网络时断时续导致多次重试）：约 1 个工作日。
