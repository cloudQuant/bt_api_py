# 迭代07：代码质量提升 —— 需求文档

> 版本：v1.54
> 日期：2026-09-21
> 状态：进行中（M1、M2 核心、M4 指标与主要 M3 已实施；Task30/31/33–41 已分批收敛 mypy 与新增类型债，fresh 254 文件可比范围已为 0，Task32 已补强质量棘轮；Task42 已建立本地 singleton 性能安全上限；Task43 已将 `OrderRequest` 校验入口 C901 **18 → 1** 并补齐边界合同；精确 256 文件命令与 hosted performance 均未运行；M5-Task15/16 已完成；余项以本文件 §8 的已决策阶段推进）
> 关联设计：`研发设计文档.md`
> 关联实施：`实施计划.md`
> 关联验收：`验收文档.md`
> 既有资产：`docs/CODE_QUALITY.md`、`docs/code-quality-baseline.md`、
> `docs/acceptance/2026-08-17-quality-baseline.json`、`docs/plans/`（74 份计划）

---

## 0. 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-19 | 初稿：基于 2026-09-19 对仓库的**实测扫描**（ruff 全仓、AST 注解/文档/函数长度、CI 与 Makefile 门禁定义、git 跟踪状态），并与 2026-08-18 质量基线对照 |
| v1.1 | 2026-09-19 | **D1 决策落档**：15 个子仓全部纳入门禁，由主仓侧实施（不改子仓文件）；扫描范围修正为完整门禁范围（34 路径 / 1301 项，含子仓 tests 与 examples/scripts），`F821` 修正为 135 处 |
| v1.2 | 2026-09-20 | 回填滚动快照 **1301 → 11**；纠正“子仓矩阵可直接转阻塞”的前提：现有 lint 与 format 存量必须分阶段治理；补充公共 API 文档指标的可复算要求 |
| v1.3 | 2026-09-20 | 公共 API 专用 AST 指标落地并经过 fixture 测试；固定分母实测 docstring **594/691（85.96%）**、参数注解 **1016/1035（98.16%）**，AC-9 的文档口径可复算 |
| v1.4 | 2026-09-20 | 子仓质量门禁已拆为独立 lint/format report-only job；升级仍以各自清零、稳定 CI 与独立回滚证据为条件 |
| v1.5 | 2026-09-20 | 确认已移除 DEX 公共实现后，5 个 Curve/Raydium/SushiSwap 不可收集示例测试正式退役；以精确 AST 导入扫描防回归 |
| v1.6 | 2026-09-20 | 固定 CTP/SWIG 探针的 S603/B404/B603 收敛为经过威胁建模和行为测试的 E-05 行级例外；棘轮 **11 → 10**，剩余项全部在子仓 |
| v1.7 | 2026-09-20 | Task15 的第三项纯预算计算完成等价拆分：先补完整路径、reason 顺序和 fail-closed 行为契约，再将入口 **170 → 128**；仍保留 2 项待逐项评审 |
| v1.8 | 2026-09-20 | Task15 的第四项事件归一化完成最小等价拆分：orderbook fields 先补行为契约，再将入口 **181 → 134**；仍保留 1 项待逐项评审 |
| v1.9 | 2026-09-20 | Task15 的第五项 Binance read-only 编排完成：读取顺序/参数/深拷贝契约先固定，再将 `get_order_readiness` **157 → 104**；五项均已完成，余下超长函数仍按风险逐项治理 |
| v1.10 | 2026-09-20 | Task16 以 CI 同标记并行根套件重新覆盖率采样：**66.45%** ≥55%，不再仅依据历史 66.29% 结论 |
| v1.11 | 2026-09-20 | 将当前需求指标、D3 决策和质量标准同步为新鲜 CI 同标记证据 **66.45%**，消除仍指向历史 66.29% 的活动表述 |
| v1.12 | 2026-09-20 | 需求台账补足根仓 8 条渐进 Ruff ignore 的只读实时盘点：`make tech-debt-report` 当前 **86** 项；计划索引已覆盖 74/74，实际清理和 36 份 owner 复核仍未完成 |
| v1.13 | 2026-09-20 | 以隔离扫描确认 `PERF402`/`S113` 为 0 后移除其根全局及测试路径例外；报告收缩为 6 条、**86** 项，新增命中将直接进入 lint，变更后根并行回归 **1376 passed、2 warnings、76.01s**；剩余清理与 36 份 owner 复核仍未完成 |
| v1.14 | 2026-09-20 | `PERF403` 唯一根仓命中已先以离线 Logstash payload 契约固定，再改为字典推导式；规则从根 ignore 和报告移除，当前 5 条、**85** 项，ELK/报告定向回归 **47 passed**、根 marker 回归 **1377 passed、2 warnings、79.72s**，不涉及外部 ELK |
| v1.15 | 2026-09-20 | 两个 IBKR Cookie 脚本的 `S112` 命中已先以浏览器/requests/logger 内存替身固定，再改为不记录 Cookie 或异常正文的 debug 诊断；移除根全局 ignore，报告收缩为 4 条、**83** 项，根 marker 回归 **1379 passed、2 warnings、73.73s**，不涉及浏览器、Gateway 或网络 |
| v1.16 | 2026-09-20 | 本地 CTP 前置 YAML 加载失败的 `S110` 以临时文件/内存 `yaml` 契约固定：保留内置地址回退，debug 仅记录异常类型、不含内容/路径/异常正文；报告 **83 → 82**，selector/报告定向回归 **18 passed**、根 marker **1379 passed、2 warnings、74.57s**，不涉及 CTP、SimNow 或网络 |
| v1.17 | 2026-09-20 | 两个 margin 文档抓取脚本的侧栏发现失败以假 `playwright.sync_api`/假页面契约固定：返回空列表，warning 仅记录异常类型；报告 **82 → 80**，scraper/报告定向回归 **15 passed**、根 marker **1381 passed、2 warnings、88.93s**，未调用主流程、浏览器或网络 |
| v1.18 | 2026-09-20 | 生产监控示例 exporter 停止失败的安全返回由 fake module 契约固定，文档契约检查器的错误顺序由离线测试固定后收敛 3 个 `PERF401`；报告 **80 → 76**（S110 9、PERF401 25），各定向测试 **2 passed**、合并根 marker **1383 passed、2 warnings、74.69s**，不启动 exporter、不访问网络或写入文档 |
| v1.19 | 2026-09-20 | exchange-support 生成器与 GitHub 治理校验器的顺序分别以本地契约保护后收敛两个 `PERF401`（**3/9 passed**）；质量/格式棘轮脚本的两个仅类型 `Sequence` 导入移入 `TYPE_CHECKING`（合计 **41 passed**）。报告 **76 → 72**（S110 9、PERF401 23、TC001 22、TC003 18），新鲜根 marker **1385 passed、2 warnings、76.91s**；不写文档、不访问 GitHub API、不更新快照或修改子仓 |
| v1.20 | 2026-09-20 | 两份字节一致的慢测试分析器以临时 JSON 锁定多条记录、缺失字段和顺序后同步收敛 `PERF401`（**2 passed**）；报告 **72 → 70**（S110 9、PERF401 21、TC001 22、TC003 18），新鲜根 marker **1387 passed、2 warnings、75.63s**；不运行主入口或访问网络 |
| v1.21 | 2026-09-20 | 两份字节一致的覆盖率分析器以 stub 锁定未测试交易所排序、模块行、低覆盖率筛选/顺序和全文后同步收敛 `PERF401`（**2 passed**）；报告 **70 → 68**（S110 9、PERF401 19、TC001 22、TC003 18），新鲜根 marker **1389 passed、2 warnings、74.91s**；不运行主入口、`pytest.main()` 或写出报告 |
| v1.22 | 2026-09-20 | 两对字节一致的 capability 提取器先以纯内存完整 Markdown/CSV 契约锁定非排序输入、能力字段与回退后同步收敛 **6** 个 `PERF401`（各 **2 passed**、各自 `cmp` 一致）；四个测试模块仅类型 `Path`/`AsyncIterator` 移入 `TYPE_CHECKING` 收敛 **4** 个 `TC003`。报告 **68 → 58**（S110 9、PERF401 13、TC001 22、TC003 14）；不触发临时 venv/pip 的确定性范围 **42 passed**。两项 artifact-first 临时环境测试在 `PIP_NO_INDEX=1` 下因本地包为 `unavailable` 保持 NOT_RUN，原断言未放宽；本版根 marker 待复核 |
| v1.23 | 2026-09-20 | 完整离线根回归取得 **1390 passed、3 failed、2 warnings**；失败为两项 artifact-first 临时环境测试与一项 wheel contract，均因本地 `bt_api_base` 工件不可用。明确排除这 3 个工件前置条件后的确定性根范围 **1390 passed、2 warnings**；不将该子集表述为完整通过，AC-12 为 NO-GO，等待符合 `PIP_NO_INDEX=1` 的本地工件后复核 |
| v1.24 | 2026-09-20 | 两个质量分析器的 Python 遍历先以临时目录/确定性 walk 锁定输出顺序、隐藏文件过滤和排除目录剪枝，再各自等价改为列表推导加 `extend`（**2 passed**）。`PERF401` **13 → 11**、报告 **58 → 56**；不调用主入口或写出质量报告，离线工件导致的 AC-12 NO-GO 不变 |
| v1.25 | 2026-09-20 | 两个代码质量检查器的目录收集先以临时目录/确定性 walk 与 `check_file`/report 替身锁定默认/自定义排除、隐藏目录/文件、顺序、verbose、更新和异常隔离，再各自等价改为列表推导加 `extend`（**6 passed**）。`PERF401` **11 → 9**、报告 **56 → 54**；不调用 CLI 或写报告，离线工件导致的 AC-12 NO-GO 不变 |
| v1.26 | 2026-09-20 | 本轮最终根门禁、文档合同和严格构建均通过；完整离线根回归为 **1398 passed、3 failed、2 warnings**，三项失败仍仅为 `bt_api_base` 本地工件前置条件。排除三项后的确定性范围 **1398 passed、2 warnings** 不替代完整 marker，AC-12 维持 NO-GO |
| v1.27 | 2026-09-20 | 两个 docstring 工具的文件发现函数先以临时目录/确定性 walk 固定默认/自定义排除、动态 `*.egg-info`、隐藏目录/文件与排序结果，再各自等价改为列表推导加 `extend`（**4 passed**）。`PERF401` **9 → 7**、报告 **54 → 52**；不调用 CLI 或外部服务，本批完整根 marker 待重跑 |
| v1.28 | 2026-09-20 | 最后 7 个根 `PERF401` 已以离线行为契约清零，根及测试 ignore 和报告规则已移除；`make tech-debt-report` 现为 **3 条 / 51 项**（S110 9、TC001 22、TC003 20）。新增 46 目标 `typing.get_type_hints` 回归，纠正仅依据延迟注解的 type-only 移动并恢复直接导入；完整离线根回归 **1452 passed、3 failed、2 warnings**，本机 `bt_api_base` 工件前置条件未满足，AC-12 保持 NO-GO |
| v1.29 | 2026-09-20 | 最后 9 个根 `S110` 先以 fake-only 契约锁定 best-effort 清理/继续接收语义，再仅记录异常类型；根/测试 ignore 与报告规则移除，CTP vendor 例外保留。`make tech-debt-report` 收缩为 **2 条 / 42 项**（TC001 22、TC003 20）；本批完整根 marker 待新增回归纳入后重跑 |
| v1.30 | 2026-09-20 | 四个未进入运行时反射面的测试专用 `Path`/`ModuleType` 导入仅移入 `TYPE_CHECKING`，根 `TC003` **20 → 16**、报告 **42 → 38**；反射契约 **46 → 57 passed**。完整离线根 marker 为 **1475 passed、3 failed、2 warnings、111.06s**，显式排除三项本地 `bt_api_base` 工件前置条件后为 **1475 passed、2 warnings、89.21s**；子集不替代完整 marker，AC-12 仍 NO-GO |
| v1.31 | 2026-09-20 | 五个私有风险检查 mixin 的 `RiskMetrics` 导入在反射/行为契约保护下移入 `TYPE_CHECKING`，根 `TC001` **22 → 17**、报告 **38 → 33**；并修复订单/持仓六个限额 helper 的 `CRITICAL` 分支未初始化 `warning` 缺陷。反射契约为 **58 passed**，风险测试 **34 passed**、组合 **106 passed**；完整离线根 marker **1484 passed、3 failed、2 warnings、56.45s**，显式排除三项工件前置条件后 **1484 passed、2 warnings、47.72s**，AC-12 仍 NO-GO |
| v1.32 | 2026-09-20 | 独立审查推翻 v1.31 对五个私有 helper 的安全判断：恢复其运行时 `RiskMetrics` 导入，并将五个继承到 `LimitsManager` 的 helper 纳入反射守卫（**58 → 63 passed**）。仅 `PolicyEngine` 的局部 `Callable` 注解通过审计后移入 `TYPE_CHECKING`，报告真实收敛为 **38 → 37**（TC001 22、TC003 15）。完整离线根 marker **1489 passed、3 failed、2 warnings、55.42s**，诊断范围 **1489 passed、2 warnings、42.97s**；AC-12 仍 NO-GO |
| v1.33 | 2026-09-20 | 修正 AC-12 工件根因并落地严格离线闭包：缺失 `bt_api_base` 是首个失败点，但 PEP 517 构建工具、pytest 运行时依赖也必须在本地闭包中可解析。validator/verifier 只接收绝对显式 wheelhouse，保留默认构建隔离、fresh venv、完整依赖解析与 `pip check`，且拒绝复用旧 venv/wheel。完整 `PIP_NO_INDEX=1` 根 marker **1499 passed、2 warnings、97.25s**；AC-12 通过。 |
| v1.34 | 2026-09-20 | 修正 required 聚合遗漏：`quality-gate` 现显式阻断 `quality-ratchet`，并锁定 `if: always()`，结构合同 **45 passed**。同时把子仓安全从“泛化扩容”改为分阶段准入：当前默认 Bandit profile + `--ignore-nosec` 预检为 26 项，CTP `B507` High 先由子仓独立修复；不得将 High 写入快照后伪装为绿色门禁。 |
| v1.35 | 2026-09-20 | 补齐插件认证诊断的 fail-closed 需求：`certified` 必须同时满足配置 tier、实体证据卡、安装、最低版本和入口点；否则按既有状态降级。公开契约 **11 passed**，完整离线根 marker **1510 passed、2 warnings、99.49s**。 |
| v1.36 | 2026-09-20 | 补齐两项 fail-closed 质量需求：质量棘轮对快照与发现范围双向比较，范围变化只能经审查后的 `--force-update` 重基线；单个可选 broker entry point 的常规失败不得阻断内置或后续健康适配器，枚举错误与 `BaseException` 保持可重试。最新完整离线根 marker **1522 passed、2 warnings、89.88s**。 |
| v1.37 | 2026-09-20 | 补齐 `.gitmodules`/gitlink 的严格 PR 治理需求：元数据或实际 gitlink 路径但缺 `submodules_changed=true` 和 SHA 上下文必须 fail-closed，同时精确排除普通 `bt_api/` 文件。最新完整离线根 marker **1526 passed、2 warnings、99.41s**。 |
| v1.38 | 2026-09-20 | 将 PR governance 升级为 schema v2 的逐路径 gitlink 事实与可信 base 采集，并补齐 SourceSupervisor 并发、文档 CI、optimized 安全/wheel smoke 和 same-candidate 发布要求。最终 checkout-local 根回归为 **1569 passed、2 warnings、68.47s**；显式 `--cov-branch` 复跑为 **1569 passed、2 warnings、103.02s**，覆盖率 **66.94%**。 |
| v1.39 | 2026-09-20 | 将 canonical 测试 runner 收紧为默认 `not ctp`、覆盖率启用 branch coverage 且 pytest/tee 任一失败即失败；optional performance 仅保留手工 opt-in 且对缺目录/报告/非空 benchmarks fail closed。最新 checkout-local 根回归为 **1595 passed、2 warnings、124.16s**；显式 `--cov-branch` 复跑为 **1595 passed、2 warnings、152.81s**，覆盖率 **66.94%**。真实 GitHub Actions/benchmark/network/CTP/SimNow 仍 **NOT_RUN**。 |
| v1.40 | 2026-09-20 | 补齐 CI full-suite baseline coverage 的信任边界：`-n 8` + branch coverage，threshold 只经 step env 且按 0..100 十进制白名单验证，XML/HTML 报告与 artifact 均 fail closed，Codecov 仍仅可选遥测。最新 checkout-local 根回归 **1615 passed、2 warnings、63.32s**；branch coverage 复跑 **1615 passed、2 warnings、74.40s** / **66.94%**。本地结构合同 **20 passed**，GitHub hosted **NOT_RUN**。 |
| v1.41 | 2026-09-20 | same-candidate 发布的本地候选身份闭包已实现：手动 dispatch 仅构建，外部三段统一受 release + 显式 enable gate 保护；候选清单及独立 wheel/sdist/manifest 摘要锚、TestPyPI 精确下载与 PyPI-only 依赖解析均 fail closed。本地候选/工作流与既有发布资源定向合同 **54 passed**；最新根回归 **1669 passed、2 warnings、111.54s**，branch coverage **1669 passed、2 warnings、215.92s** / **66.94%**。真实 hosted/TestPyPI/PyPI/OIDC/Environment/Ruleset 均 **NOT_RUN**，AC-17 仍 **PARTIAL / NO-GO**。当前 dirty OKX 另有 format-ratchet **87 > 86** 回退，不属于发布闭包通过证据。 |
| v1.42 | 2026-09-21 | 追加执行日志迁移的结构与原子性切片：`migrate_execution_journal` **361 → 128**，以 frozen preparation 对象和私有 helper 固定阶段边界；提交事务改为先原子写入独立 committed payload，再改变内存状态，消除写失败时跳过 rollback 的缺陷。失败注入定向 **6 passed**、execution-session 文件 **161 passed**；最新根回归 **1671 passed、2 warnings、95.99s**，branch coverage **1671 passed、2 warnings、108.50s / 67.11%**。外部发布、CTP 与性能边界不变；dirty OKX format-ratchet 仍为 **87 > 86**，L1 mypy 当前为 **3 errors**。 |
| v1.43 | 2026-09-21 | 收口 S-07：`scripts/*.py` 作为 8 个分析脚本的唯一 canonical 实现，`scripts/analysis/` 保留惰性 import、canonical 已有属性读写及删除/恢复转发和 `runpy` CLI 兼容 wrapper；独立审查先后发现首版只读代理不能转发 `wrapper.CORE_DIR = value`、`patch.object` 退出不能恢复及 `Path` 名称遮蔽，修复后兼容合同 **41 passed**、顶层 8 文件 SHA256 前后不变、复审 **PASS**。Forwarding schema/state/account-state 的 8 份计划经实现与 **59 passed** 联合复核转为已落地，计划债由待确认 **36 → 28**、已落地 **30 → 38**。最新根回归 **1696 passed、2 warnings、69.74s**，branch coverage **1696 passed、2 warnings、93.40s / 67.11%**。 |
| v1.44 | 2026-09-21 | 完成剩余 28 份计划的三批独立复核：ELK/Logstash **34 passed**，CI/runtime 相关 **472 passed** 且 Bandit/定向 mypy 通过，monitoring/forwarding/ZMQ **119 passed**；精确 CI mypy 仍有 **77 errors**，精确 Ruff format 仍有 1 个 owner 文件失败，因此真实分布为已落地 **61**、已废弃/阻塞 **8**、部分/未开工 **5**、待确认 **0**。追加安全结构切片：`coerce_funding_snapshot` 在公共签名与错误语义不变的前提下由 **81 → 28** 行；独立审查发现并纠正外层 `Mapping.source` 被提前读取的回归，最终 mapping/dataclass 边界合同 **17 passed**。新鲜根回归 **1706 passed、2 warnings、93.10s**，branch coverage **1706 passed、2 warnings、115.70s / 67.24%**；coverage 退出后另有 N-19 的插件线程 WATCH。 |
| v1.45 | 2026-09-21 | 精确 CI mypy 的首个脚本批次完成：`fill_missing_docstrings.py` 以 `TypedDict` 和精确 AST 联合类型消除 **20** 个错误，整仓范围 **77 → 57 errors（14 → 13 files）**。新增行为契约同时发现并修复多行 body 缩进、同索引 action 覆盖、跨行定义头 inline suite、tab 缩进与 UTF-8 文件的 PEP 263 cookie 位置等真实源码破坏风险；生成内容在写入前必须重新解析且确认目标 docstring 已形成，否则 fail closed、不写文件。定向 **23 passed**，目标 Ruff/format/mypy、diff-check 与独立复审 PASS；完整根 marker **1725 passed、2 warnings、100.03s**，同范围 branch coverage **1725 passed、2 warnings、119.54s / 67.24%**。极少见 split-line `async \\` / `def` 语法保持安全拒绝，不伪装为已支持。 |
| v1.46 | 2026-09-21 | F-85 后实时报告暴露 4 个新增 `TC003`：三个测试注解专用类型经运行时用法核验后移入 `TYPE_CHECKING`；脚本公开 `Iterable[str]` 的真实 `get_type_hints` 回归证明其是运行时反射依赖，故保留直接导入和报告命中。四测试 **48 passed**、目标 mypy/Ruff/format 与独立复审 PASS；报告 **40 → 37**（TC001 22、TC003 15），只计 3 个真实清理，不以别名、ignore 或基线隐藏脚本项。该阶段完整根 marker **1725 passed、2 warnings、89.32s**。 |
| v1.47 | 2026-09-21 | 补齐质量棘轮的 force-update 信任边界：有现存 baseline 时禁止删除任何 recorded scope；受审查扩张必须单独复扫旧范围且不得抬高旧债；same-scope 直接比较首次扫描结果。新增范围缩小、扩张时旧范围回升、同范围回升三条不写快照故障注入，普通 `--update` 语义不变。 |
| v1.48 | 2026-09-21 | Task33 收敛 artifact-first 路径的 5 个 mypy 边界错误：环境以 `Mapping[str, str]` 接收，先由真实运行时 guard 拒绝非映射输入再复制；公开 wheelhouse 在创建 artifacts 前将 `Path | str | None` 单次归一化，`Distribution.locate_file()` 的 `SimplePath` 显式转换为本地 `Path` 后继续 fail closed 校验。目标测试 **12 passed**、四文件 mypy 为 0、债务保持 37、独立复审 PASS；可比 254 文件范围 **57 → 52 errors（13 → 11 files）**，明确不替代 Task30 的精确 256 文件证据。最新根 marker 与 branch coverage 均为 **1731 passed、2 warnings**，覆盖率 **67.24%**。 |
| v1.49 | 2026-09-21 | Task34 收敛三个测试文件的 4 个 mypy 错误：AST import 节点显式窄化，PyArrow/YAML 假模块以精确 `ModuleType` 子类声明运行时属性。三文件 mypy **4 → 0**、定向 **7 passed**，债务保持 37、独立复审 PASS；可比 254 文件范围 **52 → 48 errors（11 → 8 files）**，Task30 的精确 256 文件证据仍为 **57 errors / 13 files**且未重跑。最新根 marker **1731 passed、2 warnings、92.95s**，branch coverage **1731 passed、2 warnings、135.56s / 67.24%**。 |
| v1.50 | 2026-09-21 | Task35 收敛 `tests/scripts/test_scrape_margin_docs.py` 与 `tests/scripts/test_get_ibkr_cookie.py` 的 8 个 mypy 错误：Playwright、browser-cookie3 与 requests/requests.exceptions 假模块使用精确 `ModuleType` 子类，不启动浏览器或网络。两文件 mypy **8 → 0**、定向 **4 passed**。fresh `--no-incremental` 且排除两份禁读测试的可比 254 文件当前快照为 **41 errors / 7 files**；该运行额外暴露一个既有 `_execution_session.py` 注解错误，因此不将 Task34 的 **48/8** 与本值写成直接因果差值；精确 256 文件命令未重跑。 |
| v1.51 | 2026-09-21 | Task36 收敛 `tests/test_production_monitoring_demo.py` 与 `tests/test_hyperliquid_integration_diagnostics.py` 的 18 个假模块属性错误，两文件 mypy **18 → 0**、定向 **7 passed**。独立复审发现并修复 `config_loader.load_exchange_config` 只有注解、运行时未绑定属性却以 `raising=False` 注入的假接口：fixture 现先绑定安全失败占位函数，异常注入恢复默认 `raising=True`。同一 fresh 254 文件范围 **41 → 23 errors（7 → 5 files）**，精确 256 文件命令未重跑。最终根 marker **1734 passed、2 warnings、91.45s**，branch coverage **1734 passed、2 warnings、119.06s / 67.27%**；真实监控服务、Hyperliquid 与网络均未运行。 |
| v1.52 | 2026-09-21 | Tasks37–41 / F-92–F-96 完成剩余可比 mypy 库存的分批收敛：Task37 以精确 key/value 注解将 `_execution_session.py` **1 → 0**，execution-session **164 passed**、`make type-check` **128 source files PASS**；Task38 以精确 `_Event` union 将 OKX WSS 诊断测试 **5 → 0**、**3 passed**；Task39 将 YAML `object` 逐路径窄化为运行时 mapping/list/string，增加坏形状负例后 workflow 测试 **5 → 0**、**21 passed**；Task40 用 connection/server/socket/receiver Protocol 保持 `websockets>=12` 公共边界并以 `port=0` 避免 Anki 8765 冲突，主目标 **11 → 0**，fresh 跨文件检查另修复 consumer 把 `_receive_messages` 标成 `AsyncMock` 导致的 1 个错误；当前 15.0.1 两文件 **8 passed**，独立复审在 13.1/15.0.1 各 **7 passed**，12.0 未安装且 **NOT_RUN**，仅核对官方公共 API shape 兼容；Task41 用 `list[str]` fake command 与 `CompletedProcess[str]` 将 isolated probe **1 → 0**、**8 passed**。联合目标 **203 passed**，各批独立复审均 PASS。同一 fresh `--no-incremental` 254 文件范围由 Task36 的 **23 errors / 5 files** 降至 Tasks37/38 后 **17/3**，最终 **0/0**；精确 256 文件命令仍未重跑。本批仅目标 TC 扫描无新增，未重跑 fresh `make tech-debt-report`，最近历史债务仍为 **37**。最终根 marker **1735 passed、2 warnings、88.69s**，branch coverage **1735 passed、2 warnings、110.06s / 67.27%**。 |
| v1.53 | 2026-09-21 | Task42 / F-97 建立 AC-19 的本地 singleton 性能能力：精确 orderbook `normalize_event` node 以 `-n 8` 运行，版本化基线要求 mean ≤ **0.0005s**、rounds ≥ **1000**，checker 对坏 JSON、重复/缺失/额外 benchmark、非有限或非正数值和超限均 fail closed。定向 **59 passed**；7 次本地有效报告 mean **8.096–32.879µs**、rounds **3613–14608**。该阈值只承诺灾难性退化安全上限（约 ≥2000 events/s），不是敏感趋势检测；7 条空闲 xdist worker warning 为已知噪声。最新完整根 marker **1796 passed、2 warnings、95.29s**；GitHub hosted、多 benchmark 聚合与 schedule 恢复仍 **NOT_RUN / NO-GO**。 |
| v1.54 | 2026-09-21 | Task43 / F-98 在不改变 `OrderRequest` 19 字段、frozen/signature/反射及 17 项异常顺序与文本的前提下，将 `__post_init__` 拆为复杂度 **9/10** 的两个私有校验段，入口 C901 **18 → 1**；`models.py` C901 **3 → 2**、主包 **84 → 83**。新增精确边界与冲突优先级合同后，七文件直接消费者 **81 passed**、目标行/分支覆盖完整、128 source mypy PASS、独立复审 PASS；最新完整根 marker **1824 passed、2 warnings、127.85s**。Task43 后未重跑 branch coverage，外部 hosted/发布/CTP 与子仓阻塞状态不变。 |

---

## 1. 背景

`bt_api_py` 已有相当完整的质量基础设施：

- **标准与命令**：`docs/CODE_QUALITY.md`（工具链与历史优化记录）、`Makefile`（lint/format/type-check/security-scan/check）。
- **每仓测试基线**：`docs/code-quality-baseline.md` 定义 `bt_api_*` 三档测试基线（L1/L2/L3）并判定"空壳测试"。
- **质量基线快照**：`docs/acceptance/2026-08-17-quality-baseline.json` 记录了 2026-08-18 的实测值。
- **BMAD 质量迭代**：`docs/plans/` 下 74 份计划，其中 **67 份**为 2026-06-17 批量生成的 `bmad-*-quality-iteration-plan.md`。
- **CI 门禁**：`.github/workflows/tests.yml` 的 Quality Gates（ruff / ruff format / mypy / bandit / pip-audit / gitleaks）与 Full Suite（覆盖率门槛）。

但**实测结果显示质量在退步**：门禁范围 ruff 违规从 2026-08-18 基线记录的 **642** 涨到当前的 **1301**（+659，约一个月），
其中包含 **103 个 `F821 undefined-name`**（潜在运行时 `NameError`）。问题根源不是"没人扫"，
而是**门禁存在覆盖盲区 + 债务没有台账**（见 §3）。

---

## 2. 目标

在不扩展业务功能、不重写架构的前提下，把质量从"局部干净、整体退步"扭转为"**全局有门禁、债务有台账、只降不升**"。

1. **止血**：修掉真实缺陷（103 个 F821）与安全泄漏（真实账号入库）。
2. **堵漏**：把 CI/本地门禁从 `bt_api_py/` + `tests/` 扩到全部 `bt_api/*/src`（≈25 万行），并建立"棘轮"防止回退。
3. **清存量**：把可机械修复的 lint 债（783 项自动可修）清零，按子仓分批落地。
4. **补短板**：类型注解完整率（当前 23%）与 docstring 覆盖率（公开函数 66%）按批次提升。
5. **治结构**：18 个超 800 行文件（最大 8864 行）给出可执行的拆分方案与优先级。
6. **理台账**：把散落在 74 份计划与 `ruff ignore` 列表里的债务，收敛为一份可执行、可验收的登记册。
7. **修配置债**：失效的 `pyrightconfig.json`、`AGENTS.md` 引用的缺失规则文件等。

## 3. 质量现状实测基线（2026-09-19）

### 3.1 规模

| 项 | 实测 |
|----|------|
| 源码 `.py`（排除 build/dist/site/缓存） | **1326 文件 / 330,205 行** |
| 按目录 | `bt_api/bt_api_ctp` 70,411 行 · `bt_api_py` 49,451 · `examples` 34,216 · `bt_api/bt_api_okx` 33,749 · `tests` 30,809 · `bt_api/bt_api_binance` 29,294 · `bt_api/bt_api_base` 25,297 · `scripts` 14,166 |
| git 跟踪文件 | 794 |

### 3.2 静态检查（ruff，门禁范围 1301 违规）

| 规则 | 数量 | 性质 | 是否自动可修 |
|------|------|------|-------------|
| `W291` trailing-whitespace | 712 | 机械 | 是 |
| `F401` unused-import | 164 | 机械 | 是 |
| **`F821` undefined-name** | **135** | **真实缺陷（潜在 NameError）** | 否 |
| `I001` unsorted-imports | 154 | 机械 | 是 |
| `E701` multiple-statements | 36 | 风格 | 否 |
| `S106` hardcoded-password | 45 | 测试桩（误报为主） | 否 |
| `PIE790`/`F541`/`TC002`/`UP006`/`UP042`/`F841`/`B017`/`E402` 等 | 9/21/4/4/3/3/2/2 | 杂项 | 部分 |

**关键分布**：`F821`（完整范围 135 处）全部集中在**被机械切分的文件**——
`bt_api_binance/feeds/rest_market.py`（14）、`bt_api_okx/feeds/live_okx/mixins/*`（`ta_part1~3`、`copy_trading_mixin_part1~3`、`rfq_mixin_part1~3`、`statistics_mixin_part1~2` 等 ≈89）。
这类"把一个巨型文件切成 `*_partN`/`*_mixin`"的文件全仓共 **46 个**（`bt_api_okx` 42 个、`bt_api_base` 2 个、测试 2 个），
切分时丢失 import 是 `F821` 的直接成因。
`W291`/`F401` 集中在 `bt_api_binance`（src + tests）。

### 3.3 代码内部质量（AST 扫描）

| 指标 | 实测 |
|------|------|
| 函数总数 | 18,533 |
| 函数 docstring 覆盖 | **59%**（公开函数 66%） |
| 类 docstring 覆盖 | 58% |
| 参数注解完整率 | **23%**（4,338 / 18,533） |
| 函数长度中位数 | 7 行（健康） |
| 超长函数 | >150 行 **42** 个；>80 行 **188** 个 |
| 最长函数 | 602 行（`test_binance_account.py` 测试）、`_execution_session.py` 内 425/401/376/361 行 |

### 3.4 超长文件（门禁阈值 800 行，`scripts/check_file_sizes.py`）

**18 个文件超限**，Top 6：

| 行数 | 文件 |
|------|------|
| 8864 | `bt_api_py/bt_api.py` |
| 8251 | `bt_api_py/_execution_session.py` |
| 4002 | `bt_api/bt_api_ctp/src/bt_api_ctp/ctp/client.py` |
| 2511 | `bt_api_py/_normalization.py` |
| 2025 | `bt_api/bt_api_ctp/src/bt_api_ctp/feeds/live_ctp_feed.py` |
| 1832 | `bt_api_py/ctp_close_plan.py` |

> 说明：`ctp_structs_*.py`（6352/5566/5315/4670/4287 行）为 CTP 结构体映射，属生成/半生成代码，
> 不在拆分范围（见 §4 非目标）。

### 3.5 测试与覆盖率

| 项 | 实测 |
|----|------|
| 根测试收集 | 1523（CI 基线标记过滤后 1321） |
| 测试文件 | 根 `tests/` 86 个 |
| 覆盖率门槛 | `pyproject.toml` 的 `[tool.coverage.report] fail_under = 40` 是默认来源；仅手工 `workflow_dispatch` 可通过已校验输入临时覆盖该次运行 |
| 实测覆盖率 | 根套件（CI 标记过滤、`-n 8`，`--cov=bt_api_py --cov-branch`）**67.27%**（1735 passed、2 warnings、110.06s） |
| 过期表述 | `docs/plans/2026-03-19-*.md` 称 `coverage fail_under = 80`，与实际 40 不符（属旧计划文档，已在台账登记订正） |
| 遗留失败（2026-08-17 基线记录） | 3 个（alpaca ×2、硬编码插件数 ×1）——本迭代复核时**均已通过** |

### 3.6 安全与仓库卫生

| 项 | 结论 |
|----|------|
| 真实密码是否入库 | **否** ✓（`yunjinqi2015` 仅存在于被忽略的 `collector.yaml`） |
| 真实账号是否入库 | **是** ⚠ `tests/bt_api_contract/test_gateway_runtime_integration.py` 含真实 CTP 账号 `089763`（≥8 处） |
| 构建产物是否入库 | 否 ✓（`build/`、`dist/`、`site/` 均被忽略；子仓内 `build/` 也未跟踪） |
| `.env` / `keys/` / `tmp_keys/` | 已忽略 ✓ |
| `stock_data/` | 已由 owner 在 `.gitignore` 覆盖（`daily/`、`adjustment_factors/`、`financials/`、`*.parquet`）；仅 `stock_data/README.md` 待入库（有意保留） |
| secret 扫描 | gitleaks 仅在 **PR** 事件运行，且未覆盖子仓 |

### 3.7 配置债与计划债

| 项 | 问题 |
|----|------|
| `pyrightconfig.json` | `extraPaths` 引用**不存在**的 `./bt_api_base/src`、`./packages/bt_api_coinbase/src`、`include` 含不存在的 `packages` |
| `AGENTS.md` | 引用 `@.joyincode/rules/backend.md`、`frontend.md`，**两文件均不存在** |
| `docs/plans/` | 基线时 74 份计划、67 份同日生成，**无索引、无状态标记**；现已由 `docs/plans/README.md` 索引并复核 **74/74**。最终分布为已落地 **61**、已废弃/阻塞 **8**、部分/未开工 **5**、待确认 **0**；状态基于当前工作树，不等同于 Git HEAD 或外部 CI 证明 |
| `ruff ignore` 列表 | 基线时渐进项只能靠行内说明；现已把仍在全局 ignore 的 2 条（`TC001`/`TC003`）登记到台账。最近一次完整 `make tech-debt-report` 的历史证据为 **37** 项（TC001 22、TC003 15）；本批只有目标 TC 扫描无新增，没有 fresh 全量报告。`PERF402`/`S113`、`PERF403`、`S112`、`PERF401` 与根 `S110` 均已移除全局 ignore；对 `TC001`/`TC003` 的实际治理还须证明 `typing.get_type_hints` 可解析既有公开注解，不能只以静态检查取代兼容性证据 |
| TODO/FIXME | 全仓 **0 处**（债务未用注释标记，只能靠外部文档承载） |

### 3.8 滚动快照（2026-09-20）

初始扫描数字保留在本节作为历史基线；当前阻塞棘轮快照为 **10** 项：`TC002×4`、`S106×3`、
`B017×2`、`E402×1`，**全部位于子仓**。原根仓 `S603`/`B404`/`B603` 已收敛为 E-05：静态命令/脚本、固定 cwd、
6 个环境键白名单、空输入拒绝和输出脱敏均由离线行为测试覆盖；它保留 CTP/SWIG 子进程隔离语义，
不使用配置级忽略。该状态仍是 **未收口**，不能把“棘轮通过”表述成全量 lint 通过。

根仓指定 2 条渐进 Ruff ignore 另有独立、只读库存：最近一次完整 `make tech-debt-report` 的历史证据为 **37** 项
（TC001 22、TC003 15）。Tasks37–41 仅执行目标 TC 扫描且无新增，未产生 fresh 全量债务报告。最后 7 个 `PERF401` 与最后 9 个根 `S110` 已在离线行为契约下清零，根/测试 ignore 和报告规则均已移除；全范围隔离扫描为 0。`tests/test_type_hint_introspection.py` 的 63 个目标说明仅靠 future annotations 或私有命名不足以支持 `TYPE_CHECKING` 移动，相关直接导入已恢复；该测试当前未跟踪，故为 **NOT_PERSISTENT** 本地观察。F-86 只处理 3 个经证明不进入反射面的测试注解；脚本公开 `Iterable[str]` 由真实 `get_type_hints` 回归证明属于运行时依赖后保留直接导入并继续计债。

工件闭包落地 marker 为 **1499 passed、2 warnings、97.25s**；Task43 后最新完整根 marker 为 **1824 passed、2 warnings、127.85s**。最近一次显式 `--cov-branch` 同范围复跑仍为 Task41 的 **1735 passed、2 warnings、110.06s**，覆盖率 **67.27%**；Task42/43 后未重跑 coverage，不把旧分母冒充新证据。先前 coverage 汇总后的 `pytest-rerunfailures 14.0` daemon thread warning 本轮未复现，但继续登记为 N-19 WATCH。当前 dirty checkout 本地证据不等于 clean-checkout、外部 CI 或发布证明。artifact-first 路径以显式绝对 wheelhouse、默认 PEP 517 隔离、fresh venv、完整解析和 `pip check` 验证本地闭包，不能借由网络、旧 artifacts 或 system site-packages 通过；Task33 进一步固定真实运行时 `Mapping` guard 与复制、wheelhouse 入口单次归一化和 `SimplePath` 到本地 `Path` 的显式边界；Task35/36 将离线脚本、生产监控与 Hyperliquid 测试中的假模块属性收敛为精确运行时 `ModuleType` 子类，并在复审后将 `config_loader` 改为先绑定安全失败占位、再以默认 `raising=True` 注入异常。

根 `lint` 与主包 `mypy` 当前 PASS；Task30 的精确 256 文件 mypy 证据为 **57 errors（13 files）**；Task33/34 在明确排除两份禁读测试后的历史可比 254 文件范围依次为 **52 errors（11 files）**、**48 errors（8 files）**。Task35 重新执行 fresh `--no-incremental` 后快照为 **41 errors（7 files）**，因同时暴露既有 `_execution_session.py` 注解错误，不与 Task34 的 48/8 作直接因果差值；Task36 在同一 fresh 口径下降至 **23 errors（5 files）**，Tasks37/38 后为 **17 errors（3 files）**，Tasks39–41 完成后最终为 **0 errors / 0 files**。这些只是排除两份禁读测试后的 fresh 254 文件可比证据，不冒充精确 256 文件 CI 复跑。`type-check-l1` 因已持久化 OKX handoff 候选的 `registry_registration.py:120,174` 缺注解与 `market_wss_base.py:906` 的参数类型不兼容失败（**3 errors**），须在后续修复后复核。CTP `B507` 仍为 NO-GO；same-candidate 的本地候选身份闭包已落地，但外部发布 P0 仍为 **PARTIAL / NO-GO**。CTP vendor 的范围化 `S110` / `S112` per-file 例外保持不变。该库存不纳入
10 项子仓 lint 棘轮余量，亦不因报告存在而视作治理完成。

## 4. 非目标（本期明确不做）

- **不重写架构、不改业务行为**：质量迭代不引入功能变更。
- **不治理 CTP 生成代码**：`ctp_structs_*.py`、`ctp_constants.py`、`ctp_wrap.*` 属 SWIG/结构体映射产物，保持"例外面单"。
- **不以批量导入移动换取类型债下降**：剩余 `TC001`/`TC003` 必须逐项通过运行时反射与最小行为回归，不能以 future annotations、扩大 ignore 或 `noqa` 代替。
- **不改子仓的发布节奏**：子仓改动按各自 `dev` 分支提交，是否需要发版由 owner 决定。
- **不做跨仓大重构**：`bt_api_py` 与 `bt_api_*` 的接口契约保持不变。

## 5. 功能需求（质量项）

### FR-1 止血：真实缺陷与安全

- `F821`（135 处）逐处定位修复：优先 `bt_api_okx` 的 `*_partN|*_mixin` 切分遗留（补 import 或删死代码），
  其次 `bt_api_binance/feeds/rest_market.py`。
- 清除 `tests/bt_api_contract/test_gateway_runtime_integration.py` 中的真实账号，改为占位符（`EXAMPLE_ACCOUNT` 等）。
- 处置 3 个遗留失败测试：要么修复（安装/降级断言），要么显式 skip 并登记原因。

### FR-2 门禁扩容与棘轮（核心）

- CI Quality Gates 与 `Makefile` 的 ruff / mypy 范围从 `bt_api_py/ tests/` 扩到 **`bt_api/*/src`**（分组执行，避免单 job 超时）；Bandit 采用下述独立阶段，避免把主包 profile 误当成子仓安全策略。
- 主包 `make security-scan` 保持其现有配置范围；子仓静态安全必须使用独立的、默认 Bandit profile 加 `--ignore-nosec` 的 source-only 扫描，逐仓验证范围和 JSON 错误并 fail closed。该棘轮以未豁免 High = 0 为绝对准入条件：当前 CTP `B507` High 修复前不得建立绿色 required 基线，也不得用根配置的 `B101/B104/B105` skip 缩小子仓策略。
- 建立**棘轮（ratchet）**：记录当前违规快照为基线，此后**只允许下降**；新违规即 CI 失败。快照范围与当前发现范围必须经尾部斜杠归一化后双向一致；普通门禁和 `--update` 都拒绝范围漂移；经审查的 `--force-update` 也只可记录安全扩张，禁止删除既有 scope 或抬高旧范围债务。
- `pyproject.toml` 恢复 `[tool.coverage.report] fail_under`，与 CI 阈值**同源**（消除 40 vs 80 的不一致）。
- 覆盖率门槛先从 40% 提到阶段性目标（见 FR-6），并在文档中写明"当前值 + 目标值"。

### FR-3 存量清理（机械项）

- 自动可修项（`W291`/`F401`/`I001`/`PIE790`，约 783 处）用 `ruff check --fix` 分批清理。
- 不可自动修项（`E701`、余下 `F401` 等）人工处理，**按子仓分批提交**，每批保持测试全绿。
- `S106`（22 处，均在测试）通过 per-file-ignores 或改用占位常量消除误报噪音。
- CTP/SWIG 探针如确需 `S603`/`B404`/`B603` 行级例外，必须先收敛为固定命令和静态脚本；调用方仅能使用白名单环境键，
  返回详情必须脱敏，并有不启动子进程的行为测试。禁止以 `per-file-ignores` 扩大豁免范围。

### FR-4 类型与文档补齐

- 公开 API（`bt_api_py/bt_api.py`、`exceptions.py`、各子仓 `feeds/*` 对外方法）优先补注解与 docstring。
- mypy 覆盖：主包 → 头部 5 个 L1 子仓（binance/okx/bybit/gateio/hyperliquid）。
- 量化目标：公开函数 docstring 覆盖 66% → 85%；参数注解完整率 23% → 45%（主包优先）。

### FR-5 结构治理（不重写）

- 为 Top 5 超长文件（`bt_api.py`、`_execution_session.py`、`client.py`、`_normalization.py`、`live_ctp_feed.py`）
  产出**拆分方案**（边界、风险、回归测试、是否本期执行），不要求本期完成拆分。
- 超长函数（>150 行，42 个）登记清单并按风险排序，本期至少处理主包内 5 个。
- Task15 已完成 **5/5**：OKX/Binance 只读 readiness mapper、`_ctp_budget.evaluate_ctp_budget`、`_normalization.normalize_event` 与 `BtApi.get_order_readiness` 均按单函数等价拆分完成。后续独立切片又将 `migrate_execution_journal` **361 → 128**，并修复 COMMITTED 事务写失败时跳过 rollback 的原子性缺陷；失败注入证明所有非空 registry manifest 精确恢复。当前剩余 **16** 个超长函数仍须按风险逐项治理，不能以方案卡替代。
- Task43 追加治理未达 150 行但圈复杂度超标的 `OrderRequest.__post_init__`：只按原校验序列拆成两个私有段，入口 C901 **18 → 1**，helper 分别为 **9/10**；不得借结构拆分收紧字段 truthiness、不可哈希输入或异常优先级合同。
- `check_file_sizes.py` 阈值维持 800，但把"例外清单"显式化（生成代码 vs 待拆分）。

### FR-6 覆盖率

- 主包 `bt_api_py` 覆盖率从当前基线提升到 **55%**（P1 目标；P2 可再议 60%+）。
- 高风险模块（`forwarding/`、`gateway/`、`brokers/`、`_execution_session.py`）优先补有意义的测试
- 本轮 CI 同标记、`-n 8` 且显式 `--cov-branch` 的新鲜结果为 **67.27%**（**1735 passed、2 warnings、110.06s**），已满足 55% 阶段目标；
  `pyproject.toml` 的 blocking 门槛维持 40%，后续提高须再次以新鲜覆盖率复算。
  （对齐 `docs/code-quality-baseline.md` 的反"空壳测试"要求）。
- 子仓测试基线按 `docs/code-quality-baseline.md` 的 L1/L2/L3 档位复核缺口。

### FR-7 台账与配置债

- 建立**技术债登记册**（单一入口），把：`docs/plans/` 74 份计划的现状、`ruff ignore` 的 "fix gradually" 项、
  超长文件/函数、遗留失败测试，统一登记为可勾选任务（含责任人/批次/状态）。
- 修复 `pyrightconfig.json`（指向真实路径或删除该文件并说明）。
- 修复 `AGENTS.md` 的失效引用（补 `.joyincode/rules/*.md` 或改为指向 `docs/CODE_QUALITY.md`）。
- `stock_data/` 的忽略规则已由 owner 落地（见 §3.6），本项只需复核覆盖完整性。
- 根 2 条渐进 Ruff ignore 必须由 `make tech-debt-report` 保持可复算；实际清理须逐条修改、配最小回归，并更新 `docs/TECH_DEBT.md` G-07/G-08，不能靠扩大 ignore 或新增 `noqa` 收口。对反射可见注解的 `TC001`/`TC003` 改动还必须新增或扩展 `typing.get_type_hints` 回归。

### FR-8 插件认证诊断（fail-closed）

- `PluginCatalog` 只能在以下五项均满足时输出 `certified`：配置的 `certification` 等于 `certified`、同名证据卡是实体 Markdown 文件、目标包已安装、版本不低于 `min_version`、并且对应 `bt_api.plugins` entry point 可发现。
- 缺卡、同名目录、未安装、版本不达标、入口点缺失或 `experimental` 配置均不得被卡片路径覆盖；须保留既有 `missing`、`loadable`、`installed` 的降级顺序。
- 此诊断只说明本地可发现性与配置/证据条件，不构成交易所连接、真实下单、外部认证或发布工件兼容性证明。

### FR-9 可选 broker entry point 隔离

- 单个 `bt_api.adapters` entry point 的 `load()`、注册函数或其常规诊断路径失败，均不得阻断内置 adapter 或同轮后续健康 entry point；本轮常规插件遍历结束后应避免重复触发同一失败插件。
- 元数据枚举/选择故障和 `BaseException` 不属于可选插件失败，必须向上传播且保持可重试；不得以宽泛捕获掩盖中断、退出或环境级故障。
- 日志只是诊断：取得 entry point 名称或写 warning 失败不得改变上述发现语义；不得承诺回滚坏插件在失败前已经产生的全局注册副作用。

### FR-10 PR governance schema v2 与可信 base（fail-closed）

- 采集器必须从可信 base 到当前 head 执行真实 `git diff --raw -z --no-renames --no-ext-diff --abbrev=40`，输出 schema v2：`changed_files`、逐路径 `gitlink_changes[]`（`path`、`operation=update|add|delete`、`old_sha`、`new_sha`）及 `collection_errors[]`。解析、Git 或对象探针失败必须进入错误集合并 fail closed。
- 校验器必须把 gitlink 身份绑定到具体路径，拒绝缺记录、重复记录、路径不匹配、短 SHA、零 SHA 冒充及未知 type-change；add/delete 可被识别，但当前策略仍拒绝。legacy 全局 `submodules_changed`、`old_sha`、`new_sha` 不再具有授权效力。
- base 必须由受信脚本解析并用 Git 对象探针确认可用；普通 `bt_api/readme.md`、`bt_api/install_and_test_all.py` 不得误判。`dev`、`master`、`code-optimization` 三分支均按 `strict=false` 部署、观察再转 strict 的两阶段 rollout；fork、三分支观察及 strict 启用在实际 GitHub 运行前均为 **NOT_RUN**。

### FR-11 SourceSupervisor 同键并发安全

- 同一 source key 的首次并发订阅必须只启动一次 upstream；最后一个租约释放时只停止一次，且 slow-stop 与新订阅不得交错造成误停。不同 key 仍可并行，空闲锁应可回收。
- 本地确定性契约已 **5 passed**；不将其表述为外部 feed、网络或生产并发证明。

### FR-12 文档 CI fail-closed

- 文档相关源码、配置、生成器与合同变化必须触发 docs workflow；安装失败不得通过 `--no-build-isolation`、`--no-deps`、fallback 或 `continue-on-error` 绕过。
- 严格构建前必须依次执行生成内容 `--check` 与文档合同。结构契约本地 **4 passed**；GitHub/Pages 运行 **NOT_RUN**。

### FR-13 optimized 安全与 wheel smoke（可选增强）

- optimized workflow 中 Bandit、pip-audit、构建和 wheel smoke 不得用 `|| true`、吞退出码或弱化依赖安装取得绿色；wheel smoke 必须检查实际导入与基本运行契约。
- 本地结构契约 **5 passed**；真实 GitHub Bandit/pip-audit/build/wheel 均 **NOT_RUN**，因此状态为 **PARTIAL**，且 CTP `B507` 继续阻断安全 NO-GO。

### FR-14 same-candidate 正式发布链（本地结构已实现，外部未验收）

- 正式发布必须按同一运行内的 `build → publish-testpypi → smoke-install-testpypi → publish-pypi` 串联，并绑定 source SHA、版本、wheel/sdist 文件名与 SHA256、候选 manifest SHA256 及 `SHA256SUMS`。手动 dispatch 只能构建/归档候选，不能写任何 registry；三个外部阶段必须同时满足 `release` 事件与 `ENABLE_PYPI_RELEASE == true`。
- smoke 必须从 TestPyPI 以有界重试下载精确候选，在 fresh 目录核验文件名/摘要，再从已校验本地 wheel 安装；依赖只能从 PyPI 解析，生产发布复用并再次验证原始 build artifact，不得重建或 `skip-existing`。本地候选/工作流与既有发布资源定向合同 **54 passed**。
- GitHub hosted、TestPyPI/PyPI、真实 OIDC/Environment/trusted publisher/Ruleset 均 **NOT_RUN**；特权 workflow 的第三方 action 仍需从官方来源核验并固定不可变完整 SHA，且 `release: published` 的 Release 元数据会先于 smoke 存在。因此正式发布维持 **PARTIAL / NO-GO**。

### FR-15 Canonical 测试 runner fail-closed

- `scripts/run_tests.sh` 必须默认应用 `not ctp` marker；`--ctp` 只解除默认排除，不得暗中改成“仅 CTP”。仅运行 CTP 必须显式使用 `--ctp -m ctp`，`make test-ctp` 必须按该语义接线。
- `--cov` 必须启用 `--cov-branch`；pytest 和 `tee` 的状态都必须传播，任一失败即返回非零，并保留可诊断的失败摘要。运行器合同本地 **23 passed**。

### FR-16 Optional performance 不得 false-green

- performance job 只能通过 `workflow_dispatch` 显式 opt-in；未建立能力前不保留 schedule，不得用定时绿色结果声称已做性能验证。
- 当前只允许精确 singleton node `tests/performance/test_event_normalization_performance.py::test_normalize_event_orderbook_dict_hot_path`，并必须使用 `-n 8`；目录级或多 benchmark 运行在解决 xdist 同路径报告聚合前不得纳入门禁。
- 版本化基线固定 mean ≤ **0.0005s**、rounds ≥ **1000**；基线与报告的 benchmark 名称集合必须精确一致。缺文件、空文件、坏 JSON、重复键/条目、缺失或额外 benchmark、非法 `mean` / `rounds` / `iterations`、超阈值均必须失败，只有 checker 成功后才上传 artifact。
- 本地合同与 benchmark 已运行（定向 **59 passed**；7 次有效报告 mean **8.096–32.879µs**、rounds **3613–14608**）。该绝对上限只防止数量级灾难性退化，不是跨机器敏感趋势基线；GitHub hosted 仍 **NOT_RUN**，schedule 仍不得恢复。

### FR-17 CI full-suite baseline coverage fail-closed

- `.github/workflows/tests.yml` 的 full-suite 必须使用 `python -m pytest tests -v -n 8` 并启用 `--cov-branch`；coverage threshold 只能通过 step `env` 传入，且必须通过 0..100 十进制数值白名单（允许小数）校验。
- 禁止 threshold 执行命令替换、注入额外 pytest 参数或借 shell 拆词；必须用 Bash 数组将每个 pytest 参数作为独立 argv 传递。
- `coverage-full.xml` 与 `htmlcov/index.html` 必须都存在且非空，artifact 同时归档两者并在缺失时失败；Codecov 只是可选遥测，不得代替本地阈值或报告/artifact 门禁。本地结构合同 **20 passed**，GitHub hosted **NOT_RUN**，故不宣称外部 CI 已通过。

## 6. 非功能需求

- **不回归**：任何一批改动后，既有测试（根 1303 + 相关子仓测试）必须全绿；CI 棘轮不得抬升。
- **可回滚**：按子仓/主题分批提交，每批可独立 revert。
- **可验证**：每个改进项都有可复现命令与前后对比数据。
- **不引入新依赖**（质量工具沿用 ruff/mypy/bandit/coverage/gitleaks）。
- **合规**：`bt_api/*` 为独立子仓，改动的提交与推送遵循仓库 `CONTRIBUTING.md` 与 `pr-governance` 流程。

## 7. 优先级与里程碑

| 批次 | 内容 | 优先级 |
|------|------|--------|
| **M1 止血** | F821 修复、账号泄漏清理、3 个遗留失败处置、配置债修复 | P0 |
| **M2 堵漏** | 门禁扩容（CI+Makefile）、棘轮脚本、覆盖率阈值同源；子仓静态安全在 CTP High 清零后分阶段接入 | P0 |
| **M3 清存量** | 机械项已清零；剩余 10 项按子仓主题收口；CTP 隔离例外受 E-05 约束 | P1 |
| **M4 类型与文档** | 注解/docstring 分批、mypy 扩到 L1 子仓 | P1 |
| **M5 结构与覆盖率** | 超长文件拆分方案、超长函数治理、覆盖率 55% | P2 |
| **M6 台账收口** | 技术债登记册、计划归纳入册、文档同步 | P2 |
| **M7 运行与发布治理** | PR/docs/optimized 工作流、canonical runner、optional performance 和 same-candidate 发布 | P0/P1 |

## 8. 待确认的决策点

1. **D1 范围**：~~是否包含 15 个子仓？~~ **已决策（2026-09-19，用户确认）**：
   15 个子仓全部纳入门禁；门禁由**主仓侧**实施（主仓 CI/Makefile 扫描 `bt_api/*/src` 与
   `bt_api/*/tests`），**不改动子仓任何文件**，存量清理按子仓分批排期。
2. **D2 策略**：**已采纳棘轮式递进**。快照只在下降时更新；本轮从 21 降至 10，不允许回抬。
3. **D3 覆盖率目标**：阶段目标 55% 已由 CI 同标记、`-n 8` 且显式 `--cov-branch` 的根套件新鲜证据 **67.27%** 覆盖；后续提高阈值仍须再次以新鲜全量覆盖率复算，不沿用旧结果。
4. **D4 遗留 3 个失败测试**：**已处置**，见 `docs/TECH_DEBT.md` T-01..T-03。
5. **D5 子仓门禁晋级**：**已实施第一阶段**：`submodule-lint` / `submodule-format` 已拆开并保持 report-only。仅在各自存量清零、可独立回滚且 CI 证据稳定后转 blocking；当前 10 个 lint 项与 453 个格式化文件（快照基线 452，已持久化 OKX handoff 候选 87 > 86）禁止直接翻转任一 job。
6. **D6 文档指标口径**：**已完成**。`make public-api-quality` 是 AC-9 的唯一通过指标；全树 `analyze_docstrings.py` 的缺失数只作诊断，不得作为 85% 通过依据。
7. **D7 已移除 DEX 示例边界**：**已完成**。Curve/Raydium/SushiSwap 的旧容器与 DEX feed 在当前公开包中不存在，不能把静态配置文件视为实现存在；对应的 5 个不可收集示例测试已正式退役，`tests/test_legacy_dex_example_retirement.py` 只禁止该 6 个已移除模块前缀，避免误扩大到其他历史示例债务。
8. **D8 CTP 探针例外边界**：**已完成**。E-05 只允许 `_ctp_probe.py` 的单个固定命令调用使用 `S603`/`B404`/`B603` 行级抑制；静态命令、固定 cwd、环境键白名单、输出脱敏和 monkeypatch 行为测试缺一不可。该离线证据不替代 SimNow/CTP 外部会话验收。
9. **D9 根仓渐进 ignore 台账边界**：**已完成盘点，未完成治理**。E-07 仅允许 `render_tech_debt.py` 的固定 Ruff JSON 调用使用 `S603`/`B404`/`B603` 行级抑制；最近一次完整报告的历史证据为 37 项（TC001 22、TC003 15）。Tasks37–41 仅执行目标 TC 扫描且无新增，没有 fresh `make tech-debt-report`，不得将 37 写成本批新鲜证据。Task43 后最新完整根 marker 为 **1824 passed、2 warnings、127.85s**；最近一次显式 `--cov-branch` 证据仍为 Task41 的 **1735 passed、2 warnings、110.06s / 67.27%**。Task33–36 的历史可比快照保留；同一 fresh 254 文件口径在 Tasks37/38 后为 **17 errors / 3 files**，Tasks39–41 后为 **0 errors / 0 files**。精确 256 文件命令仍未重跑；后续类型债治理仍需保持运行时反射与行为兼容，不得以报告或配置替代清零验收。
10. **D10 子仓静态安全准入**：**已完成只读预检，未建立基线**。固定范围是全部已检出子仓的 `src`，默认 Bandit profile 加 `--ignore-nosec`，不导入或安装子仓。当前 26 项中含 CTP SFTP connector 的 `B507` High；它必须以子仓独立提交改为可信 host-key 校验后才可建立 source-only、fail-closed 棘轮。该门禁不替代子仓依赖漏洞、secret/history 或外部运行时安全验收。

> **Task41 类型证据（取代上文 Task36 的类型快照）**：Tasks37–41 的联合目标范围 **203 passed**，目标 mypy 全部归零；同一 fresh `--no-incremental`、排除两份禁读测试的可比 254 文件范围从 Task36 的 **23 errors / 5 files**、Tasks37/38 后的 **17/3** 降至最终 **0/0**。这不是精确 256 文件全量复跑；Task30 的精确证据仍为 **57 errors / 13 files**。本批仅目标 TC 扫描无新增，没有 fresh `make tech-debt-report`，**37** 仅作为最近历史债务证据。Task41 的 branch coverage 为 **1735 passed、2 warnings、110.06s / 67.27%**；Task43 后最新非 coverage 根 marker 为 **1824 passed、2 warnings、127.85s**。
