# 迭代 1 报告：订单链路硬化

> 日期：2026-08-16 ~ 2026-08-17

## 目标回顾

消除下单链路的静默降级、幂等错配、幽灵订单、资源泄漏等 P0 资金风险（Spec A-01~A-08、A-11、A-13~A-15、A-17、A-19、A-20）。

## 任务完成情况

| 任务 | Finding | Commit | 状态 |
|------|---------|--------|------|
| 1.1 | A-01 | `1e091821` | complete（白名单校验 side/order_type） |
| 1.2 | A-01 | `56b4ee4d` | complete（client 缺省改必填） |
| 1.3 | A-02 | `c4e38ba9` | complete（幂等只缓存确定性终态） |
| 1.4 | A-03 | `361c9fe7` | complete（ZMQ 超时 drain + correlation id） |
| 1.5 | A-08 | `fb151329` | complete（cancel 幂等 key 去 uuid） |
| 1.6 | A-11 | `c599d10a` | complete（close() 关 WS） |
| 1.7 | A-06 | `5a569cff` | complete（kline 部分下载聚合异常） |
| 1.8 | A-07 | `059fb31a` | complete（_parse_time naive 按 UTC） |
| 1.9 | A-14/15/17/19/20 | `f56260af`+`a04dc156` | complete（5 小项） |
| 1.10 | A-04/05/13 | `80247e4c` | complete（常驻 loop + pending_commands + SubscribeError） |

## 验收清单结果

1. ✅ `pytest tests/test_forwarding_bus_router_client.py tests/test_forwarding_zmq_transport.py tests/test_bt_api_helpers.py -v` → **75 passed**
2. ✅ 非法 side/order_type 全部拒绝且 adapter 零调用（Task 1.1 两参数化测试）
3. ✅ 幂等测试：retryable 错误不缓存（`test_router_does_not_cache_retryable_errors`）、终态错误缓存（`test_router_caches_terminal_rejects`）
4. ✅ 幽灵订单：超时命令进入 `pending_commands()` 可查询、loop 内 `start_sync` 不崩溃（Task 1.10 4 测试）
5. ✅ `--cov=bt_api_py/forwarding` → **87.18%**（≥80%）
6. ⚠️ code-reviewer agent 复审：当前执行环境无 reviewer 子 agent，由 controller self-review 关键 diff（常驻 loop 单例 / pending_commands / start_sync 重构 / subscribe），无 CRITICAL/HIGH。

## 关键设计决策

- **A-04/A-05**：引入 `_LoopRunner` 常驻事件循环单例（`bt_api_py/forwarding/memory.py`），所有 sync→async 桥接（`_run_awaitable_sync`、`start_sync`、ZMQ `_run_handler`）统一投递到该 loop；`grep asyncio.run bt_api_py/forwarding/` 零处。超时语义改为 `TimeoutError("result unknown")`，命令 key 进入 `ForwardingClient.pending_commands()`。
- **仓库完整性处置（用户批准）**：母仓库 HEAD 缺 26 个核心包文件（forwarding 7 文件、brokers、backtrader、gateway、certification 等，6-17 质量迭代产物从未 commit，干净 clone 后 `import bt_api_py` 失败），补齐为 `f56260af`。clone 验证（`git archive HEAD` + import）通过。

## 遗留问题

1. **code-reviewer 复审未完成**（第 6 项）：需在有 reviewer 子 agent 的环境补做，或人工复审本次迭代 5 个 commit。
2. Task 1.1 minor：cancel_order try 块异常缩进（已由 1.5 修复）、`float(command.size)` 无 ValueError 守卫（带入 1.9 但 A-16/A-18 属迭代 5）。
3. `local_time` 字段（`receive_time / 1000.0`）仍秒/毫秒混用，未在 A-20 范围。
4. ZMQ `ZmqCommandClient.send` 超时消息仍为 "timed out"（Task 1.4 drain 机制），与 memory 的 "result unknown" 语义未统一（但 ZmqForwardingClient 已接 pending_commands）。
5. 母仓库 12 个 commit（`a64689af..80247e4c`）尚未 push（推送时机由用户决定）。

## 时间花费

- 本次续跑（1.8/1.9/1.10 + 验收 + 环境搭建 + 仓库完整性处置）：约半天。
- 前置（1.1~1.7）：见 2026-08-16 各 task report。
