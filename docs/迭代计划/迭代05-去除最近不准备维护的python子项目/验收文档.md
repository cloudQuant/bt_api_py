# 迭代05：去除最近不准备维护的 Python 子项目 —— 验收文档

> 版本：v1.1
> 日期：2026-09-16
> 状态：已完成（全部验收项通过）
> 关联需求：`需求文档.md`
> 关联设计：`研发设计文档.md`
> 关联实施：`实施计划.md`

---

## 0. 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-16 | 初稿：定义 AC-1…AC-8 验收项与证据口径 |
| v1.1 | 2026-09-16 | 执行完成：填入实际证据（AC-1…AC-8 全通过）；补充遗留项 |

---

## 1. 验收说明

本迭代交付物为「仓库结构变更」，验收维度：**移除完整性**、**引用零残留**、**留档可恢复**、**门禁无回归**、**历史归档完整**。每条给出验证方式与通过判据；实际执行证据汇总在第 3 节。

---

## 2. 验收项

### AC-1 submodule 移除完整性

- [ ] `.gitmodules` 中 `[submodule ...]` 段数为 **15**。
- [ ] `git submodule status --recursive` 输出 **15** 行。
- [ ] `git ls-files --stage | grep 160000` 为 **15** 行，且不含任何被移除 path。
- [ ] 45 个工作目录 `bt_api/bt_api_<name>/` 不存在。
- [ ] 45 个 `.git/modules/bt_api/bt_api_<name>/` 不存在。
- [ ] `bt_api/` 下 `bt_api_*` 目录数为 **15**。
- **验证方式**：`实施计划.md` 第 3 节命令 1。

### AC-2 活跃引用零残留

- [ ] 用 `研发设计文档.md` 第 2 节口径扫描，排除 `bt_api/bt_api_<name>/` 内部与迭代05留档文档后，命中 = **0**。
- [ ] 被删 token 的 45 个均不再出现在 `conftest.py`、`pyrightconfig.json`、`scripts/**`、`_generate_docs.py`、`bt_api/readme.md`、`docs/CODE_QUALITY.md`、`docs/exchanges/**`、`docs/source-tree-analysis.md`。
- **验证方式**：扫描脚本 + 逐文件 grep。

### AC-3 Python 与 shell 语法完整

- [ ] Task 3 涉及的 `.py` 文件全部 `ast.parse` 通过。
- [ ] `pyrightconfig.json` 为合法 JSON。
- [ ] Task 4 涉及的 `.sh` 文件全部 `bash -n` 通过。
- **验证方式**：`实施计划.md` 第 3 节命令 3。

### AC-4 文档契约通过

- [ ] `python scripts/ci/check_docs_contract.py` 退出码 0。
- [ ] `python scripts/generate_exchange_support_docs.py --check` 退出码 0（生成块未被破坏）。
- **验证方式**：命令见 `实施计划.md` 第 3 节命令 4。

### AC-5 站点构建通过

- [ ] `mkdocs build --strict` 退出码 0。
- **验证方式**：命令见 `实施计划.md` 第 3 节命令 4。

### AC-6 测试基线无回归

- [ ] `python -m pytest tests/test_repository_baseline.py -q` 通过（`.gitmodules` 与 manifest 一致）。
- [ ] `python -m pytest tests/ -q` 无新增失败（对照基线）。
- **验证方式**：命令见 `实施计划.md` 第 3 节命令 5。

### AC-7 留档可恢复

- [ ] `研发设计文档.md` 附录 A 含 45 条，每条有 path、URL、40 位 pin SHA。
- [ ] 附录 A 的 45 条 SHA 与移除前 `git ls-tree HEAD bt_api/` 的 gitlink 一致。
- [ ] 恢复步骤（`git submodule add` + `checkout <sha>`）可执行且无歧义。
- **验证方式**：脚本比对附录 A 与 `git log` 中删除前 commit 的 `ls-tree` 输出。

### AC-8 历史归档完整

- [ ] `docs/archive/**`、`docs/plans/**`、`docs/superpowers/**`、`docs/迭代计划/迭代01-02`、`docs/acceptance/2026-08-17-baseline-inventory.json` 摘除后仍为合法 Markdown/JSON。
- [ ] 归档文件除被删交易所引用行外，其余内容与改动前一致（`git diff --stat` 仅小量行变更）。
- [ ] 未删除任何整份历史归档文件（除纯专属文件）。
- **验证方式**：`git diff` 审查 + `python -m json.tool`。

---

## 3. 实现进度与证据

> 本节在执行完成后填写，记录实际命令与输出。

### 3.1 扫描基线（执行前）

| 项 | 值 |
|----|----|
| 全仓命中文件数（含 submodule 内部） | 1124 |
| 需人工处理活跃文件 | 24 |
| 需摘除历史归档文件 | 11 |

### 3.2 执行结果

| 验收项 | 命令 | 实际结果 | 结论 |
|--------|------|----------|------|
| AC-1 | `grep -c '^\[submodule ' .gitmodules` | `15` | ✅ |
| AC-1 | `git submodule status --recursive \| wc -l` | `15` | ✅ |
| AC-1 | `git ls-files --stage \| grep -c 160000` | `15` | ✅ |
| AC-1 | `ls -d bt_api/bt_api_* \| wc -l` | `15` | ✅ |
| AC-1 | 45 个 `bt_api/bt_api_<name>/` 与 `.git/modules/bt_api/bt_api_<name>/` | 全部不存在（worktrees removed 45/45；.git/modules removed 45/45） | ✅ |
| AC-2 | 扫描脚本（排除子模块内部与迭代05留档） | `TOTAL FILES: 0` | ✅ |
| AC-3 | `ast.parse` 11 个 `.py` | `all py OK` | ✅ |
| AC-3 | `json.tool pyrightconfig.json` / `baseline-inventory.json` | OK / OK | ✅ |
| AC-3 | `bash -n` 6 个 `.sh` | `all sh OK` | ✅ |
| AC-4 | `python3.12 scripts/ci/check_docs_contract.py` | `Documentation contract passed.` exit 0 | ✅ |
| AC-4 | `python3.12 scripts/generate_exchange_support_docs.py --check` | exit 0 | ✅ |
| AC-5 | `python -m mkdocs build --strict` | `Documentation built in 1.69 seconds` exit 0 | ✅ |
| AC-6 | `pytest tests/test_repository_baseline.py -q` | `7 passed in 6.71s` | ✅ |
| AC-6 | `pytest tests/ -q -m "not network"` | `1523 passed, 2 warnings in 115.74s` | ✅ |
| AC-7 | 附录 A 条目数 | 45 条，URL 与 pin SHA 齐全（与删除前 `git ls-tree HEAD bt_api/` 一致） | ✅ |
| AC-8 | `git diff --stat` 归档文件 | 仅行级/条目级删除（JSON `4 insertions, 700 deletions` 等），无整份归档删除 | ✅ |

**执行环境：** 文档门禁与 mkdocs 用 `python3.12` / anaconda base `python 3.11.8`（项目阻塞目标 3.11–3.13）；测试用 `python 3.11.8` + `PYTHONPATH=bt_api/bt_api_base/src`。

**变更规模：** `git status` 汇总 `114 D`（45 个 gitlink + 67 个 examples 文件 + 2 个空目录内文件）、`56 M`、`2 AM`（迭代05 文档）、`4 ??`。

### 3.3 遗留与后续

1. **远端仓库去留**：`github.com/cloudQuant/bt_api_<name>.git`（45 个）归档或删除属组织级决策，本迭代只在 `研发设计文档.md` 附录 A 留档 URL + pin SHA。
2. **PyPI 已发布包**：对应 wheel 是否 yank，待人工决策。
3. **`.git/modules/bt_api/` 残留 5 项**：`bt_api_bequant`、`bt_api_bigone`、`bt_api_bingx`、`bt_api_bitbank`、`bt_api_bitflyer` 为**本次清单之外**的历史残留（早已不在 `.gitmodules`）。按"只清理本迭代范围"原则未删除，可后续单独清理。
4. **`docs/exchanges/EXCHANGE_STATUS.md` 生成源**：该文件带 `EXCHANGE_SUPPORT_STATUS` 旧 marker，由遗留脚本 `scripts/docs/generate_exchange_support_docs.py`（非 `docs/AGENTS.md` 指定的活跃生成器）维护。本迭代已手工更新其内容，但未重跑该遗留脚本；若将来运行它，可能覆盖本次更新。
5. **本次清单之外的遗留引用**：`examples/network_tests/` 中仍保留 `test_bequant.py`、`test_bigone.py`、`test_bingx.py`、`test_bitbank.py`、`test_bitflyer.py`、`test_bitbns.py` 等（对应交易所早已不在 `.gitmodules`，不在本迭代清单），以及 `bequant`/`bigone`/`bingx`/`bitbank`/`bitflyer` 在若干脚本中的引用。按 surgical 原则未处理。
6. **`.gemini` 例外**：`.gitignore` 与 `scripts/analyze_code_lines.py` 中的 `.gemini` 为 AI 编码工具缓存目录，非交易所引用，保留。
