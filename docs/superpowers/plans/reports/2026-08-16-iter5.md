# 迭代 5 报告：架构收敛与安全加固

**日期**：2026-08-17

## 任务完成清单

| 任务 | 内容 | commit |
|------|------|--------|
| 5.1 | 删除 4 个死文件 + risk_management `__all__` 双定义 | `379e0125` |
| 5.2 | 插件惰性加载 + async 白名单收敛 `__getattr__` | `5c8c0c7` |
| 5.3 | AuditLogger 接入 router 下单/撤单路径 | `c24f860b` |
| 5.4 | 审计脱敏 + 真原子写 + chmod 0600 | `8c73d206` |
| 5.5 | PBKDF2 随机盐 + 密钥文件 0600/目录 0700 | `431a0ec8` |
| 5.6 | TLS CERT_NONE 后门删除 + Prometheus 默认回环 | `a3ccdcfe` |
| 5.7 | pickle 路径限制 + 全掩码 + sklearn 惰性 + mock 加权 | `97f57838` |
| 5.8 | check_file_sizes.py + 母仓库 5 文件拆分 | `1ada0515`..`64b4d320` |
| 5.8 | binance 子模块拆分(request_base/account/wallet) | `4563e7c`..`b5b2c2c`(子模块) |
| 5.8 | OKX 子模块拆分(trade/trading_account/7 mixins) | `2379a35`..`10e0b67`(子模块) |
| 5.9 | 占位模块决策 + hub 接入 bus + config 反向依赖 + CTP 配置 + gateway 别名 | `ee296dbb` |

## 验收清单核对

- [x] 死代码清单零引用：`grep` 4 个死文件全部零结果
- [ ] `check_file_sizes.py` ≤800 行：**母仓库 0 超限 ✅；5 个子模块文件剩余 >800**(CTP ctp_trader_api 1802 / MT5 adapter 1100 / OKX market_wss_base 1062 / HTX spot 947 / dydx request_base 819)——用户决策"先不拆了"，记录 backlog
- [x] 安全修复测试全绿(E-02~E-05)：test_audit_logger_redaction / test_encryption_manager_salt / test_security_hardening 全绿
- [x] import 耗时下降 + 坏插件不阻断：import 237.5ms，`_plugins_loaded=False`(import 不触发插件加载)
- [ ] code-reviewer 复审：当前环境无 reviewer 子 agent，由 controller self-review 关键 diff

## 验收命令输出摘要

```
$ grep -rn "bt_api_py.feed_registry\|_plugin_shims\|utils.time\|backtrader.mapping" bt_api_py/ tests/ --include="*.py"
(零引用 ✓)

$ python scripts/check_file_sizes.py
Found 5 source file(s) over 800 lines:  # 全部在子模块(见上)

$ python -c "import bt_api_py"
import 耗时: 237.5ms, _plugins_loaded: False
```

母仓库全量测试：525 passed(除 3 个预先存在的插件环境失败 found 0 adapters)。

## 遗留问题

1. **5 个子模块超限文件**(CTP/MT5/OKX wss/HTX/dydx)——需进入各自子模块仓拆分，后续轮次按交易所分批。
2. **code-reviewer 复审**——环境无 reviewer 子 agent，待补充。
3. **插件环境失败**——`bt_api.plugins` entry points 为 0(插件包未安装)，3 个插件测试失败为预先存在。
4. **子模块 push**——binance/OKX 子模块拆分 commit 已本地完成，push + 母仓库 pin 更新需按"子模块三步"执行(用户决定时机)。
