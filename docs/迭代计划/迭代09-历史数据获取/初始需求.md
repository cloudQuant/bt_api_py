当前bt_api_py项目已经有一部分脚本通过rest请求下载历史数据了，但是获取历史数据的功能并不全，
我希望你能帮我完成一个迭代计划 docs/迭代计划/迭代07-历史数据获取，
在这个迭代计划中，我希望bt_api_py这个包能够整合 akshare, openbb, 和 ths（同花顺）获取数据的能力，
希望你能帮我做一个详细的迭代方案出来。

参考本地仓库：
- /Users/yunjinqi/Documents/new_projects/OpenBB
- /Users/yunjinqi/Documents/new_projects/openbb-docs
- /Users/yunjinqi/Documents/new_projects/agents-for-openbb
- /Users/yunjinqi/Documents/new_projects/akshare

参考在线文档：
- 同花顺金融数据 API https://fuyao.aicubes.cn/docs/api-reference/overview/

---

（补充澄清，2026-09-19）

我希望能够把 akshare、openbb、同花顺 API 这些功能里面的代码，按照现在的结构**深度整合**进来，
而不仅仅是要"依赖" akshare、openbb、fuyao（同花顺 API）。希望可以：

1. 创建一个 `bt_api_http`：把 akshare 里面爬取的数据的接口都整合到这里面；
2. 把 openbb 的接口也按照 provider 的数据源，整合成 `bt_api_xxx`；
3. 同花顺的也创建一个 `bt_api_fuyao`；
4. 统一的数据接口还是 `BtApi`。

希望实现的是**源代码级别的整合**，后面就不再依赖 akshare、openbb 这些包。
