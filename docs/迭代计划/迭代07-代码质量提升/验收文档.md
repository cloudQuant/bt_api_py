# 迭代07：代码质量提升 —— 验收文档

> 版本：v1.64
> 日期：2026-09-21
> 状态：进行中（AC-1/2/3/4/6/7/9/10/11/12/13/18 已通过；AC-5/8/14/15/16/17/19/20 部分；AC-17/19 仍 NO-GO，其中 AC-19 本地 singleton 能力已通过、hosted 未运行）
> 关联需求：`需求文档.md`
> 关联设计：`研发设计文档.md`
> 关联实施：`实施计划.md`

---

## 0. 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-19 | 初稿：量化验收标准（AC-1~AC-12）+ 安全/门禁/成本专项 + 通过门槛 |
| v1.1 | 2026-09-19 | D1 决策落档（子仓纳入门禁）；AC-6 已通过（含范围完整性守卫的故障注入证据）、AC-5 部分通过（ruff 范围已覆盖子仓，type-check/security-scan 子仓范围待 M4）；门槛阈值更新为从 1301 起降 |
| v1.2 | 2026-09-19 | 回填 M1/M3 执行结果：**lint 债 1301 → 21（-98.4%）**，F821/W291/F401/I001/E701/F541/PIE790 全部清零，账号泄漏清零，配置债清零，零回归（根测试 1321 passed，binance 对照实验一致）；新增 §0.1 进展表 |
| v1.3 | 2026-09-20 | 本轮根仓 examples 清理使棘轮 **21 → 11**；重置 AC-8 为部分通过，明确 10 个子仓 lint 与 452 个格式化文件阻止总 job 直接转 blocking；AC-9 改为先固化可复算公共 API 指标 |
| v1.4 | 2026-09-20 | 本轮独立并行根测试通过：`./scripts/run_tests.sh -m "not network and not integration and not performance and not e2e and not ctp"` 经 `python -m pytest -n 8` 执行，**1321 passed、2 warnings、169.36s**；AC-12 更新为通过 |
| v1.5 | 2026-09-20 | 公共 API AST 指标与 fixture 测试落地：docstring **594/691（85.96%）**、参数注解 **1016/1035（98.16%）**；fresh `make type-check-l1` 全绿，AC-9 更新为通过 |
| v1.6 | 2026-09-20 | 新增 5 项指标回归测试后的最终根回归：**1326 passed、2 warnings、100.72s**（`python -m pytest -n 8`）；AC-12 证据更新为本次变更后结果 |
| v1.7 | 2026-09-20 | 子仓 quality 工作流拆分完成：`submodule-lint` / `submodule-format` 各自 report-only，结构测试固定命令与矩阵隔离；AC-5 保持部分通过，尚未达到 blocking 门槛 |
| v1.8 | 2026-09-20 | 工作流结构测试纳入根套件后的新鲜并行回归：**1327 passed、2 warnings、74.57s**；AC-12 证据更新 |
| v1.9 | 2026-09-20 | 旧 DEX 示例退役测试纳入根套件后的新鲜并行回归：**1328 passed、2 warnings、91.10s**；N-08 从 TODO 收口为 DONE |
| v1.10 | 2026-09-20 | CTP/SWIG 探针 S603/B404/B603 收敛为 E-05：静态命令、固定 cwd、环境键白名单、输出脱敏与离线行为测试；棘轮 **11 → 10**，新鲜根回归 **1336 passed、2 warnings、74.65s**，余量全部在子仓 |
| v1.11 | 2026-09-20 | AC-10 的首项实施前评审已落档：OKX 只读 readiness mapper 的职责、I/O、最小行为回归与不变边界明确；尚未将其计为完成拆分 |
| v1.12 | 2026-09-20 | Task15 首项完成：OKX readiness mapper 仅提取私有 checks/reasons/判定 helper，AST span **198 → 139**；直接契约 **41 passed**，新鲜根回归 **1336 passed、2 warnings、74.00s** |
| v1.13 | 2026-09-20 | Task15 第二项完成：Binance readiness mapper 的两条失败/短路契约先落地，入口 AST span **180 → 143**；修复 ZMQ 测试污染全局线程 mock 后，新鲜根回归 **1338 passed、2 warnings、76.11s** |
| v1.14 | 2026-09-20 | Task15 第三项完成：纯 CTP 预算计算的状态扫描 helper 抽取，入口 AST span **170 → 128**；新增完整路径、状态 reason 顺序及后段 fail-closed 契约，直接/session/根本地回归均通过 |
| v1.15 | 2026-09-20 | Task15 第四项完成：事件归一化的 orderbook fields helper 抽取，入口 AST span **181 → 134**；新增列表档位/strict 配对/continuity 契约，直接分发器与根本地回归均通过 |
| v1.16 | 2026-09-20 | Task15 第五项完成：Binance 读取顺序/参数/深拷贝契约先在原实现通过，`get_order_readiness` **157 → 104**；完整 readiness 契约与新鲜根并行标记回归通过，AC-10 收口 |
| v1.17 | 2026-09-20 | Task16 新鲜覆盖率验收：CI 同标记、8 workers 的根套件为 **66.45%**，**1340 passed、2 warnings、83.00s**，继续满足 AC-7 与 55% 阶段目标 |
| v1.18 | 2026-09-20 | 对余下 10 个子仓 lint 形成带源码位置、审计 SHA、最小修复与测试门的交付图；Binance/Bybit/IB Web/OKX 的现有离线基线已验证，Bitget 因缺 bt_api_bequant 依赖为 NOT_RUN。无子仓代码或 gitlink 在本条记录中被宣称完成 |
| v1.19 | 2026-09-20 | 新增逐仓 `format-ratchet`：15 仓/452 文件基线，增长、新仓或缺失仓均失败；阻塞 job 已接入 `quality-gate`，专属工作流/更新契约测试 **23 passed**、本机实扫与 `make format-ratchet` 均通过；变更后的新鲜根并行回归为 **1363 passed、2 warnings、75.95s**。它不自动格式化，也不把 report-only 的 `submodule-format` 或 452 项存量表述为已完成 |
| v1.20 | 2026-09-20 | 新增根仓渐进 Ruff ignore 的只读报告与 **12 passed** 行为测试；固定 8 条规则当前为 **86** 项，配置/JSON/退出状态 fail closed，E-07 行级例外已在源码、台账、设计和本验收中对齐。变更后的新鲜根并行回归为 **1375 passed、2 warnings、89.30s**。该报告只证明可复现盘点，不证明 86 项已清零 |
| v1.21 | 2026-09-20 | `PERF402`/`S113` 在根源码、测试、脚本和示例的隔离扫描均为 0，已从全局及 `tests/*.py` 例外移除；报告改为 6 条、**86** 项，防回归行为测试 **13 passed**，变更后根并行回归 **1376 passed、2 warnings、76.01s**。该结论不替代剩余 86 项的逐条治理 |
| v1.22 | 2026-09-20 | `PERF403` 唯一命中先以离线 Logstash payload 契约固定，再替换为字典推导式并移除根全局 ignore；隔离扫描为 0，报告收缩为 5 条、**85** 项，ELK/报告定向回归 **47 passed**、根 marker 回归 **1377 passed、2 warnings、79.72s**、根 lint 通过；未连接外部 ELK |
| v1.23 | 2026-09-20 | `S112` 的两个 IBKR Cookie 脚本命中以浏览器/requests/logger 内存替身契约固定后，改为不含 Cookie/异常正文的 debug 诊断并移除根全局 ignore；隔离扫描为 0，报告收缩为 4 条、**83** 项，Cookie/报告定向回归 **15 passed**、根 marker 回归 **1379 passed、2 warnings、73.73s**；未访问浏览器、Gateway 或网络 |
| v1.24 | 2026-09-20 | `ctp_env_selector._load_default_fronts` 的本地 YAML 失败回退先以临时文件和内存模块固定，旧代码契约 **1 failed**（无诊断）；修复后只记录异常类型，未泄露内容/路径/异常正文，`S110` **13 → 12**、报告 **83 → 82**，selector/报告定向回归 **18 passed**、根 marker **1379 passed、2 warnings、74.57s**；未启动 CTP、SimNow 或网络 |
| v1.25 | 2026-09-20 | 两个 margin 文档抓取脚本的侧栏发现失败先以假 `playwright.sync_api`/假页面固定，旧代码契约 **2 failed**（无 helper）；修复后返回空列表、warning 只含异常类型，`S110` **12 → 10**、报告 **82 → 80**，scraper/报告定向回归 **15 passed**、根 marker **1381 passed、2 warnings、88.93s**；未调用主流程、浏览器或网络 |
| v1.26 | 2026-09-20 | 生产监控示例的 exporter 停止失败由 fake module 契约保护：成功保留 info，失败仅 warning 异常类型并返回；文档契约检查器在错误顺序断言保护下收敛 3 个 `PERF401`。`S110` **10 → 9**、`PERF401` **28 → 25**、报告 **80 → 76**，各定向回归 **2 passed**、合并根 marker **1383 passed、2 warnings、74.69s**；不启动 exporter、不访问网络或写入文档 |
| v1.27 | 2026-09-20 | exchange-support 生成器与 GitHub 治理校验器先锁定输出顺序，再分别收敛一个 `PERF401`（**3/9 passed**）；两个棘轮脚本仅将延迟注解所需的 `Sequence` 移入 `TYPE_CHECKING`（合计 **41 passed**）。`PERF401` **25 → 23**、`TC003` **20 → 18**、报告 **76 → 72**、新鲜根 marker **1385 passed、2 warnings、76.91s**；未写文档、调用 GitHub API、更新基线或修改子仓 |
| v1.28 | 2026-09-20 | 两份字节一致的慢测试分析器以临时 JSON 锁定多记录、缺失字段与顺序后，同步收敛两个 `PERF401`（**2 passed**）；报告 **72 → 70**，新鲜根 marker **1387 passed、2 warnings、75.63s**；未运行主入口或访问网络 |
| v1.29 | 2026-09-20 | 两份字节一致的覆盖率分析器以 stub 锁定未测试交易所排序、模块行、低覆盖率筛选/顺序和完整报告后，同步收敛两个 `PERF401`（**2 passed**）；报告 **70 → 68**、新鲜根 marker **1389 passed、2 warnings、74.91s**；未调用 `main()`、`pytest.main()` 或写出报告 |
| v1.30 | 2026-09-20 | 两对 capability 提取器先以纯内存 Markdown/CSV 契约锁定输入键序、能力字段、缺失值回退与完整输出后，同步收敛 **6** 个 `PERF401`（v1/v2 各 **2 passed**、各自 `cmp` 一致）；四测试模块的注解专用 `Path`/`AsyncIterator` 移入 `TYPE_CHECKING` 收敛 **4** 个 `TC003`。报告 **68 → 58**（S110 9、PERF401 13、TC001 22、TC003 14）；目标 Ruff/format/diff 通过，确定性范围 **42 passed**。两项 artifact-first 临时环境测试在 `PIP_NO_INDEX=1` 下因本地包为 `unavailable` 保持 NOT_RUN，原断言未放宽；因此 AC-12 重置为待本版根 marker 复核 |
| v1.31 | 2026-09-20 | 本版完整离线根回归为 **1390 passed、3 failed、2 warnings**：两项 artifact-first 临时环境测试和一项 wheel contract 都因本地 `bt_api_base` 工件不可用而失败；未改断言、未访问索引。显式排除三项工件前置条件后为 **1390 passed、2 warnings**，但该子集不替代完整 marker。因此 AC-12 明确为 **NO-GO**，待 owner 提供符合 `PIP_NO_INDEX=1` 的本地可安装工件后重跑 |
| v1.32 | 2026-09-20 | 两个质量分析器以临时目录/确定性 walk 锁定 Python 遍历顺序、隐藏文件过滤和排除目录剪枝后，各自收敛一个 `PERF401`（**2 passed**）；未调用主入口、写报告或访问外部服务。`PERF401` **13 → 11**、报告 **58 → 56**；离线工件导致的 AC-12 NO-GO 不变 |
| v1.33 | 2026-09-20 | 两个代码质量检查器以临时目录/确定性 walk 与 `check_file`/report 替身锁定默认/自定义排除、隐藏目录/文件、顺序、verbose、更新与异常隔离后，各自收敛一个 `PERF401`（**6 passed**）；未调用 CLI、写报告或访问外部服务。`PERF401` **11 → 9**、报告 **56 → 54**；离线工件导致的 AC-12 NO-GO 不变 |
| v1.34 | 2026-09-20 | 最终根门禁、文档合同和严格构建通过；完整离线根回归为 **1398 passed、3 failed、2 warnings**，两项 artifact-first 临时环境测试与一项 wheel contract 仍仅因本地 `bt_api_base` 工件不可用而失败。排除三项后的确定性范围 **1398 passed、2 warnings** 不替代完整 marker，AC-12 维持 **NO-GO** |
| v1.35 | 2026-09-20 | 两个 docstring 文件发现器以临时目录/确定性 walk 锁定默认/自定义排除、动态 `*.egg-info`、隐藏目录/文件和排序输出后，各自收敛一个 `PERF401`（**4 passed**）；未调用 CLI、写文件或访问外部服务。`PERF401` **9 → 7**、报告 **54 → 52**；本批完整根 marker 待重跑，既有离线工件 NO-GO 未解除 |
| v1.36 | 2026-09-20 | 最后 7 个根 `PERF401` 在最小离线契约保护下清零，根/测试 ignore 与报告规则移除；报告为 **3 条 / 51 项**（S110 9、TC001 22、TC003 20）。46 目标 `typing.get_type_hints` 回归发现并阻止了部分 type-only 移动的运行时兼容性破坏，相关直接导入已恢复。完整 `PIP_NO_INDEX=1` 根回归 **1452 passed、3 failed、2 warnings**，三项失败仍是本机 `bt_api_base` 工件前置条件，AC-12 继续为 NO-GO |
| v1.37 | 2026-09-20 | 最后 9 个根 `S110` 以 fake-only 契约固定清理/接收的 best-effort 语义后清零，诊断仅记录异常类型；根/测试 ignore 与报告规则移除，CTP vendor 范围化例外保留。报告为 **2 条 / 42 项**（TC001 22、TC003 20）；本批完整根 marker 待新增回归纳入后重跑，AC-12 的离线工件 NO-GO 未解除 |
| v1.38 | 2026-09-20 | 四个未进入运行时反射面的测试专用 `Path`/`ModuleType` 导入最小移入 `TYPE_CHECKING`，根 `TC003` **20 → 16**、报告 **42 → 38**；反射契约 **46 → 57 passed**。完整 `PIP_NO_INDEX=1` 根回归 **1475 passed、3 failed、2 warnings、111.06s**，三项失败仍仅为本地 `bt_api_base` 工件缺失；显式排除后 **1475 passed、2 warnings、89.21s**，不替代完整 marker，AC-12 仍 NO-GO |
| v1.39 | 2026-09-20 | 五个私有风险检查 mixin 的注解专用 `RiskMetrics` 导入最小移入 `TYPE_CHECKING`，根 `TC001` **22 → 17**、报告 **38 → 33**；并修复六个订单/持仓限额 helper 的 `CRITICAL` 分支未初始化 `warning`。反射 **58 passed**、风险 **34 passed**、组合 **106 passed**；完整 `PIP_NO_INDEX=1` 根回归 **1484 passed、3 failed、2 warnings、56.45s**，显式排除后 **1484 passed、2 warnings、47.72s**，不替代完整 marker，AC-12 仍 NO-GO |
| v1.40 | 2026-09-20 | 独立审查发现五个私有风险 helper 经 `LimitsManager` 继承后仍可由 `typing.get_type_hints` 解析，故恢复其运行时导入并将反射守卫 **58 → 63 passed**。仅 `PolicyEngine.__init__` 的局部 `Callable` 通过审计后移入 `TYPE_CHECKING`，报告真实值为 **37**（TC001 22、TC003 15）；完整 `PIP_NO_INDEX=1` 根回归 **1489 passed、3 failed、2 warnings、55.42s**，诊断范围 **1489 passed、2 warnings、42.97s**，不替代完整 marker，AC-12 仍 NO-GO |
| v1.41 | 2026-09-20 | 离线 artifact-first 闭包修复经红绿测试、独立复跑与代码审查后通过：绝对 wheelhouse、默认 PEP 517 隔离、fresh build/venv、完整 resolver、`pip check`、root/base `site-packages` receipt 与旧 artifacts 不复用均有回归。完整 `PIP_NO_INDEX=1` 根 marker **1499 passed、2 warnings、97.25s**，AC-12 更新为通过；不构成外部 CTP/SimNow 证据。 |
| v1.42 | 2026-09-20 | Task 10-A 二次只读审计确认五个子仓工作树干净、gitlink 与审计 SHA 对齐；Binance/OKX B017 的对外异常契约由 `httpx.ReadTimeout` 订正为 base 映射后的 `RequestTimeoutError`。Bitget 仍为 NOT_RUN，CI 必须以锁定 sibling SHA 的 BeQuant/Coinbase 工件并核验导入来源后才能产生行为通过证据；本次未修改子仓源码。 |
| v1.43 | 2026-09-20 | 根 G-07/G-08 的 37 项按五个独立域复核完毕，全部是现有 `get_type_hints` 兼容性契约的一部分，当前无安全清理批次。`typing.*` 替换或全限定名规避均被拒绝；守卫测试当前未跟踪，必须随最终交付纳入版本控制后才可作为持久验收证据。 |
| v1.44 | 2026-09-20 | 修复 `quality-gate` 未阻断 `quality-ratchet` 的聚合漏洞，并锁定 `if: always()`；YAML 结构合同与既有 format-ratchet 合计 **45 passed**。子仓静态安全预检复算为 26 项（High 1 / Medium 3 / Low 22）；CTP `B507` High 是 required Bandit gate 的 NO-GO 前置，不能基线化或表述为依赖/secret/外部安全通过。 |
| v1.45 | 2026-09-20 | `PluginCatalog` 的 `certified` 状态改为五条件 fail-closed：配置 tier、实体证据卡、安装、最低版本和入口点均需满足；缺卡、目录伪卡、未安装、版本/入口不足及 experimental 配置均降级。公开 `resolve_bundle()` 契约 **11 passed**，完整 `PIP_NO_INDEX=1` 根 marker **1510 passed、2 warnings、99.49s**；未访问网络、CTP 或 SimNow。 |
| v1.46 | 2026-09-20 | 可选 broker entry point 的单插件/日志 `Exception` 已隔离，枚举与 `BaseException` 仍可重试；quality-ratchet 对快照与发现范围双向 fail-closed，正常 `--update` 不写范围漂移，仅审查后的 `--force-update` 重基线。范围契约 **5 passed**、broker 定向 **15 passed（1 existing deprecation warning）**，最新完整离线根 marker **1522 passed、2 warnings、89.88s**。 |
| v1.47 | 2026-09-20 | PR governance 对 `.gitmodules`/gitlink 的单独变更不再可绕过 strict：精确路径、flag 非真即 fail-closed、普通 `bt_api/` 文件不误判；治理契约 **14 passed**，完整离线根 **1526 passed、2 warnings、99.41s**。 |
| v1.48 | 2026-09-20 | 增加 AC-13～AC-17：SourceSupervisor、docs CI、PR governance schema v2、optimized 安全/wheel smoke 与 same-candidate 发布。完整 checkout-local 根回归 **1569 passed、2 warnings、68.47s**；显式 `--cov-branch` 复跑覆盖率 **66.94%**。 |
| v1.49 | 2026-09-20 | 增加 AC-18/19：canonical runner 默认 CTP 排除、branch coverage 和退出码传播；optional performance 手工 opt-in 且缺目录/报告/非空 benchmarks 即失败。最新 checkout-local 根回归 **1595 passed、2 warnings、124.16s**；显式 `--cov-branch` 为 **1595 passed、2 warnings、152.81s** / **66.94%**。真实 GitHub Actions/benchmark/network/CTP/SimNow 仍 **NOT_RUN**。 |
| v1.50 | 2026-09-20 | 增加 AC-20：CI full-suite 固定并行 branch coverage、threshold step-env 数值白名单与 Bash argv 数组、XML/HTML 非空验证和严格归档。最新 checkout-local 根回归 **1615 passed、2 warnings、63.32s**；显式 `--cov-branch` 为 **1615 passed、2 warnings、74.40s** / **66.94%**。本地结构合同 **20 passed**，GitHub hosted **NOT_RUN**。 |
| v1.51 | 2026-09-20 | AC-17 的本地候选身份与工作流结构已实现：manifest/摘要、独立 wheel/sdist/manifest 锚、manual build-only、release+enable gate、TestPyPI 精确 wheel 校验/PyPI-only 依赖和同 artifact 复验由定向合同 **54 passed** 锁定。最新根回归 **1669 passed、2 warnings、111.54s**，branch coverage **1669 passed、2 warnings、215.92s** / **66.94%**；外部发布、action SHA/构建依赖固定与 OIDC 最小执行面仍未验收，AC-17 为 **PARTIAL / NO-GO**。此外当前 dirty OKX 的 format-ratchet 实测 **87 > 86**，迭代总体门禁不能判绿。 |
| v1.52 | 2026-09-21 | AC-10 增加 execution-journal 迁移结构/原子性切片：入口 **361 → 128**，COMMITTED payload 先原子落盘再改变内存状态；失败注入证明 source、cutover 工件、transaction 与所有非空 registry manifest 完整恢复。定向 **6 passed**、execution-session 文件 **161 passed**；最新根回归 **1671 passed、2 warnings、95.99s**，branch coverage **1671 passed、2 warnings、108.50s / 67.11%**。外部发布、CTP、性能边界不变；dirty OKX format-ratchet 仍 **87 > 86**，L1 mypy 当前为 **3 errors**。 |
| v1.53 | 2026-09-21 | AC-10 增加 S-07 单一实现源验收：8 个 legacy analysis 完整副本改为顶层 canonical + 薄兼容 wrapper。两轮独立审查发现并修复只读代理、`patch.object` 删除/恢复和 wrapper 基础设施名称遮蔽，兼容合同 **41 passed**、顶层 8 文件 SHA256 不变、最终复审 **PASS**。AC-11 同步 8 份 Forwarding 计划复核，待确认 **36 → 28**、已落地 **30 → 38**。最新根回归 **1696 passed、2 warnings、69.74s**，branch coverage **1696 passed、2 warnings、93.40s / 67.11%**。 |
| v1.54 | 2026-09-21 | AC-10 增加 funding coercion 职责拆分验收：入口 **81 → 28**；独立审查复现并推动修复自定义 Mapping 的 `source` 读取顺序/异常归一化回归，公开签名和稳定错误语义最终由 **17 passed** 保护，目标 Ruff/format/mypy/diff-check 通过。AC-11 完成剩余 28 份计划三批复核：ELK/Logstash **34 passed**、CI/runtime **472 passed**、monitoring/forwarding/ZMQ **119 passed**；最终已落地 61、已废弃/阻塞 8、部分/未开工 5、待确认 0。精确 CI mypy **77 errors** 与 1 个 owner 文件 format 失败均保留为部分完成。根回归 **1706 passed、2 warnings、93.10s**，branch coverage **1706 passed、2 warnings、115.70s / 67.24%**。 |
| v1.55 | 2026-09-21 | AC-9/11 增加 docstring 自动补全脚本的类型与写入安全验收：目标 mypy **20 → 0**，精确 CI 范围 **77 → 57 errors（14 → 13 files）**。独立审查两轮复现多行/inline、tab、同索引 action 和 UTF-8 文件前两行 PEP 263 cookie 位置边界并推动修复，最终写入前 parse + 缺失项复核 fail closed；定向 **23 passed**、目标 Ruff/format/mypy、diff-check 与最终复审 PASS。完整根 marker **1725 passed、2 warnings、100.03s**，branch coverage **1725 passed、2 warnings、119.54s / 67.24%**。split-line `async \\` / `def` 保持安全拒绝；其余 57 个 mypy 错误和 owner format 失败仍未完成。 |
| v1.56 | 2026-09-21 | AC-11 增加 F-86 的新增类型债验收：三个测试注解专用导入经运行时用法核验后移入 `TYPE_CHECKING`；脚本公开 `Iterable[str]` 经真实 `get_type_hints` 证明为运行时依赖，保留直接导入和报告命中。四测试 **48 passed**、目标 mypy/Ruff/format、diff-check 与独立复审 PASS；实时报告 **40 → 37**（TC001 22、TC003 15），只计 3 个真实清理。该阶段完整根 marker **1725 passed、2 warnings、89.32s**。 |
| v1.57 | 2026-09-21 | AC-6 增加 F-87 的 force-update 防绕过验收：scope shrink 在扫描前拒绝，扩张时旧范围回升与 same-scope 回升均拒绝且不写 baseline，安全扩张仍可记录。两个目标测试文件 **31 passed**，目标 Ruff/format/mypy、质量棘轮、diff-check 与独立复审 PASS；最终完整根 marker **1728 passed、2 warnings、90.50s**，同范围 branch coverage **1728 passed、2 warnings、107.64s / 67.24%**。 |
| v1.58 | 2026-09-21 | AC-9/11/12 增加 F-88 artifact-first 类型边界验收：真实运行时 `Mapping` guard 与只读复制、wheelhouse 在创建 artifacts 前单次归一化、`SimplePath` 显式转本地 `Path` 后继续 fail closed 校验。目标 **12 passed**、四文件 mypy/Ruff/format、债务报告与独立复审 PASS；可比 254 文件范围 **57 → 52 errors（13 → 11 files）**，精确 256 文件命令未重跑。最终根 marker **1731 passed、2 warnings、88.20s**，branch coverage **1731 passed、2 warnings、118.11s / 67.24%**。 |
| v1.59 | 2026-09-21 | AC-9/11/12 增加 F-89 测试侧 AST/假模块类型边界验收：三个目标文件 mypy **4 → 0**，定向 **7 passed**，Ruff/format、债务报告与两次独立复审 PASS；可比 254 文件范围 **52 → 48 errors（11 → 8 files）**，精确 256 文件命令未重跑。最终根 marker **1731 passed、2 warnings、92.95s**，branch coverage **1731 passed、2 warnings、135.56s / 67.24%**。 |
| v1.60 | 2026-09-21 | AC-9/11/12 增加 F-90 离线脚本测试假模块类型边界验收：两文件使用精确 `ModuleType` 子类表达 Playwright、browser-cookie3 与 requests 运行时属性，目标 mypy **8 → 0**、定向 **4 passed**，不启动浏览器或网络。fresh `--no-incremental` 可比 254 文件结果为 **41 errors / 7 files**；因该运行额外暴露既有注解错误，不把 Task34 的 48/8 与本值写成直接因果差值。 |
| v1.61 | 2026-09-21 | AC-9/11/12 增加 F-91 生产监控/Hyperliquid 离线测试假模块类型边界验收：两文件目标 mypy **18 → 0**、定向 **7 passed**。独立审查发现并修复 `config_loader` 仅注解且以 `raising=False` 创建假接口：fixture 先绑定安全失败占位函数，异常注入恢复 `raising=True`。同一 fresh 254 文件范围 **41 → 23 errors（7 → 5 files）**。最终根 marker **1734 passed、2 warnings、91.45s**，branch coverage **1734 passed、2 warnings、119.06s / 67.27%**。 |
| v1.62 | 2026-09-21 | AC-9/11/12 增加 F-92–F-96 / Task37–41：execution-session 精确 key/value 注解 **1 → 0**（**164 passed**、128 source type-check PASS）；OKX WSS 精确 Event 联合 **5 → 0 / 3 passed**；workflow 路径化 runtime YAML guards 与坏形状负例 **5 → 0 / 21 passed**；websocket Protocol 目标 **11 → 0**，修复 fresh 跨文件捕获的 receiver consumer 1 error，保持 websockets>=12 且以 `port=0` 避免 8765 冲突；probe `list[str]` + `CompletedProcess[str]` **1 → 0 / 8 passed**。联合目标 **203 passed**，fresh 254 文件范围 **23/5 → 17/3 → 0/0**，精确 256 文件未重跑。最终根 marker **1735 passed、2 warnings、88.69s**，branch coverage **1735 passed、2 warnings、110.06s / 67.27%**。 |
| v1.63 | 2026-09-21 | AC-19 增加 F-97 / Task42：exact singleton orderbook normalization benchmark、schema v1 基线与 fail-closed checker 已落地；workflow/Make 锁定 nodeid + `-n 8`。定向 **59 passed**，7 次本地有效 JSON mean **8.096–32.879µs**、rounds **3613–14608**，0.5ms/1000 rounds 门禁通过；最新完整根 marker **1796 passed、2 warnings、95.29s**。结论为本地 singleton **PASS**、GitHub hosted/multi-benchmark **NOT_RUN**，故 AC-19 整体仍 **PARTIAL / NO-GO**。 |
| v1.64 | 2026-09-21 | AC-10/12 增加 F-98 / Task43：`OrderRequest` 17 项校验按原顺序等价拆分，入口/core/intent C901 **18→1/9/10**，`models.py` **3→2**、主包 **84→83**；七文件 **81 passed**、目标行/分支覆盖完整，19 字段 frozen/signature/反射保持，128 source mypy 与独立复审 PASS。最新完整根 marker **1824 passed、2 warnings、127.85s**；branch coverage 未重跑。 |

---

## 0.1 本轮进展（2026-09-21）

**lint 债：1301 → 10（-99.2%）；本地定向合同与完整离线 AC-12 已通过。** 逐项状态：

| AC | 状态 | 证据 |
|----|------|------|
| AC-1 真实缺陷清零（F821） | **通过** | `F821: 135 → 0`（okx 95 / binance 14 / examples 32 等，逐处修复并验证导入） |
| AC-2 安全泄漏清零 | **通过** | `git grep 089763` = 0；密码经复核未入库 |
| AC-3 遗留失败处置 | **通过** | 3 项已通过（alpaca monkeypatch / 插件数断言移除），按 §6 登记为 T-01..T-03 |
| AC-4 配置债清零 | **通过** | `pyrightconfig.json` 路径修正；根 `AGENTS.md` 重写；引用文件全部存在 |
| AC-5 门禁全量覆盖 | **部分通过** | ruff 已覆盖 34 路径（`make lint-all` 与脚本 `--print-scope` 一致，由单测断言防漂移）；阻塞 `quality-ratchet` 已覆盖全量，且 `quality-gate` 现会逐项阻断其失败，L1 五仓 mypy 已阻塞。`submodule-lint` / `submodule-format` 仍独立 report-only，但新的阻塞 `format-ratchet` 已对 15 仓/452 文件基线逐仓防回退；已持久化 OKX handoff 候选将实测总数推高到 453（OKX **87 > 86**），棘轮正确失败，同时仍有 10 个 lint 项。子仓默认 Bandit 预检为 26 项且含 CTP `B507` High；High 清零、独立 fail-closed 基线及 required job 接线完成前，security-scan 的子仓覆盖仍为 NO-GO |
| AC-6 棘轮有效 | **通过（已实现维度）** | lint：规则故障注入、快照缺失路径与当前未记录路径均 → exit 1；普通 `--update` 拒绝抬高基线或 scope 漂移，审查后的 `--force-update` 也不得删除 recorded scope 或抬高旧范围债务，只能记录安全扩张；快照已由 21 降为 **10**。format：独立的 15 仓 JSON 快照对任一增长、新仓或缺失仓 fail closed；本轮实际拦截 OKX **87 > 86**，证明棘轮有效但也意味着当前门禁非绿色。质量与 format 的 `quality-gate` 接线由 **45 passed** 的 YAML 结构合同锁定；子仓 Bandit 棘轮尚未满足 High=0 的准入条件，不能计为通过 |
| AC-7 覆盖率门槛同源 | **通过** | `pyproject.toml` 的 `fail_under = 40` 是默认门槛；仅显式 `workflow_dispatch` 可经 step env、0..100 十进制数值白名单与单 argv 临时覆盖。CI 同标记、8 workers、显式 `--cov-branch` 新鲜实测覆盖率 **67.27%**（**1735 passed、2 warnings、110.06s**）≥ 默认门槛 40%，且高于计划目标 55% |
| AC-8 机械 lint 债清零 | **部分通过** | `W291/F401/I001/E701/F541/PIE790/UP006/UP035/UP041 → 0`；根仓 S603/B404/B603 已由受测 E-05 行级例外收敛，余 `TC002×4`、`S106×3`、`B017×2`、`E402×1` 全在子仓，故全量 lint 仍不能宣称通过。Task 10-A 已定义五个原子交付与验收门；Binance/OKX B017 必须断言公开边界 `RequestTimeoutError`，Bitget 行为测试仍为 NOT_RUN，须先使用可追溯、锁定 sibling SHA 的 BeQuant/Coinbase 工件并核验导入来源 |
| AC-9 类型与文档目标 | **通过（历史目标；fresh 254 文件库存已归零，子仓候选仍阻塞）** | `make public-api-quality`：docstring **594/691（85.96%）** ≥85%，参数注解 **1016/1035（98.16%）** ≥45%，脚本 fixture **5 passed**。当前 root lint 与主包 mypy PASS；Task30 的精确 256 文件 mypy 为 **57 errors / 13 files** 且未重跑。Task36 后排除两份禁读测试的 fresh 254 文件范围为 **23 errors / 5 files**，Task37/38 后为 **17 errors / 3 files**，Task41 后为 **0 errors / 0 files**；该 0/0 不是精确 256 文件全量证据。Task37–41 目标 mypy 分别为 **1 → 0**、**5 → 0**、**5 → 0**、**11 → 0**、**1 → 0**，联合目标 **203 passed**。当前 `type-check-l1` 因已持久化 OKX handoff 候选的 `registry_registration.py:120,174` 缺注解与 `market_wss_base.py:906` 参数类型不兼容共 **3 errors**，须在后续修复后复核 |
| AC-10 结构治理产出 | **通过** | Top 5 方案卡已存在；Task15 已完成 **5/5**：OKX **198 → 139**、Binance mapper **180 → 143**、CTP budget **170 → 128**、normalization event **181 → 134**、Binance read-only entry **157 → 104**，行为契约分别 **41/43/4/7/44 passed**。追加 `migrate_execution_journal` **361 → 128**，并由 **6 passed** 失败注入证明 COMMITTED 写失败完整 rollback；execution-session 文件 **161 passed**，主包 AST 清单现为 **16** 项。S-07 的 8 个 legacy analysis 副本收敛为顶层 canonical + 薄 wrapper；读写/删除恢复、重名全局与 CLI 合同 **41 passed**、canonical SHA256 不变、独立复审 **PASS**。追加干净职责切片 `coerce_funding_snapshot` **81 → 28**；自定义 Mapping 回归经独立审查修复后边界合同 **17 passed**。Task43 又将 `OrderRequest` 入口 C901 **18 → 1**，两个 helper 保持 **9/10**，主包 C901 **84 → 83**且行为合同 **81 passed** |
| AC-11 债务台账可用 | **通过（库存仍待治理）** | `docs/TECH_DEBT.md` 六类可对账并新增 F-88–F-96；`docs/plans/README.md` 已索引并逐份复核 **74/74**，最终为已落地 **61**、已废弃/阻塞 **8**、部分/未开工 **5**、待确认 **0**。Task30 精确 256 文件为 **57 errors / 13 files** 且未重跑；Task36/Task37–38/Task41 后 fresh 254 文件范围依次为 **23/5**、**17/3**、**0/0**，不得把最终 0/0 写成精确 256 文件全量结果；owner 文件 format 失败继续保留为部分完成。根 2 条渐进 Ruff ignore 的历史 `make tech-debt-report` 为 **37**（TC001 22、TC003 15）；本批仅目标 TC 扫描无新增，未重跑 fresh `tech-debt-report`，不声称实时债务已更新 |
| AC-12 零回归 | **通过（checkout-local）** | 工件闭包落地的 **1499 passed、2 warnings** 保留为历史证据；Task37–41 联合目标 **203 passed**，旧三项 artifact-first/wheel-contract 失败继续通过；Task43 后完整根回归 **1824 passed、2 warnings、127.85s**。最近一次 `--cov-branch` 仍为 Task41 的 **1735 passed、2 warnings、110.06s**、覆盖率 **67.27%**，Task42/43 后未重跑 coverage。后续完整运行均未再出现 N-19 的退出后插件线程 warning，但该项继续保留 WATCH；当前仍是 dirty checkout，本地证据不是 clean-checkout、外部 CI 或发布证明 |
| AC-13 SourceSupervisor 并发 | **通过（本地）** | 同键首次订阅/slow-stop 交错等确定性契约 **5 passed**；未连接外部 feed |
| AC-14 docs workflow | **PARTIAL** | 本地结构契约 **4 passed**；GitHub/Pages **NOT_RUN** |
| AC-15 PR governance v2 | **PARTIAL / NO-GO** | schema v2、逐 path gitlink、`collection_errors`、raw `-z`、可信 base/对象探针本地 **46 passed**；三分支两阶段 rollout、fork 与 strict 在线运行 **NOT_RUN** |
| AC-16 optimized 安全/wheel smoke | **PARTIAL** | 本地结构契约 **5 passed**；真实 GitHub Bandit/pip-audit/build/wheel **NOT_RUN**，CTP `B507` 仍 NO-GO |
| AC-17 same-candidate 发布 | **PARTIAL / NO-GO** | 本地候选身份/工作流合同通过；hosted/TestPyPI/PyPI/OIDC/D4、action 与构建依赖固定 **NOT_RUN** |
| AC-18 canonical 测试 runner | **通过（本地合同）** | 默认 `not ctp`、`--ctp -m ctp`、`--cov-branch`、pytest/tee 失败传播及失败摘要合同 **23 passed**；network/CTP/SimNow **NOT_RUN** |
| AC-19 optional performance | **PARTIAL / NO-GO** | 本地 singleton benchmark/checker/workflow 合同 **59 passed**；7 次有效 JSON 均通过 0.5ms/1000 rounds 门禁。GitHub hosted 与多 benchmark 聚合 **NOT_RUN**，schedule 未恢复 |
| AC-20 CI full-suite baseline coverage | **PARTIAL** | `python -m pytest tests -v -n 8 --cov-branch`、threshold 防注入及报告严格归档的本地结构合同 **20 passed**；GitHub hosted **NOT_RUN** |

**额外成果**：修复一个**无法编译**的示例（同步函数内 `await`）、4 处**失效**的 noqa 指令、
2 处硬编码失效绝对路径、2 条指向不存在路径的 per-file-ignores，以及**阻塞 `-n` 并行**的参数化 ID
问题（并行后根测试从 882s 降到 88s）。详见 `docs/TECH_DEBT.md` §5。

**支持状态边界**：诊断命令不得把“仅有同名路径”的插件表述为认证；证据卡必须是实体文件，且仍受配置 tier、安装、最低版本和入口点共同约束。

**血泪教训（已入台账 §10）**：禁止 `ruff check --select RUF100 --fix` —— 实测会误删仍在起作用的
`# noqa`，其中包含 `# noqa: UP017 - package supports Python 3.9` 这类关键抑制。

---

## 1. 验收说明

- 每项 AC 的验收形式：**命令 + 前后数值对比 + 结论**（实施后回填 §8）。
- 质量迭代的验收以**可复算的数字**为准，不接受"感觉干净了"这类主观结论。
- **零回归**是横切前提：任何一批改动若使既有测试变红，该批不通过。

---

## 2. 功能验收

### AC-1 真实缺陷清零（F821）

- `ruff check <QUALITY_SCOPE> --select F821` → **0**。
- 修复方式可追溯：每处要么补 import，要么删除死代码，不得用 `# noqa` 掩盖。
- 受影响子仓（binance、okx）测试全绿。

### AC-2 安全泄漏清零

- `git grep -n "089763"` → **0**（真实账号已占位化）。
- `git grep -n "yunjinqi2015"` → **0**（密码始终未入库，复核确认）。
- gitleaks 覆盖范围说明写入本文档；PR 扫描通过。

### AC-3 遗留失败测试处置

- 2026-08-17 基线记录的 3 个失败，逐项给出结论（修复 / 显式 skip + 理由），
  且 `pytest tests -m "<CI 标记>"` 无**未登记**失败。

### AC-4 配置债清零

- `pyrightconfig.json` 引用的所有路径存在（或文件已删除并在本文档说明）；
- `AGENTS.md` 引用的 `.joyincode/rules/*.md` 存在（或引用已改指向 `docs/CODE_QUALITY.md`）；
- `stock_data/` 的忽略规则覆盖完整（`daily/`、`adjustment_factors/`、`financials/`、`*.parquet`），
  仅 `README.md` 有意保留待入库。

### AC-5 门禁全量覆盖

- `make lint-all` / `make quality-ratchet` 的输出范围**包含全部** `bt_api/*/src` 与 tests；快速 `make lint` 可保持核心范围，但不得被表述为完整门禁。
- CI 存在子仓分片 job，且能看到每个子仓的独立结果。
- 子仓与父仓规则一致性：抽查 1 个文件，两侧扫描结论一致（无"父仓报、子仓不报"）。

> **本轮状态（2026-09-20）**：`make lint-all` 已覆盖 34 个路径（核心 + 15 子仓 × src/tests +
> scripts + examples），与 `scripts/ci/check_quality_ratchet.py --print-scope` 逐字一致
> （由 `tests/scripts/test_check_quality_ratchet.py` 断言，防范围漂移）。
> CI 已新增 `quality-ratchet`（阻塞）与 `submodule-lint` / `submodule-format`（各自 report-only、每仓分片）。
> `quality-gate` 已补齐对 `quality-ratchet` 的 needs、非 success 失败与 summary 行；相关 YAML 结构合同与
> format-ratchet 合计 **45 passed**，并锁定 `if: always()`，不会再让质量棘轮失败留下绿色汇总。
> `tests/test_submodule_workflow.py` 当前未跟踪，故其本地观察为 **NOT_PERSISTENT**；只有 owner 显式纳入交付后，才可用它锁定两个 job 的发现矩阵、命令隔离、固定 Ruff 版本和 report-only 晋级条件。
> 此外 `tests.yml` 的阻塞 `format-ratchet` job 已用 `check_format_ratchet.py` 对 15 个仓逐仓比较
> `docs/acceptance/2026-09-20-format-ratchet.json` 的 452 文件基线，并由 `quality-gate` 显式检查其
> success；计数增长、新仓或缺失仓均为失败。它不会调用 `ruff format`、不会改写子仓，也不会把
> `submodule-format` 从 report-only 升级。**待完成**：子仓 security-scan 的独立基线。当前默认 Bandit
> profile + `--ignore-nosec` 预检为 26 项（High 1 / Medium 3 / Low 22），CTP `B507` High 必须先清零，
> 不能写入绿色基线；之后才可建立 fail-closed required gate。10 个 lint 项与
> 当前 453 个待格式化文件（快照基线 452）分别清零、取得稳定 CI 证据后，才可分别转 blocking。当前直接升级任一 report-only
> job 均为 **NO-GO**。

### AC-6 棘轮有效（只降不升）

- 存在可机读基线快照（`docs/acceptance/*-quality-ratchet.json`）与 `check_quality_ratchet.py`。
- **故障注入验证**：人为新增 1 处 `F401` → CI/脚本 fail 且打印位置；撤销后 pass。
- 违规清零时脚本提示更新快照，且快照只在新低、scope 双向一致时更新；当前发现但快照未记录的路径和快照记录但当前缺失的路径均必须失败；审查后的 `--force-update` 也只可记录安全扩张，禁止删除既有 scope 或抬高旧范围债务。
- 子仓 format 另有逐仓快照；任一计数增长、新仓或缺失仓必须失败，且正常更新只允许所有仓未增长、至少一仓下降。该门禁不得自动执行 `ruff format`。

> **本轮状态（2026-09-19）：已通过**。快照
> `docs/acceptance/2026-09-19-quality-ratchet.json`（34 路径 / 1301 项 / ruff 0.16.2）已入库。
> 已验证：
> ① 正常 → exit 0；
> ② 注入 `scripts/_ratchet_probe.py`（1 处 F401）→ exit 1，输出 `F401: 165 > baseline 164 (+1)`；
> ③ **范围完整性守卫**：伪造含不存在路径的快照 → exit 1 并提示"Check out the submodules…"，
> 杜绝"子仓未检出导致范围变小而假通过"；
> ④ `--update` 在存在回归时拒绝写入（不会抬高基线）。
> ⑤ 新增独立范围契约 **5 passed**：当前新增 `src/tests` 而快照未记录时，即使 Ruff 计数相等也会失败；`--update` 不写入，`--force-update` 也只能记录经审查且旧范围债务不升的安全扩张，不能删除既有 scope。
> format 独立快照 `docs/acceptance/2026-09-20-format-ratchet.json` 已覆盖 15 仓/452 文件；
> `python -m pytest tests/scripts/test_check_format_ratchet.py -q -n 8` 为 **23 passed**，本机
> 上述 452 逐仓计数在快照建立时 exit 0；已持久化 OKX handoff 候选为 87 > 86、总计 453，故 `python scripts/ci/check_format_ratchet.py` 与 `make format-ratchet` 应并确实 fail closed。
> 工作流测试还锁定 `quality-gate` 的 `needs`、失败 `exit 1` 与 summary 接线；这是“不得新增 format 债”的
> 通过证据；现亦锁定 `quality-ratchet` 的同一聚合接线与 `if: always()`，合计 **45 passed**。它们不是“format 项已清零”或“子仓安全已通过”的
> 证据。该 runner 的 B404/S603/B603 精确 E-06 例外也经固定 argv/cwd/
> `shell=False` 行为测试和单脚本 Bandit 扫描复核；未新增全局安全忽略。

### AC-7 覆盖率门槛同源且达标

- `pyproject.toml` 的 `fail_under` 与 CI 门槛**同一来源**，数值一致。
- 文档中不再出现互相矛盾的门槛值（40 vs 80 已修正）。
- 主包覆盖率 ≥ 门槛（阶段目标 **55%**）。

### AC-8 机械 lint 债清零

- `W291` / `I001` / `PIE790` → **0**。
- `F401` → 0（或残余项在例外清单中逐条说明）。
- `E701` / `B017` / `TC002` / `E402` → 0（或例外说明）。
- `S106` → 0（改为占位常量）或缩小到在**对应子仓配置**和台账都有效的显式例外。
- `S603`/`B404`/`B603` 仅可作为经过威胁建模的精确行级例外：静态命令/脚本、无 shell、输入环境键白名单、输出脱敏和不启动子进程的行为测试均为必需证据；不得扩大为配置级忽略。
- 当 `make lint-all` 仍非零时，AC-8 一律为 **PARTIAL**，即便棘轮已通过。

### AC-9 类型与文档目标达成

| 指标 | 基线 | 目标 |
|------|------|------|
| 公开函数 docstring 覆盖 | 66% | **≥ 85%** |
| 参数注解完整率 | 23% | **≥ 45%** |
| mypy 覆盖范围 | `bt_api_py` + `tests` | **+ L1 五个子仓** |
| mypy 错误数 | 74（全量） | ≤ 基线（棘轮） |

- 复算命令：`make public-api-quality`（schema version 1、固定源级分母）与 `python -m pytest tests/scripts/test_public_api_quality.py -q -n 8`；`python scripts/analyze_docstrings.py` 仅供全树诊断，不得作为本 AC 的通过凭据。
- F-88 的四个 artifact-first 目标文件 mypy 为 0；目标 **12 passed**，非映射环境在复制前 fail closed，债务报告保持 37。Task30 的精确 256 文件结果与 Task33 的可比 254 文件结果必须并列保留，后者不得冒充精确 CI 全量证据。
- F-89 的三个测试目标文件 mypy **4 → 0**；AST import 节点显式窄化，PyArrow/YAML 假模块以精确 `ModuleType` 子类保留运行时属性语义，定向 **7 passed**。Task34 的可比 254 文件结果为 **48 errors / 8 files**，不得冒充精确 256 文件全量证据。
- F-90 的两份离线脚本测试目标 mypy **8 → 0**；Playwright、browser-cookie3 与 requests/requests.exceptions 均以精确 `ModuleType` 子类保留运行时属性语义，定向 **4 passed**。Task35 的 fresh `--no-incremental` 可比 254 文件快照为 **41 errors / 7 files**；因额外暴露既有注解错误，不把 Task34 的 48/8 与本值写成直接因果差值。
- F-91 的生产监控/Hyperliquid 两份离线测试目标 mypy **18 → 0**，定向 **7 passed**。独立审查确认 `config_loader` 必须先绑定安全失败占位函数，异常注入使用默认 `raising=True`，不允许“仅注解 + `raising=False`”维持假接口。Task36 在同一 fresh 254 文件范围将结果 **41 → 23 errors（7 → 5 files）**；该 23/5 仍非精确 256 文件全量证据。
- F-92–F-96 的 Task37–41 目标 mypy 分别 **1/5/5/11/1 → 0**：execution-session 精确 key/value 注解；OKX WSS 精确 Event 联合；workflow 路径化 runtime YAML guards 与坏形状负例；websocket connection/server/socket 及最小 receiver Protocol；probe `list[str]` + `CompletedProcess[str]`。联合目标 **203 passed**。Task40 保持 websockets>=12 API shape，`port=0` 避免 Anki 8765 冲突；15.0.1 两文件 **8 passed**，复审在 13.1/15.0.1 各 **7 passed**，12.0 未安装执行但官方 API shape 复核兼容，最终 PASS。fresh 254 文件范围 **23/5 → 17/3 → 0/0**；该 0/0 不是精确 256 文件全量证据。

### AC-10 结构治理产出

- Top 5 超长文件各产出方案卡（`docs/refactor/*.md`），含"是否本期执行"的明确结论。**此项已通过**。
- 主包内 5 个 >150 行函数完成等价拆分，长度 ≤150，相关测试全绿。
- Task15 已完成 **5/5**：`bt_api_py._venue_mappers.okx.normalize_order_readiness` 由 198 行拆至 139 行（完整只读快照、数量、缺失合约、最大量和双向杠杆契约 **41 passed**）；`bt_api_py._venue_mappers.binance.normalize_order_readiness` 由 180 行拆至 143 行（既有契约加失败/短路补强共 **43 passed**）；`bt_api_py._ctp_budget.evaluate_ctp_budget` 由 170 行拆至 128 行（完整路径、状态 reason 顺序、余额/估值/过期 fail-closed 契约 **4 passed**，并经 arming/recovery 调用链 **133 passed**）；`bt_api_py._normalization.normalize_event` 由 181 行拆至 134 行（orderbook 列表档位、strict 配对和 continuity 契约 **7 passed**，并经 event dispatcher 链 **171 passed**）；`BtApi.get_order_readiness` 由 157 行拆至 104 行（完整 Binance 读取顺序、参数和 `extra_data` 深拷贝隔离契约 **44 passed**）。追加 Task27 将 `migrate_execution_journal` 由 361 行拆至 128 行；RED 失败注入同时发现并修复 COMMITTED 原子写失败时跳过 rollback 的缺陷，所有非空 identity-registry manifest 均按旧值恢复。定向 **6 passed**、execution-session 文件 **161 passed**；主包 AST 清单由 42 的基线降至 **16**。Task28 将 8 个 `scripts/analysis/` 完整副本改为顶层 canonical 的薄兼容 wrapper；独立审查先后阻止只读代理、标准 patch 恢复与 `Path` 名称遮蔽缺陷，最终读写/删除恢复和 CLI 合同 **41 passed**、canonical SHA256 不变、复审 **PASS**，本 AC 通过。
- `check_file_sizes.py` 超限文件数 ≤ 基线（18），且例外清单（生成代码）显式化。

### AC-11 债务台账可用

- `docs/TECH_DEBT.md` 存在，条目与扫描结果**可对账**（无孤立债务、无幽灵条目）。
- `docs/plans/` 74 份计划 **100%** 有状态标注（已落地/部分/未开工/已废弃）。
- `make tech-debt-report` 对根仓指定 2 条渐进 ignore 输出每条计数与配置行内理由；配置缺项、坏 JSON、异常退出或意外规则必须失败。`PERF402`/`S113`、`PERF403`、`S112`、`PERF401` 与根 `S110` 均已按离线契约清理并移除相应根规则。最近一次完整报告的历史证据为 **37** 项；Tasks37–41 仅有目标 TC 零新增证据，未取得 fresh 全范围库存。该历史数不是 AC-8 已通过的替代证据。`TC001`/`TC003` 的清理还必须以 `typing.get_type_hints` 对反射可见 API 验证，或明确证明不进入反射面；F-86 只清理 3 个测试命中，脚本直接导入继续如实计债。
- F-88 已对账 artifact-first 的 `Mapping`、`SimplePath` 与 wheelhouse 公开输入类型；可比 254 文件从 57 降至 52 errors，但精确 256 文件命令未重跑，不能把 52 写成当前精确 CI 库存。
- F-89 已对账三个测试文件的 AST/假模块类型边界；三文件 mypy **4 → 0**，可比 254 文件从 52 降至 48 errors（11 → 8 files），但精确 256 文件命令未重跑，不能把 48 写成当前精确 CI 库存。
- F-90 已对账两份离线脚本测试的精确 fake 模块类型边界；两文件 mypy **8 → 0**，fresh 254 文件快照为 **41 errors / 7 files**。由于该运行额外暴露既有注解错误，不能把 Task34 的 48/8 与 41/7 写成直接算术下降。
- F-91 已对账生产监控/Hyperliquid 两份离线测试的精确 fake 模块类型边界及 `config_loader` 运行时绑定；两文件 mypy **18 → 0**，同一 fresh 254 文件范围 **41 → 23 errors（7 → 5 files）**。精确 256 文件命令仍未重跑，不能把 23/5 写成当前精确 CI 库存。
- F-92–F-96 已对账 Task37–41 的精确类型边界和直接行为证据；联合目标 **203 passed**，fresh 254 文件范围最终 **0 errors / 0 files**，精确 256 文件命令仍未重跑。历史技术债仍为 **37**；本批仅目标 TC 扫描无新增，未重跑 fresh `tech-debt-report`。
- F-97 已对账 singleton benchmark、版本化 0.5ms/1000 rounds 基线、strict checker、workflow/Make 精确 nodeid 与临时报告清理；定向 **59 passed**、7 次有效 JSON 门禁通过。多 benchmark 聚合和 hosted 仍未验收。
- F-98 已对账 `OrderRequest` 17 项校验顺序、异常类型/文本、边界与跨 helper 首错优先级；入口/core/intent C901 为 **1/9/10**，七文件 **81 passed**、目标覆盖完整、主包 C901 **84 → 83**，独立复审 PASS。
- 配置级例外在 `pyproject.toml`、本迭代文档、台账三处一致；精确行级例外在源码行、本迭代文档、台账和行为测试四处一致。

### AC-12 零回归（横切）

- 每批提交后：根测试（CI 标记过滤）全绿；受影响子仓测试全绿。
- 全迭代结束时：`python -m pytest tests -q -n 8 -m "not network and not integration and not performance and not e2e and not ctp"` 无失败（除 AC-3 登记的显式例外）。
- artifact-first 临时环境测试必须保持 `PIP_NO_INDEX=1`；pip 只能从调用方提供的、存在的绝对 wheelhouse 解析，且不得以 `--no-deps`、关闭构建隔离、system site-packages 或旧 artifacts 取得通过。
- Task33 进一步要求环境先通过真实运行时 `Mapping` guard，再复制并只重建四个受控 `PIP_*`；wheelhouse 在创建 artifacts 前单次归一化，抽象 `SimplePath` 只有显式转为本地 `Path` 并通过 purelib/platlib 与存在性检查后才可使用；非映射环境、相对字符串和不存在路径均 fail closed。
- Task34 进一步要求 AST import 节点在访问 `lineno` 与 helper 前显式窄化，并以精确 `ModuleType` 子类表达 PyArrow/YAML 假模块属性；该定向合同不读取真实 Parquet、不连接 CTP/SimNow 或网络。
- Task35 进一步要求精确 `ModuleType` 子类表达 Playwright、browser-cookie3 与 requests 属性，并以 **4 passed** 锁定 Chrome/Firefox/Safari/Edge 离线分支；不启动浏览器或网络。Task36 以 **7 passed** 锁定生产监控/Hyperliquid 假模块运行时属性，并要求 `config_loader` 安全占位函数真实绑定且异常注入使用 `raising=True`；不启动真实服务、Hyperliquid 或网络。
- Task37–41 联合目标 **203 passed**；其中 websocket 行为证据覆盖 13.1/15.0.1，12.0 只完成官方 API shape 兼容复核，不冒充安装执行证据。本批不连接外部 websocket、交易所、CTP/SimNow 或网络。
- 工件闭包落地时的根 marker 为 **1499 passed、2 warnings、97.25s**。Task43 后最新完整根 marker 为 **1824 passed、2 warnings、127.85s**；最近一次显式 `--cov-branch` 同范围复跑仍为 Task41 的 **1735 passed、2 warnings、110.06s**，覆盖率 **67.27%**，Task42/43 后未重跑 coverage。三项旧失败经本地 source base wheel 与完整运行时/构建闭包修复；validator 在安装后执行 `pip check`，wheel receipt 同时验证 root/base 导入位置。测试用临时 `pytest-socket` 只覆盖最小 socket-plugin 行为，真实发布或外部工件验收仍需真实归档 wheel。当前 dirty checkout 不能作为 clean-checkout、外部 CI 或发布证明。

### AC-13 SourceSupervisor 同键并发安全

- 首次并发订阅同一 key 只允许一次 upstream start；最后释放只允许一次 stop。
- slow-stop 与新订阅必须线性化；不同 key 不共享临界区，空闲锁可回收。
- 本地 `tests/test_source_supervisor.py` **5 passed**。结论：**PASS（仅本地确定性合同）**。

### AC-14 Docs CI fail-closed

- push/PR 触发路径覆盖 docs、配置、依赖元数据、主包、生成器、合同脚本和 workflow 自身。
- 安装无 fallback/`--no-deps`/`--no-build-isolation`/`continue-on-error`；generator `--check`、contract、strict build 顺序固定。
- 本地结构合同 **4 passed**；GitHub/Pages **NOT_RUN**。结论：**PARTIAL**。

### AC-15 PR governance schema v2 与 rollout

- schema v2 必须提供逐 path `gitlink_changes`、`collection_errors`，真实 raw `-z` 采集和可信 base/对象探针。
- update 可按完整 path record 通过；add/delete 被识别但当前拒绝；legacy 全局 flag/SHA 无授权。
- 本地 collector/validator/workflow 合同 **46 passed**；`dev`、`master`、`code-optimization` 的 `strict=false → 观察 → strict`、fork 和 strict 在线运行均 **NOT_RUN**。结论：**PARTIAL / NO-GO**。

### AC-16 Optimized 安全与 wheel smoke

- Bandit、pip-audit、build、wheel smoke 不得 fail-open；wheel smoke 必须绑定本轮产物并验证安装/导入/最小运行。
- 本地结构合同 **5 passed**；真实 GitHub Bandit/pip-audit/build/wheel **NOT_RUN**，CTP `B507` 仍为 NO-GO。结论：**PARTIAL**。

### AC-17 Same-candidate 正式发布

- 要求同一候选完成 build、TestPyPI publish、精确候选 smoke、PyPI publish，并绑定 source/version/filename/SHA256/SHA256SUMS。
- 本地已实现候选 manifest/摘要、独立 wheel/sdist/manifest 锚、manual build-only、release + enable gate、TestPyPI 有界精确下载/校验、本地 wheel 安装、PyPI-only 依赖与生产前同 artifact 复验；候选/工作流与既有发布资源定向合同 **54 passed**。
- GitHub hosted、TestPyPI/PyPI、OIDC、Environment、trusted publisher 与 tag Ruleset 均 **NOT_RUN**；第三方 action 和发布构建依赖仍需官方/哈希固定，OIDC job 执行候选源码的表面仍需收窄。`release: published` 还意味着 Release 元数据先于 smoke。结论：**PARTIAL / NO-GO**。

### AC-18 Canonical 测试 runner fail-closed

- 无显式 marker 且无 `--ctp` 时必须默认 `not ctp`；`--ctp` 只解除默认排除，不代表“仅 CTP”。
- `make test-ctp` 必须显式使用 `--ctp -m ctp`；`--cov` 必须包含 `--cov-branch`，pytest 或 `tee` 任一失败都返回非零并保留失败摘要。
- 本地 runner/Makefile 合同 **23 passed**。结论：**PASS（本地合同）**；network/CTP/SimNow 均 **NOT_RUN**。

### AC-19 Optional performance 不得 false-green

- performance job 仅允许 `workflow_dispatch` opt-in；精确运行唯一 orderbook normalization node 与 `-n 8`。缺 benchmark 文件/报告、坏或空 JSON、重复/缺失/额外 fullname、非法统计、rounds 不足或 mean 超限都必须失败；checker 通过前不得上传 artifact。
- schema v1 基线固定唯一 fullname、mean ≤ **0.0005s**、rounds ≥ **1000**。定向 **59 passed**，7 次本地有效 JSON mean **8.096–32.879µs**、rounds **3613–14608**；Make 临时报告清理、目标 Ruff/format/mypy/diff-check 均通过。
- 0.5ms 是灾难性退化安全上限（约 ≥2000 events/s），不是敏感跨机器趋势检测。当前 xdist singleton 会由 7 个空闲 worker 发出无 benchmark warning；扩展到多个 benchmark 前必须解决同路径 JSON 聚合/覆盖风险。GitHub hosted **NOT_RUN**、schedule 未恢复，结论：本地 singleton **PASS**，整体 **PARTIAL / NO-GO**。

### AC-20 CI full-suite baseline coverage fail-closed

- full-suite 必须使用 `python -m pytest tests -v -n 8` 并显式启用 `--cov-branch`；完整 marker 过滤保持不变。
- 手工 `coverage-threshold` 只能通过 step `env` 进入 shell，必须通过 0..100 十进制数值白名单（允许小数），再以 Bash 数组的单独 argv 元素传递；命令替换、空白拆词和额外 pytest 参数必须被拒绝。
- `coverage-full.xml` 与 `htmlcov/index.html` 必须存在且非空；artifact 必须同时归档两者并使用 `if-no-files-found: error`。Codecov 仅为可选遥测，不能替代阈值、报告或 artifact 门禁。
- 本地 workflow 结构合同 **20 passed**；真实 GitHub hosted 运行 **NOT_RUN**。结论：**PARTIAL**。

---

## 3. 安全验收专项

| 检查项 | 命令 | 通过标准 |
|--------|------|---------|
| 真实账号清零 | `git grep -n 089763` | 0 |
| 真实密码未入库 | `git grep -n yunjinqi2015` | 0 |
| 私钥未入库 | `git ls-files \| grep -E "\.pem$\|\.key$"` | 无（或已忽略） |
| 敏感文件忽略 | `git check-ignore collector.yaml .env keys tmp_keys` | 全部 ignored |
| secret 扫描 | gitleaks（PR） | 通过；范围说明已记录 |
| CTP 探针命令边界 | `pytest tests/test_isolated_trader_probe.py -q -n 8` | 证明固定命令/cwd/环境键、输出脱敏和空输入拒绝；**不发起外部连接** |
| CTP 前置本地配置回退 | `pytest tests/test_ctp_env_selector.py -q -n 8` | 证明 YAML 失败时返回硬编码前置地址，debug 不含内容/路径/异常正文；**不发起外部连接** |
| margin 脚本侧栏回退 | `pytest tests/scripts/test_scrape_margin_docs.py -q -n 8` | 证明两个副本失败时返回空列表，warning 不含页面内容、URL、路径或异常正文；**不调用主流程、浏览器或网络** |
| 渐进债报告命令边界 | `pytest tests/scripts/test_render_tech_debt.py -q -n 8` | 证明固定 Ruff argv/根 cwd/无 shell，配置、JSON、规则与退出状态均 fail closed；报告不写文件、不扫描子仓 |
| 子仓 Bandit 准入预检 | Conda base 下 `python -m bandit -q -r --ignore-nosec -f json bt_api/bt_api_*/src` | 当前仅为当前 checkout 的源码预检：15 个 src 根为 26 项（High 1 / Medium 3 / Low 22）。CTP `B507` High 必须先清零；不把结果表述为依赖漏洞、secret/history 或外部运行时安全通过 |

## 4. 门禁与棘轮验收

| 项 | 方式 | 通过标准 |
|----|------|---------|
| 门禁覆盖 | `make lint-all` / `make quality-ratchet` 的范围检查 | 含全部 `bt_api/*/src` 与 tests |
| 棘轮拦截力 | 故障注入（新增 F401） | CI fail |
| 棘轮不回退 | 连续两次快照比较 | 第二次 ≤ 第一次，且快照/当前范围双向一致；`--force-update` 只可记录经审查的安全扩张，不能删除既有 scope 或抬高旧范围债务 |
| 子仓 format 棘轮 | `make format-ratchet` | 15 仓逐仓计数不得增长；新仓/缺失仓失败；不自动格式化 |
| required 聚合 | `tests/scripts/test_check_quality_ratchet.py` 与 `tests/scripts/test_check_format_ratchet.py` | `quality-gate` 必须保持 `if: always()`，并逐项阻断 quality、quality-ratchet、compatibility、full-suite、wheel-contract 与 format-ratchet；当前结构合同合计 **45 passed** |
| PR 子模块元数据治理 | `python -m pytest tests/unit/scripts/test_validate_pr_governance.py -q -n 8` | `.gitmodules` 或真实 gitlink 路径而 flag 非真时 strict 失败；普通 `bt_api/` 文件不误判，report-only 不阻断 |
| PR governance schema v2 | collector/validator/workflow 本地合同 | **46 passed**；三分支两阶段 rollout、fork、strict **NOT_RUN** |
| docs workflow | workflow 结构合同 | **4 passed**；GitHub/Pages **NOT_RUN** |
| optimized security/wheel | optimized workflow 结构合同 | **5 passed**；真实 GitHub scanner/build/wheel **NOT_RUN** |
| same-candidate release | 候选身份 + workflow 结构 + 外部发布 | 定向合同 **54 passed**；hosted/TestPyPI/PyPI/OIDC/D4、action/构建依赖固定 **NOT_RUN**，**PARTIAL / NO-GO** |
| canonical runner | runner/Makefile 合同 | **23 passed**；默认 `not ctp`、`--ctp -m ctp`、`--cov-branch`、pytest/tee fail-closed |
| optional performance | singleton benchmark + baseline checker + workflow/Make 合同 | **59 passed**；7 次本地报告通过 0.5ms/1000 rounds；hosted/multi-benchmark **NOT_RUN**，**PARTIAL / NO-GO** |
| CI full-suite baseline coverage | tests workflow 结构合同 | **20 passed**；并行 branch coverage、threshold 防注入、XML/HTML 非空和严格归档均已锁定，GitHub hosted **NOT_RUN** |
| 子仓 Bandit 棘轮 | 未来 source-only、默认-profile 的 fail-closed 脚本与递归 checkout job | **当前 NO-GO**：先以子仓独立提交清零 CTP `B507` High；快照不得豁免 High，也不得替代 pip-audit/gitleaks/外部验收 |
| 根渐进债盘点 | `make tech-debt-report` | 2 条固定规则、根范围和行内理由均可复算；最近一次完整报告的 37 项历史证据必须保留，Tasks37–41 未取得 fresh 全范围库存，不得以此作为清零证明；反射可见类型导入必须通过 `typing.get_type_hints` 契约，且不得以别名隐藏命中 |
| 子仓分片 | CI job 列表 | 每子仓可见结果 |
| 门禁切换可控 | report-only → blocking 的 commit | 可单独 revert |

## 5. 成本验收

| 项 | 关注点 | 通过标准 |
|----|--------|---------|
| CI 分钟数 | 新增子仓矩阵 job 后的总时长 | 单 PR ≤ 原时长 + 10 分钟（超出需在本文档说明取舍） |
| 本地检查耗时 | `make lint` + `make type-check` | ≤ 3 分钟（超出则拆分范围变量） |
| 覆盖率运行 | Full Suite 时长 | 不明显劣化（±20% 内） |

## 6. 验收通过门槛

1. AC-1 ~ AC-20 全部通过；**AC-1 / AC-2 / AC-6 / AC-15 / AC-17 / AC-19 为零容忍项**（不接受例外）。AC-12 已取得 checkout-local 通过证据；当前 AC-14/15/16/17/19/20 尚未满足，因此迭代总体仍为 **NO-GO**。
2. 棘轮快照已入库，且相对 2026-09-19 基线**至少下降**：ruff 总违规 ≤ 500（从 1301 起降），其中 `F821 = 0`。
3. 子仓 lint 与 format 各有独立门禁，并均已切换为阻塞模式（report-only 仅作为过渡期，验收时不得仍是 report-only）。
4. 主包覆盖率 ≥ 55%，且 `pyproject` 与 CI 门槛一致。
5. `docs/TECH_DEBT.md` 建立且 74 份计划 100% 有状态。
6. 零未登记回归，且必须有本次变更后的新鲜并行根测试证据。

## 7. 里程碑验收映射

| 里程碑 | 出口判据（详见实施计划 §3） |
|--------|---------------------------|
| M1 | AC-1 / AC-2 / AC-3 / AC-4 |
| M2 | AC-5 / AC-6 / AC-7（门槛部分） |
| M3 | AC-8 / AC-5（规则一致性） |
| M4 | AC-9（Task 12–13、30、33–41）+ AC-11（Task 17–18、30–31、33–41） |
| M5 | AC-7（覆盖率达 55%）/ AC-10 |
| M6 | AC-11 |
| M7 | AC-13 / AC-14 / AC-15 / AC-16 / AC-17 / AC-18 / AC-19 / AC-20 |
| 全迭代 | AC-12 + §6 门槛 |

## 8. 实现进度与证据（实施后回填）

> 章节预留：各批次 commit 范围、AC 逐项状态、命令与前后数值对比、
> 棘轮快照路径、CI job 截图/日志摘要、遗留项与下轮建议。
