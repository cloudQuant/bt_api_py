# 迭代07 实施计划（代码质量提升）

> 版本：v1.65
> 日期：2026-09-21
> 状态：进行中（M1 完成、M2 核心完成：子仓 lint/format 已独立 report-only，质量/format 债已有根仓阻塞棘轮；子仓静态安全等待 CTP `B507` High 的独立修复后准入；M3 降至 10 项但未收口、M4 公开指标/L1 已完成且 fresh 254 文件 mypy 目标库存已归零、M6 计划复核完成；M5-Task14/15/16/27/28/29/43 已完成，Task30–43 已完成；AC-19 本地 singleton 能力已建立但 hosted 仍未运行）
> 本次更新：Task 37 以精确 key/value 注解将 `_execution_session.py` mypy **1 → 0**，execution-session **164 passed**、`make type-check` 的 128 source PASS；Task 38 以精确 Event 联合收敛 OKX WSS 测试 **5 → 0**，定向 **3 passed**；Task 39 以路径化 runtime YAML guards 与坏形状负例将 workflow 测试 **5 → 0**，定向 **21 passed**；Task 40 以 connection/server/socket 与最小 receiver Protocol 将 websocket 基础设施目标 **11 → 0**，并修复 fresh 跨文件扫描额外捕获的 `_receive_messages: AsyncMock` consumer 1 error，保持 websockets>=12 API shape，以 `port=0` 避免 Anki 8765 冲突；Task 41 以 `list[str]` + `CompletedProcess[str]` 将 probe **1 → 0**，定向 **8 passed**。联合目标 **203 passed**，fresh `--no-incremental` 且排除两份禁读测试的 254 文件范围由 **23 errors / 5 files**，经 Task37/38 降至 **17 errors / 3 files**，最终为 **0 errors / 0 files**；精确 256 文件命令未重跑。历史技术债仍为 **37**；本批仅确认目标 TC 扫描无新增，不声称 fresh `tech-debt-report`。最终根 marker 为 **1735 passed、2 warnings、88.69s**，同范围 branch coverage 为 **1735 passed、2 warnings、110.06s / 67.27%**。发布、CTP、性能与 dirty OKX 边界不变。
> Task42 更新：singleton 性能门禁定向 **59 passed**，7 次有效 JSON 通过 0.5ms/1000 rounds 基线；最新完整根 marker **1796 passed、2 warnings、95.29s**。Task42 后未重跑 coverage，最近 branch coverage 仍为 Task41 的 **1735 passed、2 warnings、110.06s / 67.27%**。GitHub hosted、多 benchmark 聚合、发布与 CTP 边界不变。
> Task43 更新：`OrderRequest` 17 项校验等价拆分后，入口/core/intent C901 为 **1/9/10**，主包 C901 **84 → 83**；七文件直接消费者 **81 passed**、目标行/分支覆盖完整、128 source mypy 与独立复审 PASS。最新完整根 marker **1824 passed、2 warnings、127.85s**；Task43 后仍未重跑 branch coverage。
> 补充：`quality-gate` 已补齐对 `quality-ratchet` 的 required 聚合并锁定 `if: always()`（相关合同 **45 passed**）；子仓默认 Bandit 预检为 26 项，CTP `B507` High 不得写入绿色基线。
> 插件诊断的认证状态亦改为 fail-closed：只有配置 tier 为 `certified`、实体证据卡存在、包已安装、版本达标且入口点可发现时才可显示 `certified`；定向公开契约 **11 passed**，完整离线根回归见验收 v1.45。
> 关联需求：`需求文档.md`
> 关联设计：`研发设计文档.md`
> 关联验收：`验收文档.md`

---

## 0. 修订记录（本轮）

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.46 | 2026-09-20 | 纠正活动验收口径：工件闭包的落地证据保留 **1499 passed**，当前完整离线根 marker 更新为 **1510 passed、2 warnings、99.49s**。新增 PluginCatalog 五条件认证诊断与 **11 passed** 公开契约；未跟踪的子仓工作流/类型反射测试降为 **NOT_PERSISTENT** 本地观察。 |
| v1.47 | 2026-09-20 | 补齐根仓两项真实质量缺陷：可选 broker entry point 的常规失败/诊断失败不再阻断内置或后续适配器，且保留枚举与 `BaseException` 可重试；quality-ratchet 对快照/发现 scope 双向 fail-closed，普通 `--update` 拒绝范围漂移，仅 `--force-update` 可重基线。新增范围契约 **5 passed**、broker 定向 **15 passed（1 existing deprecation warning）**，完整离线根回归 **1522 passed、2 warnings、89.88s**。 |
| v1.48 | 2026-09-20 | 修复 PR governance 的 `.gitmodules`/gitlink 单独变更绕过：精确识别元数据路径，flag 非真即 fail-closed；普通 `bt_api/` 文件不误判，报告模式仍不阻断。治理契约 **14 passed**，完整离线根回归 **1526 passed、2 warnings、99.41s**。 |
| v1.49 | 2026-09-20 | 新增 Task 19–23：PR governance schema v2/可信 base、SourceSupervisor 同键并发、docs CI fail-closed、optimized 安全/wheel smoke、same-candidate 发布设计。最终根 marker 为 **1569 passed、2 warnings、68.47s**，覆盖率 **66.94%**；GitHub/Pages/TestPyPI/PyPI 均未运行。 |
| v1.50 | 2026-09-20 | 新增 Task 24–25：canonical runner 默认 `not ctp`、`--ctp -m ctp`、branch coverage 和 pytest/tee fail-closed；optional performance 改为仅手工 opt-in，缺目录/报告/非空 benchmarks 即失败。runner 合同 **23 passed**，optimized 合同累计 **8 passed**（performance 新增 **3**）；真实 GitHub Actions/benchmark/network/CTP/SimNow 均 **NOT_RUN**。 |
| v1.51 | 2026-09-20 | 新增 Task 26：CI full-suite 固定 `python -m pytest tests -v -n 8` + `--cov-branch`，threshold 只经 step env 且以 0..100 十进制白名单/Bash 数组传参，XML 与 HTML 报告非空并严格归档。结构合同 **20 passed**，GitHub hosted **NOT_RUN**。 |
| v1.52 | 2026-09-20 | Task 23 从设计推进为本地实现：新增候选 manifest/摘要记录与复验，manual build-only，release 外部链统一 enable gate，TestPyPI 精确 wheel/hash + 本地 wheel/PyPI-only 依赖安装，PyPI 复验同一 artifact；独立锚覆盖 wheel、sdist 与 manifest。定向合同 **54 passed**，根回归 **1669 passed**，branch coverage **66.94%**。外部 D4/hosted/TestPyPI/PyPI 与 action 不可变 SHA 固定仍 **NOT_RUN / NO-GO**。 |
| v1.53 | 2026-09-21 | 新增 Task 27：执行日志迁移入口 **361 → 128**，冻结 preparation 对象替代 9 元组并抽取阶段 helper；RED 失败注入发现 COMMITTED 原子写前先改内存状态会跳过 rollback，现改为先持久化独立 payload。定向 **6 passed**、execution-session 文件 **161 passed**；根回归 **1671 passed、2 warnings、95.99s**，branch coverage **1671 passed、2 warnings、108.50s / 67.11%**。 |
| v1.54 | 2026-09-21 | 新增 Task 28 并关闭 S-07：8 个 `scripts/analysis/` 完整副本改为顶层 canonical 的薄兼容 wrapper；独立审查先后发现只读 proxy 的模块属性写入漂移、`patch.object` 删除/恢复失败与 `Path` 名称遮蔽，补齐私有基础设施别名、持久 canonical 属性集合和读写/删除恢复路由后，双路径/隔离合同 **41 passed**、顶层 8 文件 SHA256 不变、复审 **PASS**。同时复核 8 份 Forwarding 计划，待确认 **36 → 28**、已落地 **30 → 38**；根回归 **1696 passed、2 warnings、69.74s**，branch coverage **1696 passed、2 warnings、93.40s / 67.11%**。 |
| v1.55 | 2026-09-21 | 新增 Task 29：`coerce_funding_snapshot` **81 → 28**；独立审查发现首版提前读取外层 `Mapping.source` 会改变访问次数并泄漏原生异常，修复后 mapping/dataclass 行为、稳定错误原因和对象身份由 **17 passed** 保护，目标静态门禁通过。并以三批并行审计完成余下 28 份计划复核，最终待确认 **28 → 0**，真实分布为已落地 61、已废弃/阻塞 8、部分/未开工 5；精确 CI mypy 77 errors 与 owner 文件 format 失败继续保留。根回归 **1706 passed、2 warnings、93.10s**，branch coverage **1706 passed、2 warnings、115.70s / 67.24%**。 |
| v1.56 | 2026-09-21 | 新增 Task 30：`fill_missing_docstrings.py` 的 20 个 mypy 错误以精确 AST 类型收敛，整仓 **77 → 57 errors**。独立审查先后发现多行 body 非法缩进、同索引 module action 丢失、跨行 header inline suite 误插、tab 混缩进与 UTF-8 文件第二行 PEP 263 cookie 位置失效；全部以真实临时文件回归修复，并以写前验证 fail closed。最终定向 **23 passed**、目标门禁与独立复审 PASS；根回归 **1725 passed、2 warnings、100.03s**，branch coverage **1725 passed、2 warnings、119.54s / 67.24%**。 |
| v1.57 | 2026-09-21 | 新增 Task 31：F-85 后实时报告新增 4 个 `TC003`；三个测试注解专用导入经运行时用法核验后移入 `TYPE_CHECKING`，脚本 `Iterable[str]` 经真实 `get_type_hints` 回归证明为运行时依赖，故保留直接导入和报告命中。四测试 **48 passed**、目标 mypy/Ruff/format 与独立复审 PASS；报告 **40 → 37**，只计 3 个真实清理。该阶段完整根 marker **1725 passed、2 warnings、89.32s**。 |
| v1.58 | 2026-09-21 | 新增 Task 32：`check_quality_ratchet.py --force-update` 在现存 baseline 下不得缩小 recorded scope；范围扩张需复扫旧范围并拒绝旧债回升；same-scope 复用首次扫描并保持债务底线。新增三条不写快照故障注入且安全扩张仍通过；目标回归、静态门禁、质量棘轮与 diff-check PASS。 |
| v1.59 | 2026-09-21 | 新增 Task 33：收敛 artifact-first 路径的 5 个 mypy 边界错误，同时保持完整离线解析、fresh venv、`pip check` 与创建 artifacts 前拒绝非法 wheelhouse；新增 `Mapping` 真实运行时 guard 后，TC003 不回退。目标 **12 passed**、四文件 mypy/Ruff/format、债务报告与独立复审 PASS；可比 254 文件范围 **57 → 52 errors（13 → 11 files）**，精确 256 文件命令未重跑。根 marker/coverage 均为 **1731 passed、2 warnings**，覆盖率 **67.24%**。 |
| v1.60 | 2026-09-21 | 新增 Task 34：三个测试文件以 AST import 节点显式窄化和精确 PyArrow/YAML `ModuleType` 子类收敛 4 个 mypy 错误，不改变行为、断言或依赖隔离。目标 mypy **4 → 0**、定向 **7 passed**、Ruff/format、债务报告和独立复审 PASS；可比 254 文件范围 **52 → 48 errors（11 → 8 files）**，精确 256 文件命令未重跑。根 marker **1731 passed、2 warnings、92.95s**，branch coverage **1731 passed、2 warnings、135.56s / 67.24%**。 |
| v1.61 | 2026-09-21 | 新增 Task 35 / F-90：两份离线脚本测试以精确 `ModuleType` 子类建模 Playwright、browser-cookie3 与 requests 属性，目标 mypy **8 → 0**、定向 **4 passed**。fresh `--no-incremental` 可比 254 文件结果为 **41 errors / 7 files**；该运行额外暴露一个既有注解错误，故不把 Task34 的 48/8 与本值写成直接算术下降。 |
| v1.62 | 2026-09-21 | 新增 Task 36 / F-91：生产监控/Hyperliquid 两份离线测试的 18 个假模块属性改为精确 `ModuleType` 子类，目标 mypy **18 → 0**、定向 **7 passed**。独立审查发现并修复 `config_loader` 仅注解且以 `raising=False` 创建假接口：fixture 改为先绑定安全失败占位函数，异常注入恢复 `raising=True`。同一 fresh 254 文件范围 **41 → 23 errors（7 → 5 files）**；根 marker **1734 passed、2 warnings、91.45s**，branch coverage **1734 passed、2 warnings、119.06s / 67.27%**。 |
| v1.63 | 2026-09-21 | 新增 Task 37–41 / F-92–F-96：`_execution_session.py` 精确 key/value 注解 **1 → 0**（execution-session **164 passed**、128 source type-check PASS）；OKX WSS 测试精确 Event 联合 **5 → 0 / 3 passed**；workflow 测试路径化 runtime YAML guards 与坏形状负例 **5 → 0 / 21 passed**；websocket 基础设施与跨文件 receiver Protocol 保持 websockets>=12 并以 `port=0` 避免 8765 冲突，目标 **11 → 0**；probe 的 `list[str]` + `CompletedProcess[str]` **1 → 0 / 8 passed**。联合目标 **203 passed**，fresh 254 文件范围 **23/5 → 17/3 → 0/0**，精确 256 文件未重跑。最终根 marker **1735 passed、2 warnings、88.69s**，branch coverage **1735 passed、2 warnings、110.06s / 67.27%**。 |
| v1.64 | 2026-09-21 | 新增 Task42 / F-97：建立 exact singleton orderbook normalization benchmark、schema v1 版本化基线和 fail-closed checker；workflow/Make 均锁定 nodeid + `-n 8`，只有 checker 成功才上传，Make 临时报告自动清理。定向 **59 passed**，7 次本地有效报告 mean **8.096–32.879µs**、rounds **3613–14608**，0.5ms/1000 rounds 门禁通过；最新根 marker **1796 passed、2 warnings、95.29s**。该能力只防灾难性退化；GitHub hosted、多 benchmark 聚合和 schedule 恢复仍 **NOT_RUN / NO-GO**。 |
| v1.65 | 2026-09-21 | 新增 Task43 / F-98：机械拆分 `OrderRequest` 校验入口并补足精确边界/优先级合同；入口/core/intent C901 **18→1/9/10**，`models.py` **3→2**、主包 **84→83**。七文件 **81 passed**、目标覆盖完整、128 source mypy、Ruff/format/diff-check 与独立复审 PASS；最新根 marker **1824 passed、2 warnings、127.85s**，branch coverage 未重跑。 |

---

## 0.0 已完成（2026-09-19，含证据）

用户确认 **D1：15 个子仓全部纳入门禁**。门禁由**主仓侧**实施，**未改动子仓逻辑**。

### M1 止血 —— 完成

| Task | 结果 |
|------|------|
| T1 `F821` | **135 → 0**：okx 机械切分 95 处（87 处改自引用 + 2 处函数内惰性导入）、binance `Any` 未导入 14 处、examples 31 处死代码墓碑精简、1 处 `asyncio` 未导入 |
| T2 账号泄漏 | 真实账号 `089763`（9 处）→ 占位 `test_account`；`git grep 089763` = 0 |
| T3 遗留失败 | 3 项已通过（alpaca 用 monkeypatch、插件数断言已去除），无需改动 |
| T4 配置债 | `pyrightconfig.json` 路径修正；根 `AGENTS.md` 重写为项目真实规范；`stock_data/` 由 owner 处理 |

### M2 堵漏 —— 核心完成

| Task | 结果 |
|------|------|
| T5 Makefile 扩容 | `QUALITY_SCOPE`（34 路径）；新增 `lint-submodules` / `lint-all` / `lint-stat` / `quality-ratchet` / `-update` 及 `format-ratchet` / `-update`；`lint` 保持核心范围 |
| T6 CI 扩容 | `tests.yml` 新增 `quality-ratchet` 与 `format-ratchet`（均为**阻塞**，带递归子仓检出）；`quality-gate` 现逐项阻断这两项及 quality/compatibility/full-suite/wheel-contract；`submodule-tests.yml` 以 `discover-submodules` 驱动独立 `submodule-lint` / `submodule-format` 矩阵（均 report-only，且有结构回归测试） |
| T6-A 子仓静态安全 | 当前只读预检为 15 个 `src` 根 / 26 项（High 1 / Medium 3 / Low 22）；CTP `B507` High 先由子仓独立修复。High 清零后才能实施默认-profile、source-only、fail-closed Bandit 棘轮并接入 `quality-gate`；不把静态扫描混同依赖/secret/外部安全验收 |
| T7 棘轮 | `scripts/ci/check_quality_ratchet.py` + lint 快照 + **22 项**当前结构测试；`scripts/ci/check_format_ratchet.py` + 15 仓/452 文件 format 快照，逐仓拒绝增长、新仓和缺失仓，且 `--update` 只允许无增长的下降；两者合计 **45 passed**，均不自动修改子仓 |
| T8 覆盖率同源 | **完成**：确认 `pyproject.toml` 已有 `fail_under = 40`（此前误判为缺失，已订正）；它是默认门槛，dispatch 输入留空即沿用；Task 26 仅允许手工 dispatch 通过已校验的 step env 临时覆盖。最新同标记 branch coverage 为 **67.27%** |

### M3 清存量 —— 进行中（1301 → 10）

| Task | 结果 |
|------|------|
| T9 自动修复 | `W291 712→0`、`F401 164→0`、`I001 154→0`、`F541 21→0`、`PIE790 9→0`；docstring 内 266 处 W291 用 AST 定位精确清理 |
| T10 人工项 | `E701 36→0`（脚本拆分，**逐文件 AST 等价校验**）；root examples 再清理 11 项，完整范围 **21→10**。唯一根仓 `S603`/`B404`/`B603` 已收敛到 `_ctp_probe.py` 的 E-05 行级例外：静态命令、环境键白名单、输出脱敏和 8 个 monkeypatch 行为测试；不使用配置级忽略。余量仅为子仓 `TC002×4`/`S106×3`/`B017×2`/`E402×1` |
| T11 子仓配置统一 | **延后到子仓交付**：实测发现 14/15 子仓自带 ruff 配置且规则集不同（N-01），统一会改变基线口径；D1 的当前主仓范围不改子仓文件。每个子仓必须以独立任务、独立测试和独立提交交付，随后由父仓仅更新子模块指针；父仓任务不得替子仓擅自选定规则子集。 |

### 结构治理（okx mixins，用户指定优先治理）

| 项 | 结果 |
|----|------|
| 机械切分合并 | 25 个 `*_partN` 文件 → 合并回 8 个逻辑模块（44 → 20 文件） |
| 端点样板收敛 | 新增 `RestCallMixin`（`_finish` / `_rest` / `_rest_async`）；**514 个方法**由三段样板收敛为一行委托 |
| API 零变更 | **方法清单对账 916 → 916（0 丢失 / 0 新增 / 0 签名变化）**，测试无需改动（仅 1 处 Part 类引用同步更新） |
| 行数 | 20,369 → **17,129**（-3,240） |
| 验证 | okx 292 passed（排除 network）；bt_api_base 669 passed；根的最新并行证据见下（1336 passed）；棘轮 exit 0 |
| 迁移脚本 | 子仓 `scripts/merge_mixin_parts.py`、`scripts/collapse_endpoints.py`（严格模板匹配，形态不同即跳过并报告） |
| 已知余量 | ~90 builder（尾部键序不同）与 ~183 包装（内层调用为 `**extra_data` 展开等变体）保留原样 |

### 额外修复（迭代中发现的真实缺陷）

| 问题 | 修复 |
|------|------|
| `examples/risk_management_root_demo.py` 同步函数内 `await`（**无法编译**） | 改 `async def main()` + `asyncio.run()` |
| 4 个示例集成测试的 `# ruff: noqa` 写在 docstring 内（**机制失效**） | 移到文件顶部成为真正指令 |
| 2 个文件硬编码失效绝对路径 `/Users/.../source_code/bt_api_py` | 改为由 `__file__` 推导 |
| `pyproject.toml` 2 条 per-file-ignores 指向不存在的路径 | 替换为真实模式 |
| 参数化测试 ID 含实时时间戳（**阻塞 `-n` 并行**） | 加显式静态 `ids=[...]` |
| 5 个 Curve/Raydium/SushiSwap 示例测试引用已经删除的 DEX 实现 | 退役无法收集的测试模块；新增精确模块前缀 AST 回归测试，避免把旧导入重新带回示例 |
| SimNow CTP 探针的 `S603`/`B404`/`B603` 缺少可审计边界 | 提取为内部固定子进程 helper；固定命令/脚本/cwd、6 个环境键、输出脱敏和离线行为测试，行级 E-05 例外受台账与迭代文档约束 |
| CTP 前置本地 YAML 配置加载失败静默回退 | 先以临时配置文件和内存 `yaml` 替身固定“回退内置地址且不泄露 YAML 内容、路径或异常正文”的契约；随后只增加记录异常类型的 debug 诊断，未改选择规则、公开 API 或外部会话边界 |
| margin 文档脚本侧栏发现失败静默吞掉异常 | 先以假 `playwright.sync_api` 和假页面覆盖两个字节一致副本；失败时返回空列表并仅 warning 异常类型，未改抓取主流程或调用浏览器/网络 |
| 生产监控示例 exporter 停止失败静默吞掉异常 | 以 fake logging/monitoring 模块离线加载；停止成功保留 info，失败仅 warning 异常类型且 helper 返回，不调用 `main()` 或启动 exporter |
| 文档契约检查器 3 个 `PERF401` append 循环 | 锁定 certification entry 的缺字段/路径/SHA/过期错误顺序后，改为等价 `errors.extend(...)`；不改变文档判定或写入文档 |
| exchange-support 文档生成器 `PERF401` | 锁定自定义双交易所条目的表格行、状态与政策文字顺序后，将 `render()` 行构建改为同序列表推导式；`--check` 不写文档 |
| GitHub 治理校验器 `PERF401` | 锁定多个 required check 缺失时的 drift 消息顺序后，改为同序 `drifts.extend(...)`；不调用 GitHub API 或修改治理清单 |
| 两个棘轮脚本 `TC003` | 前次仅依据延迟注解的 `TYPE_CHECKING` 移动已由 F-55 纠正：`typing.get_type_hints` 发现运行时解析风险，故恢复直接导入；后续清理必须先通过反射契约 |
| 两个慢测试分析器 `PERF401` | 先锁定临时 JSON 中多条记录、缺失字段默认值与顺序；两个字节一致副本同步改为同序列表推导，不运行 `main()` |
| 两个覆盖率分析器 `PERF401` | 先 stub 报告依赖并锁定未测试交易所排序、模块行、低覆盖率筛选/顺序与全文；两个字节一致副本同步改为同序 `extend`，不调用 `pytest.main()` |
| 两个 v2 capability 提取器 `PERF401` | 先以非排序输入、混合 capability 与缺失字段锁定完整 Markdown/CSV，再同步以同序 `values.extend(...)` 收敛 4 处内层 append；副本保持字节一致，不读写文件或调用主入口 |
| 两个 v1 capability 提取器 `PERF401` | 先以非排序输入与混合 capability 锁定 Markdown/CSV，再仅同步收敛 CSV 的两处内层 append；副本保持字节一致，不读写文件或调用主入口 |
| 四个测试模块 `TC003` | 前次仅依据延迟注解的 `TYPE_CHECKING` 移动已由 F-55 纠正：恢复直接导入并新增反射回归；artifact-first 临时环境用例保持原断言且在离线包不可用时明确 NOT_RUN |
| 两个质量分析器 `PERF401` | 先用临时目录与确定性 walk 替身锁定根/子目录顺序、隐藏文件过滤及排除目录剪枝；再各自改为同序列表推导加 `extend`，不触碰两副本其他既有差异，也不调用 `main()` |
| 两个代码质量检查器 `PERF401` | 先用临时目录、确定性 walk 与 `check_file`/report 替身锁定默认/自定义排除、隐藏目录/文件、顺序、verbose、更新和异常隔离；再各自改为同序列表推导加 `extend`，不调用 CLI |
| 两个 docstring 文件发现器 `PERF401` | 先用临时目录/确定性 walk 锁定默认/自定义排除、动态 `*.egg-info` 剪枝、隐藏目录/文件与排序输出；再各自改为同序列表推导加 `extend`，不调用 CLI |
| 最后 7 个根 `PERF401` | `reconcile_tick_universe`、`split_ctp_wrapper`、`install_bt_api_submodules`、风险 limits、CTP close-plan 与 execution recovery 均先固定顺序/输出/错误语义，再做等价列表构造；全范围隔离扫描为 0，移除根/测试 ignore 与报告规则 |
| 最后 9 个根 `S110` | OKX request/WSS 的 best-effort 挂单清理、Hyperliquid 示例的外层失败和 WebSocket 接收循环均先以 fake-only 契约固定继续处理/返回语义，再仅记录异常类型；全范围隔离扫描为 0，移除根/测试 ignore 与报告规则，CTP vendor 例外保留 |
| 类型导入反射兼容性 | `tests/test_type_hint_introspection.py` 本地观察覆盖 63 个目标；发现并阻止部分 `TYPE_CHECKING` 移动破坏 `typing.get_type_hints`，保留既有直接导入和注解 API。该测试当前未跟踪，故为 **NOT_PERSISTENT**，不得单独作为交付验收证据 |
| 四个非反射测试注解 `TC003` | `test_check_docs_contract.py`、`test_monitoring_contracts.py`、`test_split_ctp_wrapper.py` 与 `test_submodule_validation.py` 只将注解专用 `Path`/`ModuleType` 移入 `TYPE_CHECKING`；运行时 `Path`、注解文本、fixture 与断言保持不变，根 `TC003` **20 → 16** |
| 五个私有风险检查 mixin `TC001` | 独立审查证明 `LimitsManager` 继承的私有 helper 仍可被 `typing.get_type_hints` 解析；五个 `RiskMetrics` 导入恢复为运行时，新增五个 helper 反射 target，根 `TC001` 保持 **22** |
| `PolicyEngine` 局部 `Callable` `TC003` | 仅用于 `__init__` 内局部实例属性注解，不出现在可反射的函数/类注解且无运行时构造/检查用途；初始化测试固定默认 handlers 非空且均可调用，根 `TC003` **16 → 15** |
| 风险限额 `CRITICAL` 返回 | 六个订单/持仓 helper 的关键分支均显式返回空 `warning`，避免未赋值局部变量使结果异常并被预交易入口吞掉；状态、restriction、阈值和字段结构保持不变 |
| OKX 只读 readiness mapper 为 198 行 | 仅抽取 checks/reasons/最终判定为私有 helper；入口签名、输出与调用顺序不变，AST span **198 → 139** |
| ZMQ forwarding 启动失败测试污染共享 `threading.Thread` | mock 改为仅替换 `service_module.threading` 的局部绑定，并追踪 publisher/command server 清理；消除 `-n 8` 下的后台 loop 死等 |
| CTP 预算准入计算为 170 行 | 先以纯 fixture 固定完整路径、状态扫描与后段 fail-closed 行为，再只提取 `_evaluate_reachable_states`；入口 AST span **170 → 128**，不改预算保留或外部会话边界 |
| 事件归一化 dispatcher 为 181 行 | 先固定 orderbook 列表档位、strict 配对和 continuity fallback，再只提取 `_orderbook_event_fields`；入口 AST span **181 → 134**，不改其他 event kind 或 `_finish_event` 边界 |
| Binance 只读 readiness 编排为 157 行 | 先固定从账户配置到可选账户读取的完整顺序、参数和 `extra_data` 深拷贝隔离，再只提取 `_get_binance_order_readiness`；公共 guard、OKX 分支和错误外壳不变，AST span **157 → 104** |
| PluginCatalog 将同名路径直接视为认证 | 认证须同时满足配置 tier、实体卡文件、安装、最低版本和入口点；缺卡、目录伪卡、未安装、版本不达标、无入口或 experimental 配置均按 `missing` / `loadable` / `installed` 降级，公开 `resolve_bundle()` 契约 **11 passed** |
| 可选 broker entry point 失败阻断适配器加载 | 单插件 `Exception` 仅做 best-effort 带 traceback 的诊断后继续；名称/日志观察失败也不改变发现，常规遍历完成才标记 loaded。元数据枚举/选择和 `BaseException` 保持传播与重试，测试注册表以快照隔离 |
| quality-ratchet 只检查快照路径缺失 | 对当前与快照范围做尾部斜杠归一化后的双向比较；普通门禁与 `--update` 拒绝新增/缺失 scope；审查后的 `--force-update` 也只记录安全扩张，禁止删除既有 scope 或抬高旧范围债务；新增独立回归 **5 passed** |
| `.gitmodules` 单独变更绕过 strict PR governance | 精确识别 `.gitmodules` 与 `bt_api/bt_api_<name>`；路径存在而 `submodules_changed` 非真即报具体违规并要求 gitlink/SHA 上下文。普通 `bt_api/readme.md` 与工具文件不误判，report-only 保持非阻塞 |

### 验证结果

- 根测试：`pytest tests -q -n 8 -m "not network and not integration and not performance and not e2e and not ctp"` → **1321 passed / 88s**（单进程原为 882s）
- 加入 Task15 全部五项等价拆分后的根测试：`./scripts/run_tests.sh -m "not network and not integration and not performance and not e2e and not ctp"` → **1340 passed、2 warnings、76.11s**（`python -m pytest -n 8`；CTP 标记排除；不运行外部 CTP）
- format-ratchet、工作流契约与 E-06 行级安全豁免纳入后的新鲜根测试：`python -m pytest tests -q -n 8 -m "not network and not integration and not performance and not e2e and not ctp"` → **1363 passed、2 warnings、75.95s**；专属格式棘轮测试 **23 passed**、工作流/两类棘轮联合测试 **42 passed**，`make format-ratchet` 仍逐仓复算 **452**，未运行网络、CTP 或 SimNow。
- `S112` 安全诊断与移除根全局 ignore 后的新鲜根测试：`python -m pytest tests -q -n 8 -m "not network and not integration and not performance and not e2e and not ctp"` → **1379 passed、2 warnings、73.73s**；该命令仅覆盖本地标记范围，未运行网络、ELK、CTP 或 SimNow。
- CTP 前置本地配置回退诊断：新的临时 YAML 失败契约在旧代码上为 **1 failed**（缺 debug），修改后 selector/报告定向回归 **18 passed**；`S110` 定向扫描通过，报告为 **4 条 / 82 项**；其后同一根 marker 回归为 **1379 passed、2 warnings、74.57s**。未连接 CTP、SimNow 或网络。
- margin 文档脚本侧栏诊断：两个副本的假 Playwright/假页面契约在旧代码上为 **2 failed**（无 helper），修改后 scraper/报告定向回归 **15 passed**；`S110` 定向扫描通过，报告为 **4 条 / 80 项**；其后同一根 marker 回归为 **1381 passed、2 warnings、88.93s**。未调用 `main()`、浏览器、Playwright 或网络。
- 生产监控示例 exporter 清理：fake 模块离线契约在旧代码上为 **2 failed**（无 helper），修改后 **2 passed**；目标 `S110`/format 通过，报告为 **4 条 / 79 项**。未调用 `main()`、启动 exporter、访问网络或等待。
- 文档契约检查器性能收敛：既有/补强的顺序契约 **2 passed**，目标 `PERF401`/format 和 `python scripts/ci/check_docs_contract.py` 通过，报告为 **4 条 / 76 项**；未访问网络或写入文档。两项合并后的新鲜根 marker 为 **1383 passed、2 warnings、74.69s**。
- exchange-support 生成器与 GitHub 治理校验器性能收敛：顺序契约分别 **3 passed**、**9 passed**，目标 `PERF401`/format 通过；前者 `--check` 通过且不写文档，后者不调用 GitHub API；报告 **76 → 74**。
- 质量/格式棘轮 type-only 导入收敛：两脚本定向回归合计 **41 passed**，目标 `TC003`/format 通过，`make quality-ratchet` 与 `make format-ratchet` 均通过且不更新基线；报告 **74 → 72**。三项合并后的新鲜根 marker 为 **1385 passed、2 warnings、76.91s**。
- 慢测试分析器性能收敛：两个脚本路径的临时 JSON 契约 **2 passed**，目标 `PERF401`/format 通过且 `cmp` 保持字节一致；报告 **72 → 70**，新鲜根 marker **1387 passed、2 warnings、75.63s**；不运行 `main()` 或访问网络。
- 覆盖率分析器性能收敛：两个脚本路径的 stub 报告契约 **2 passed**，目标 `PERF401`/format 通过且 `cmp` 保持字节一致；报告 **70 → 68**，新鲜根 marker **1389 passed、2 warnings、74.91s**；不调用 `main()`、`pytest.main()` 或写出报告。
- capability 提取器性能收敛：v2/v1 两对脚本均以纯内存完整 Markdown/CSV 契约保护，定向各 **2 passed**；目标 `PERF401`/format 通过且各自 `cmp` 字节一致，报告 **68 → 62**；未读写能力文件、调用主入口或访问外部服务。
- 测试模块 type-only 导入收敛：目标 `TC003`/format 与 `git diff --check` 通过，报告 **62 → 58**；不触发临时 venv/pip 的确定性范围 **42 passed**。完整 `PIP_NO_INDEX=1` 根回归为 **1390 passed、3 failed、2 warnings**：两项 artifact-first 临时环境测试与一项 wheel contract 均因本地 `bt_api_base` 不可用而失败，原断言未修改；显式排除这 3 项工件前置条件的根范围为 **1390 passed、2 warnings**。后者不替代完整 marker，故本版 AC-12 为 NO-GO，未访问索引或外部服务。
- 质量分析器性能收敛：两个脚本路径的临时目录 walk 契约 **2 passed**，目标 `PERF401`/format 与 `git diff --check` 通过；报告 **58 → 56**。不调用 `main()`、不写质量报告或访问外部服务。
- 代码质量检查器性能收敛：两个脚本路径的临时目录契约 **6 passed**，目标 `PERF401`/format 与 `git diff --check` 通过；报告 **56 → 54**。本批最终完整离线根回归为 **1398 passed、3 failed、2 warnings**，三项失败均为 `bt_api_base` 本地工件前置条件；显式排除后三项以外范围 **1398 passed、2 warnings**，不替代 marker。未调用 CLI、不写报告或访问外部服务。
- docstring 文件发现器性能收敛：两个脚本路径的临时目录契约 **4 passed**，目标 `PERF401`/format 与 `git diff --check` 通过；报告 **54 → 52**。不调用 CLI、不写文件或访问外部服务。
- 最后 7 个根 `PERF401`：`reconcile_tick_universe` **1 passed**、`split_ctp_wrapper` **2 passed**、`install_bt_api_submodules` **1 passed**、风险管理 **26 passed**、CTP close-plan **8 passed**、execution recovery **48 passed**；`ruff check bt_api_py tests scripts examples --isolated --select PERF401` 为 0，规则已从根/测试 ignore 与报告移除。
- 类型导入兼容性：`tests/test_type_hint_introspection.py -q -n 8` 本地观察为 **63 passed**。这项回归推翻了“延迟注解或私有命名即可安全移动 import”的假设；五个风险 mixin 的运行时导入已恢复。最近一次完整报告为 TC001 22、TC003 15；Tasks37–41 只有目标 TC 零新增证据，未取得 fresh 全范围库存。该历史 37 项不得为压债机械移动。F-86 只处理 3 个无运行时用法的测试注解，脚本 `Iterable` 运行时依赖保留直接导入和报告命中。该反射测试当前未跟踪，故为 **NOT_PERSISTENT**，须被显式纳入交付后才成为持久守卫。
- 风险限额关键分支：六个参数化 helper 契约与真实预交易入口契约使 `tests/test_risk_management.py` 为 **34 passed**；与反射/报告联合为 **111 passed**。修复只补空 `warning`，未改变限额状态、restriction、阈值或外部 I/O 边界。
- 工件闭包落地的根回归为：`PIP_NO_INDEX=1` 下 **1499 passed、2 warnings、97.25s**。Task43 后最新完整离线根回归为 **1824 passed、2 warnings、127.85s**；最近一次 `--cov-branch` 同范围复跑仍为 Task41 的 **1735 passed、2 warnings、110.06s** / **67.27%**，Task42/43 后未重跑 coverage。旧三项 artifact-first/wheel-contract 失败已由严格本地闭包修复并继续通过；外部 network/CTP/SimNow 仍未运行。
- Task16 完成时的历史 CI 同标记覆盖率：`python -m pytest tests -q -n 8 -m "not network and not integration and not performance and not e2e and not ctp" --cov=bt_api_py --cov-report=term-missing` → **1340 passed、2 warnings、83.00s**；总覆盖率 **66.45%**（阶段目标 ≥55%，`pyproject.toml` 门槛 40%）。该记录已由上一条 **1735 passed / 67.27%** 的后续同口径证据取代，不代表当前最新结果。
- 第三项的直接契约：`python -m pytest tests/bt_api_contract/test_ctp_execution_budget.py tests/bt_api_contract/test_execution_arming.py tests/bt_api_contract/test_execution_recovery.py -q -n 8` → **133 passed、5.75s**；其中纯预算文件 **4 passed**。
- 子仓对照实验（binance）：有/无本迭代改动均为 **165 failed / 432 passed**（失败为既有联网/密钥类测试，零回归）
- 子仓（okx）：排除 `network` 后 **292 passed**
- 子仓（bt_api_base）：**669 passed**
- 棘轮：`make quality-ratchet` → exit 0；快照由 1301 更新为 **10**（本批前为 21）

### 尚未落地

子仓 Task11（Ruff 配置统一）、10 项 lint 与当前 453 个格式化文件（快照基线 452，dirty OKX 87 > 86）的清理及分别晋级（均须以独立子仓任务/提交交付，见 N-01/N-18），以及根仓 2 条渐进 Ruff ignore 在最近一次完整报告中的 **37** 项逐条治理（见 G-07/G-08；Tasks37–41 未取得 fresh 全范围库存）。74 份计划的逐份复核已经完成；类型债余项须逐一证明运行时反射兼容或明确不进入反射面，不能仅凭 private/future annotations 移入 `TYPE_CHECKING`。

> M5-Task16（覆盖率 40% → 55%）**完成**：Task16 当时以 CI 同标记、并行根套件实测为 **66.45%**；后续同口径新鲜证据已升至 **67.27%**，均高于阶段目标 55%。

### M6 台账（部分完成）

| Task | 结果 |
|------|------|
| T17 计划与渐进债台账 | `docs/plans/README.md` 已完成 74/74 逐份复核，最终分布为已落地 61、已废弃·阻塞 8、部分·未开工 5、待确认 0；末轮三批证据为 **34/472/119 passed**。Task30 的精确 256 文件 mypy 为 **57 errors / 13 files** 且未重跑；Task33/34 的历史可比 254 文件范围依次为 **52 errors / 11 files**、**48 errors / 8 files**。Task35 的 fresh 同范围快照为 **41 errors / 7 files**，不与 Task34 数值写成直接因果差值；Task36 后为 **23 errors / 5 files**，Task37/38 后为 **17 errors / 3 files**，Task41 后最终为 **0 errors / 0 files**。这些都是排除两份禁读测试的 fresh 254 文件口径，不是精确 256 文件全量证据。历史技术债为 **37**；本批仅确认目标 TC 扫描无新增，没有 fresh `tech-debt-report`。其余 owner 文件 format 失败继续保留为部分完成。`PERF402`/`S113`、`PERF403`/`S112`、`PERF401` 与根 `S110` 均已移除，CTP vendor 的范围化 `S110` / `S112` 例外保留。类型导入的 63 目标反射观察禁止以 `TYPE_CHECKING` 破坏运行时注解 API；F-86 只清理 3 个无运行时用法的测试命中，脚本项继续留债 |
| T18 例外清单 | 配置级例外保持 `docs/TECH_DEBT.md` §4 与 `pyproject.toml` per-file-ignores 一致；E-05/E-06/E-07 是精确行级例外，均固定为源码行 + 台账 + 迭代设计/验收 + 行为测试。E-07 只允许固定 Ruff JSON argv、根 cwd 和 `shell=False`，配置/JSON/退出状态异常均 fail closed |
| T13 mypy 扩展 | **完成**：L1 五仓（binance/okx/bybit/gateio/hyperliquid）**416 → 0** 个 mypy 错误，新增 `make type-check-l1` 并接入 CI；过程中 mypy 抓出 2 类**真实缺陷**（bybit 错误映射死代码、okx 5 处错误返回注解），见 `docs/TECH_DEBT.md` F-15/F-16 |
| T14 巨型文件方案卡 | **完成**：`docs/refactor/` 下 5 份方案卡（`bt_api.md` 8864 行、`execution_session.md` 8251、`ctp_client.md` 4002、`normalization.md` 2511、`live_ctp_feed.md` 2025），含现状结构/风险/拆分边界/回归测试/**是否本期执行=否** |

---

## 0. 环境与基线

| 项 | 值 |
|----|----|
| 主仓 | `/Users/yunjinqi/Documents/new_projects/bt_api_py`（分支 `dev`） |
| 子仓 | `bt_api/bt_api_{binance,okx,bybit,gateio,hyperliquid,htx,mexc,bitget,coinbase,kraken,dydx,mt5,ib_web,base,ctp}` |
| 质量工具 | ruff 0.16.2、mypy、bandit、pip-audit、gitleaks、coverage（均已在 CI 使用） |
| 扫描基线（2026-09-19 实测） | 门禁范围 ruff **1301**（W291 712 / F401 133 / **F821 135** / I001 78 / E701 36 / S106 22 / 其他 16）；主包 `bt_api_py` **0** |
| 测试基线 | 根 1523 收集（CI 标记过滤 1303）；3 个遗留失败（alpaca ×2、硬编码插件数 ×1） |
| 覆盖率基线 | CI 门槛 40%；`pyproject.toml` 无 `fail_under` |
| 结构基线 | >800 行文件 18 个；>150 行函数 42 个 |
| 注解/文档基线 | 参数注解完整率 23%；公开函数 docstring 66%；函数 docstring 59% |

## 1. 全局约束

1. **不改业务行为**：质量迭代只做"等价改写/补测/补注解/门禁"，任何行为变更需单独 issue。
2. **不用 `--unsafe-fixes`**：`ruff --fix` 仅用安全修复集。
3. **按仓分批**：每批单一主题、独立提交、可独立 revert；子仓改动不强制发版。
4. **测试全绿**：每批提交前，受影响的测试套件必须全绿（根 + 相关子仓）。
5. **例外显式**：新增任何 `ignore`/`noqa`/例外路径，必须同时登记到 `docs/TECH_DEBT.md` 与本迭代文档。
6. **门禁先报告后阻塞**：新增的子仓门禁先以 report-only 运行，确认噪音可控后再切为阻塞。

---

## 2. 任务清单

### M1 止血（P0）

#### Task 1：修复 `F821 undefined-name`（真实缺陷）
- 内容：逐处定位 103 个未定义名并修复。
  - 优先 `bt_api/bt_api_okx/src/bt_api_okx/feeds/live_okx/mixins/`（`ta_part1~3`、`copy_trading_mixin_part1~3`、`rfq_mixin_part1~3`、`statistics_mixin_part1~2` 等，≈89 处）——机械切分丢 import。
  - 其次 `bt_api/bt_api_binance/src/bt_api_binance/feeds/rest_market.py`（14 处）。
- 方法：**先补 import 止血**；"是否合并回原文件"留到 M5 结构治理评估。
- 产出：子仓 `fix/quality-f821` 分支提交（binance、okx 各一笔）。
- 验证：`ruff check bt_api/bt_api_binance/src bt_api/bt_api_okx/src --select F821` → 0；
  子仓测试全绿（okx/binance 各自测试目录）。

#### Task 2：清除真实账号泄漏
- 内容：`tests/bt_api_contract/test_gateway_runtime_integration.py` 中真实 CTP 账号 `089763`（≥8 处）
  改为占位常量（如 `EXAMPLE_ACCOUNT_ID`），并确认密码从未入库。
- 产出：主仓修复提交 + gitleaks 范围说明。
- 验证：`git grep -n "089763"` → 0；`pytest tests/bt_api_contract/test_gateway_runtime_integration.py -q` 全绿。

#### Task 3：处置 3 个遗留失败测试
- 内容：对 `docs/acceptance/2026-08-17-quality-baseline.json` 记录的 3 个失败：
  ① 两个 alpaca 插件未安装用例 → 若插件已从保留清单移除则删除/改 skip 并注明；② 硬编码 `>=61` 插件数断言 → 改为动态断言（对齐迭代05"无固定插件数"的方向）。
- 产出：测试修复提交或显式 skip + 台账登记（二选一，需在验收中说明理由）。
- 验证：`pytest tests/test_bt_api_plugin_integration.py tests/test_plugin_discovery.py -q` 结果与台账一致。

#### Task 4：配置债修复
- 内容：① `pyrightconfig.json` 的 `extraPaths`/`include` 指向不存在的 `bt_api_base/`、`packages/` → 修正为真实路径或删除该文件并说明；
  ② `AGENTS.md` 引用缺失的 `.joyincode/rules/{backend,frontend}.md` → 补文件或改为指向 `docs/CODE_QUALITY.md`；
  ③ `stock_data/` 忽略规则**已由 owner 落地**（本项仅需复核覆盖完整）；
- 验证：`ls` 引用路径均存在（或文件已删）；`git status` 不再出现 `stock_data/` 未忽略告警。

### M2 堵漏（P0，核心）

#### Task 5：Makefile 门禁范围扩容
- 内容：已引入 34 路径的 `QUALITY_SCOPE`。日常 `make lint` 保持核心快速检查；完整范围由
  `make lint-all` 与阻塞的 `make quality-ratchet` 承担，避免把慢速全仓扫描伪装成日常命令。
- 验证：`make lint-all` 的路径与 `check_quality_ratchet.py --print-scope` 一致；`make quality-ratchet` 覆盖全部路径且不能回退。

#### Task 6：CI 门禁扩容（矩阵 + 先报告后阻塞）
- 内容：已落地阻塞 `quality-ratchet`，并将原合并的 report-only `submodule-quality` 替换为独立
  `submodule-lint` / `submodule-format` 矩阵。**不得**直接移除任一 job 的 `continue-on-error`：2026-09-20
  本地复算仍有 10 个子仓 lint 项和当前 453 个待格式化文件（快照基线 452）。只有该阶段自己的存量清零、CI 结果稳定且能独立回滚后，
  才把对应 job 切为 blocking。
- 验证：`tests/test_submodule_workflow.py` 当前只是未跟踪的本地观察（**NOT_PERSISTENT**）；在 owner 显式纳入交付前，不得把它作为两个 job 的发现矩阵、命令分离、report-only 状态或晋级条件的持久验收证据。PR 中
  可见每子仓独立结果；lint/format 的 blocking 切换必须分别提交、分别可 revert，不以 report-only 红灯冒充通过。
- 聚合纠偏（2026-09-20）：`quality-gate` 此前未把 `quality-ratchet` 列为 needs/result check，现已补齐
  needs、非 success 的 `exit 1` 与 summary 行；`tests/scripts/test_check_quality_ratchet.py` 以 YAML
  结构合同锁定，相关两个棘轮测试为 **45 passed**。这只修复已有 required 汇总的真实性，不改变子仓 report-only 晋级条件。

##### Task 6-A：子仓静态安全棘轮准入（P0，当前 NO-GO）

- 当前证据：固定默认 Bandit profile、`--ignore-nosec` 和全部 15 个已检出子仓 `src` 根的只读扫描为
  **26** 项（High 1 / Medium 3 / Low 22）。根 `pyproject.toml` 跳过 `B101/B104/B105`，不能用作该范围的
  安全策略；扫描不导入或安装子仓。
- 前置修复：唯一 High 为 `bt_api_ctp/src/bt_api_ctp/cleaner/pull/sftp_backend.py` 的 `B507`。先由 CTP
  子仓独立提交将 `AutoAddPolicy` 改为加载系统 known_hosts 后的 `RejectPolicy`，新增 fake-Paramiko 离线回归，
  并更新 cleaner 部署说明的可信 fingerprint 预置要求。不得以 `nosec`、根 ignore 或快照豁免替代修复。
- 后续根仓交付：High 为 0 后新增 source-only、默认-profile 的 Bandit ratchet；固定 argv/cwd/无 shell，校验
  JSON、`errors`、范围、模块与 Python 文件指标并 fail closed，按 finding identity 比较；High 绝对上限为 0，
  `--force-update` 也不得放宽。再以递归子仓 checkout 的独立阻塞 job 接入 `quality-gate`。
- 验证：CTP child 定向测试、Ruff 与 `B507` 扫描均通过；根脚本须覆盖缺失/逃逸子仓、异常退出、坏 JSON、计数增加、
  新 finding、快照降低与 High 不可豁免。此门禁不替代 pip-audit、gitleaks、发布或 CTP/SimNow 外部验收。

#### Task 7：棘轮脚本与首个基线快照
- 内容：新增 `scripts/ci/check_quality_ratchet.py`（比对当前扫描 vs 快照，规则见设计 §3.2）
  与快照 `docs/acceptance/2026-09-19-quality-ratchet.json`（含例外清单）；接入 CI。
- 验证：人为新增一处 `F401` → 脚本 fail 并打印位置；恢复后 pass；规则清零时提示更新快照。快照记录路径缺失或当前新发现路径未入快照均必须失败；普通 `--update` 不得写入范围漂移，审查后的 `--force-update` 也只允许记录安全扩张，禁止删除既有 scope 或抬高旧范围债务。

#### Task 8：覆盖率门槛同源
- 内容：`pyproject.toml` 恢复 `[tool.coverage.report] fail_under`（当前值 40）；CI 改为引用同一来源；
  同步修正 `docs/plans/2026-03-19-*.md` 与 `docs/CODE_QUALITY.md` 中"80"的失真表述。
- 验证：`pytest tests -m "<CI 标记>" --cov=bt_api_py` 在低于门槛时失败；文档中门槛数值一致。

### M3 清存量（P1）

#### Task 9：自动可修项清理（约 783 处）
- 内容：`ruff check --fix`（安全修复集）分批清理 `W291`(712)、`I001`(78)、`PIE790`(9)、部分 `F401`；
  **按仓分片提交**（binance / okx / 其他），每批测试全绿。
- 验证：`ruff check <scope> --statistics` 中 W291/I001/PIE790 → 0；受影响仓测试全绿；棘轮快照更新（下降）。

#### Task 10：不可自动修项与 `S106` 收口
- 内容：现余子仓 `B017×2`、`TC002×4`、`E402×1`、`S106×3`；前四类在对应子仓中单主题修复并独立验证，
  `S106` 以具名占位常量替换。根仓 `S603`/`B404`/`B603` 已完成：E-05 仅保留在 `_ctp_probe.py` 的固定子进程调用，
  以静态命令/脚本、固定 cwd、环境键白名单、输出脱敏与 mock 行为测试维持隔离语义。禁止扩大为 `per-file-ignores` 或添加无依据的 `noqa`。
- 验证：最终 `make lint-all` → 0；配置级例外须同步 `pyproject.toml`、台账和验收证据；精确行级例外须同步源码行、台账、设计/验收与行为测试。

##### Task 10-A：剩余 10 项的子仓原子交付图（2026-09-20 审计复核）

以下是当前子模块 SHA 的只读审计结论。复核时 Binance、Bitget、Bybit、IB Web、OKX 的工作树均干净，父仓 gitlink 与表中子仓 HEAD 一致。列出的“最小改动”是待子仓 maintainer 实施并验证的设计，不是已完成的代码变更。每一行必须作为单一子仓 PR/提交交付；父仓仅在子仓提交可用后更新对应 gitlink。

| 交付 ID / 审计基线 | 残余位置 | 预期最小改动 | 已知离线证据与子仓验收门 |
|---|---|---|---|
| Q10-BINANCE-B017 / f4b0a7d | tests/feeds/test_demo_environment.py:101，B017×1 | 将盲捕获 `Exception` 收窄为公开边界抛出的 `RequestTimeoutError`；MockTransport 在内层注入的是 `httpx.ReadTimeout`，但经 base `HttpClient`/feed 映射后不可直接向调用方传播。保留一次请求、不重试断言 | 变更前 test_order_timeout_is_not_retried 已在离线 MockTransport 下通过（1 passed）。子仓须以 python -m pytest -q -n 8 的该 case 和 ruff check 该文件复验 |
| Q10-BITGET-S106 / 3651036 | tests/feeds/test_bequant_bitget_coinbase_request_base.py:81,92，S106×2 | 将 api_key/api_secret 的字面量移为具名测试占位常量；不得增加 ignore 或 noqa | 当前根环境缺少 `bt_api_bequant`，原联合测试在收集阶段 ModuleNotFoundError，故此项为 NOT_RUN 而非通过。子仓 CI 须先安装可追溯、锁定 sibling SHA 的 BeQuant/Coinbase 工件并验证导入来源，再运行该文件的两个 alias case 与 ruff check；不得借用偶然存在的外部 editable 安装 |
| Q10-BYBIT-S106 / c9a8e64 | tests/feeds/test_bybit_request_base.py:39，S106×1 | 将 api_key/api_secret 的字面量移为具名测试占位常量（`TEST_API_KEY = "public-key"`、`TEST_API_SECRET = "secret-key"`）；不改别名兼容行为 | 变更前 test_bybit_accepts_api_key_and_api_secret_aliases 已通过（1 passed）。子仓须运行该 case（-n 8）和 ruff check 该文件 |
| Q10-IBWEB-E402 / 6bcbd07 | tests/containers/ib/test_ib_order.py:10，E402×1 | 在动态模块加载前完成 OrderStatus 顶层导入；保留动态导入和导出别名行为 | 变更前该测试文件已通过（28 passed）。子仓须运行 python -m pytest -q -n 8 tests/containers/ib/test_ib_order.py 和对应 ruff check |
| Q10-OKX-TC002-B017 / 37ef814 | containers/markprices/okx_mark_price.py:8、containers/tickers/okx_ticker.py:8、plugin.py:4,6（TC002×4）；tests/feeds/test_demo_environment.py:109（B017×1） | 依托 future annotations，只把 Self、GatewayRuntimeRegistrar、ExchangeRegistry 移入 TYPE_CHECKING；PluginInfo 保持运行时导入。超时断言收窄为公开边界的 `RequestTimeoutError`，而非内层 MockTransport 注入的 `httpx.ReadTimeout`。先新增覆盖 register_plugin 导入/注册的离线测试，防止类型导入移动掩盖插件入口回归 | 变更前 timeout、两个容器和注册模块的选定离线集已通过（5 passed；ticker 有既有 unknown-mark warnings）。子仓须运行这些 cases、新增插件入口 case、相关 ruff check 与受影响 mypy；不得以延迟导入绕过入口测试 |

所有 pytest 命令均以子仓自己的测试依赖为前提并使用 -n 8；不运行 network、CTP 或 SimNow 会话。所有五个子仓的交付还须满足：

1. 先复核干净工作树及审计 SHA（或在 PR 中明确 rebase 后 SHA）。
2. 先让现有或新增的行为测试在改动前或同一 PR 中证明目标语义，再做最小修改。
3. 子仓范围 ruff 清零、受影响测试通过、独立提交后，才允许父仓单独更新 gitlink。
4. 父仓 closeout 重新运行 make lint-all、make quality-ratchet；只有 10 项降为 0 才能把 AC-8 改为通过。当前 453 个 format 存量（快照基线 452）仍是 AC-5 的独立阻塞条件。

##### Task 10-B：452 个 format 存量的逐仓交付排程（2026-09-20 复算）

采用 CI 同版本 Ruff 0.16.2，并逐仓执行 ruff format --check bt_api/bt_api_*. 结果为 **452** 个待格式化文件，与 submodule-format 工作流注释和结构测试一致。该数字包含 Ruff 支持的 README/docs 代码块及 Python 文件，不能被表述为“452 个 Python 文件”或以无测试的大批量自动改写处理。

根仓已将该分布固化到 `docs/acceptance/2026-09-20-format-ratchet.json`，并新增 `make format-ratchet` 与 CI `format-ratchet` 阻塞 job。它对每个当前检出的 `bt_api/bt_api_*` 单独调用同一命令：任一仓计数上涨、出现未入库的新仓或已记录仓缺失都会失败，`quality-gate` 同时检查该 job 的结果。此门禁**不会执行** `ruff format`，不会改写子仓，也不把 `submodule-format` 从 report-only 升级为 blocking；当前 dirty OKX 使实际存量为 453，因此相对 452 快照正确失败。`make format-ratchet-update` 只可在所有仓未增长且至少一仓下降后刷新快照；刻意调整范围/基线须审查后显式使用 `--force-update`。

| 优先级 | 子仓 | 待格式化文件 | 交付约束 |
|---|---|---:|---|
| P0 | bt_api_binance | 100 | 仅 format-only 子仓提交；与 Q10-BINANCE-B017 的 lint 修复分开，防止 report-only 两维失去独立回滚性 |
| P0 | bt_api_okx | 86 | 仅 format-only 子仓提交；与 Q10-OKX-TC002-B017 分开，并复验插件入口及容器行为 |
| P1 | bt_api_ib_web | 40 | 仅 format-only 子仓提交；与 Q10-IBWEB-E402 分开 |
| P1 | bt_api_base | 33 | 单独 format-only 子仓提交；其余插件以该公共依赖的测试基线为准 |
| P1 | bt_api_ctp | 25 | 单独 format-only 子仓提交；不启动 CTP/SimNow，采用本地 marker 过滤回归 |
| P1 | bt_api_gateio | 25 | 单独 format-only 子仓提交 |
| P1 | bt_api_coinbase | 23 | 单独 format-only 子仓提交 |
| P1 | bt_api_htx | 23 | 单独 format-only 子仓提交 |
| P1 | bt_api_hyperliquid | 23 | 单独 format-only 子仓提交 |
| P1 | bt_api_dydx | 20 | 单独 format-only 子仓提交 |
| P2 | bt_api_bybit | 18 | 单独 format-only 子仓提交；与 Q10-BYBIT-S106 分开 |
| P2 | bt_api_mexc | 12 | 单独 format-only 子仓提交 |
| P2 | bt_api_bitget | 10 | 单独 format-only 子仓提交；与 Q10-BITGET-S106 分开，不能借 format 跳过其依赖缺口 |
| P2 | bt_api_kraken | 7 | 单独 format-only 子仓提交 |
| P2 | bt_api_mt5 | 7 | 单独 format-only 子仓提交 |

每个 format-only 子仓交付先执行 ruff format 对该子仓、检查 git diff 仅包含格式变更，再运行 ruff format --check 和受影响的本地测试（统一使用 python -m pytest -q -n 8；排除或不配置任何外部网络会话）。子仓 owner 独立提交后，父仓只更新对应 gitlink，并重新执行 `make format-ratchet`；只有无仓增长且确有下降时才执行 `make format-ratchet-update` 固化新低。所有 15 个子仓 format 归零、各自 CI 稳定且可独立 revert 前，submodule-format 保持 report-only。

#### Task 11：子仓 ruff 配置统一
- 内容：各子仓 `pyproject.toml` 的 ruff 规则集对齐父仓（先取父仓规则的子集，逐步收紧）；
  排除生成代码路径。
- 验证：同一文件在父仓与子仓扫描结果一致（无"父仓报、子仓不报"）。
- 交付边界（2026-09-20 澄清）：D1 规定当前迭代的父仓门禁实施不改子仓文件，且每个子仓都有独立版本历史。
  因此本任务必须拆为子仓级任务：先由 owner 选择规则子集，再在目标子仓内完成测试与独立提交，最后由父仓仅更新
  子模块指针。不得在当前父仓变更集里批量修改或提交子仓配置。

### M4 类型与文档（P1）

#### Task 12：公开 API 注解与 docstring 补齐（主包）
- 内容：先新增并测试一个限定主包公共 API 的 AST 指标（明确排除测试、示例和生成代码）；随后再补
  `bt_api_py/bt_api.py`、`exceptions.py`、`data_downloader.py`、`forwarding/`、`gateway/`、`brokers/` 的高价值公开接口。
- 目标：以该指标的固定分母评估公开函数 docstring ≥85%、参数注解 ≥45%；不为 Queue 兼容壳批量填充无信息模板。
- 结果（2026-09-20）：**完成**。`scripts/measure_public_api_quality.py` 与 `tests/scripts/test_public_api_quality.py` 固定源级 public callable 分母；`make public-api-quality` 输出 docstring **594/691（85.96%）**、参数注解 **1016/1035（98.16%）**，均达到目标。旧 `analyze_docstrings.py` 仅保留为全树诊断。
- 验证：fixture 测试覆盖 public/private、同步/异步、property、overload、隐式接收者、空分母、路径排除与 JSON 输出；`python -m pytest tests/scripts/test_public_api_quality.py -q -n 8` → **5 passed**。

#### Task 13：mypy 覆盖扩展到 L1 子仓
- 内容：对 binance/okx/bybit/gateio/hyperliquid 逐步开启 mypy（可先 `disable_error_code` 放宽，再收紧）；
  每仓记录先/后错误数作为棘轮基线。
- 验证：`mypy bt_api/bt_api_<x>/src --ignore-missing-imports` 错误数 ≤ 仓级基线。

### M5 结构与覆盖率（P2）

#### Task 14：超长文件拆分方案卡（Top 5）
- 内容：为 `bt_api.py`(8864)、`_execution_session.py`(8251)、`ctp/client.py`(4002)、
  `_normalization.py`(2511)、`live_ctp_feed.py`(2025) 各产出一份方案卡（设计 §3.6 字段）。
- 验证：5 份 `docs/refactor/*.md` 存在，且每份含"是否本期执行"的明确结论。

#### Task 15：超长函数治理（主包内 5 个）
- 内容：先为每个候选函数建立职责清单、输入输出契约和最小回归测试，再一次只拆一个函数；不在没有
  直接行为测试的前提下批量移动 5 个高风险函数。首批候选及其测试由评审记录选择。
- 验证：每一函数拆分前后相关测试全绿、公共导入不变、函数长度 ≤150；未完成的候选保留 `TODO`，不得以方案卡替代实现通过。

##### 首项评审与实施结果（2026-09-20）

| 项 | 结论 |
|----|------|
| 候选 | `bt_api_py._venue_mappers.okx.normalize_order_readiness`（198 行） |
| 选择理由 | 纯只读响应归一化，不发网络请求、不提交订单；通过 `BtApi.get_order_readiness` 有现成的本地行为契约，适合作为单函数等价拆分的低风险首项。 |
| 职责清单 | 解析账户权限/等级/持仓模式；定位合约与数量网格；归一化杠杆和双向最大下单量；生成 checks、按既有顺序生成 reasons，并组装只读 readiness 快照。 |
| 输入契约 | 固定函数签名的交易所、合约、原生数量、保证金/预期持仓模式，以及账户/合约/杠杆/最大数量的已读取响应；不接受或发起任何 I/O。 |
| 输出契约 | 返回键集合、`checks` 键集合、`reasons` 顺序、`ready` / `definite_failure` / `execution_unproven=True` 的判定均保持不变；公共导入与调用签名不变。 |
| 最小回归集 | `test_okx_readiness_combines_only_read_operations`（完整快照与只读调用）、`test_okx_readiness_rejects_native_quantity_outside_instrument_rules`、`test_okx_readiness_never_accepts_invalid_native_quantity`、`test_okx_readiness_missing_account_instrument_is_definite_and_short_circuits`、`test_okx_readiness_maximum_native_size_is_checked_for_both_sides`、`test_okx_readiness_requires_both_leverage_sides_in_dual_side_mode`。 |
| 实施结果 | 提取私有 `_build_okx_readiness_assessment` 承担 checks、reasons 与最终布尔判定；入口函数 AST span **198 → 139**，无公共导入、函数签名、调用顺序或测试期待变更。 |
| 验证 | 该契约文件 **41 passed**；入口 span 由 AST 复算为 139；改动文件 Ruff/format 通过，加入后的根并行回归 **1336 passed、2 warnings、74.00s**。 |
| 边界 | 本批仅拆该函数，计为 Task15 的 **1/5**；后续候选仍须逐项评审。不得借机调整交易判断、调用顺序或对外 API。 |

##### 第二项评审与实施结果（2026-09-20）

| 项 | 结论 |
|----|------|
| 候选 | `bt_api_py._venue_mappers.binance.normalize_order_readiness`（180 行） |
| 选择理由 | 与首项同为纯已读响应归一化，位于相同的上游两阶段 preflight 调用边界；已有 Binance 本地契约测试，能把本批限制为一处等价拆分。 |
| 职责清单 | 解析账户/持仓模式和交易规格；汇聚用户保证金配置、杠杆与最大数量；生成 checks、按既有顺序生成 reasons，并组装 readiness 快照。 |
| 输入契约 | 固定函数签名的交易所、合约、原生数量、保证金/预期持仓模式，以及账户/交易规格/持仓模式/用户配置/杠杆/最大数量的已读取响应；不接受或发起任何 I/O。 |
| 输出契约 | 返回键集合、`checks` 键集合、`reasons` 顺序、`ready` / `definite_failure` / `execution_unproven=True` 和仪器规格错误编码的表达均保持不变；公共导入与调用签名不变。 |
| 已有回归集 | `test_binance_readiness_uses_account_mode_symbol_rules_and_leverage`、`test_binance_readiness_keeps_missing_symbol_config_unproven`、`test_typed_binance_readiness_sync_and_async_share_contract`。 |
| 拆分前补强 | 增加行为测试覆盖：①保证金模式不匹配与零杠杆时 `False` checks、原因顺序和 `definite_failure`；②非法原生数量在上游初次快照中短路，不读取可选账户数据。测试必须先表达现有行为，不得因拆分而放宽。 |
| 实施结果 | 两条补强测试先固定行为后，仅提取私有 `_build_binance_readiness_assessment` 承担 checks/reasons/最终判定；入口 AST span **180 → 143**，函数签名、读取顺序、公共导入和既有期待不变。 |
| 验证 | readiness 契约文件 **43 passed**；改动文件 Ruff/format 通过；加入后的根并行回归 **1338 passed、2 warnings、76.11s**。 |
| 边界 | 本批仅拆该函数，计为 Task15 的 **2/5**；余 3 项及当前非 CTP 清单中的其余 20 个候选仍 TODO。不改业务判定、可选读取顺序、公共 API 或其他候选函数。 |

##### 第三项评审与实施结果（2026-09-20）

| 项 | 结论 |
|----|------|
| 候选 | `bt_api_py._ctp_budget.evaluate_ctp_budget`（170 行）。 |
| 选择理由 | 模块明确不拥有文件、账户连接或 journal；该函数只消费调用方给定的证据字典、可选时间与模式，产生纯 `CtpBudgetEvaluation`。相比执行会话、恢复计划和事件归一化主路径，它能以本地 fixture 覆盖，不启动 CTP/SimNow 或发起外部 I/O。 |
| 风险分级 | **高语义风险、低副作用风险**：它是预算保留前的 fail-closed 准入计算，既被公共 `BtApi.evaluate_ctp_execution_budget` 暴露，也被 `_ExecutionSession.reserve_ctp_execution_budget` 二次调用；不得把“纯函数”误写为低风险业务逻辑。 |
| 职责清单 | 校验顶层证据/上下文；扫描可达状态并累计 state costs、ordinary/recovery 峰值与 reasons；判定完整覆盖；计算 PnL 限额、可用余额和过期条件；输出去重但保序的失败原因与不可变结果。 |
| 输入/输出契约 | 仅接受精确 `dict` 证据、`ordinary`/`recovery` 模式和可选 UTC 比较时间；保持 `CtpBudgetError` code、`CtpBudgetEvaluation` 全字段、state-cost 顺序、reason 的首次出现顺序、`accepted`/`coverage_complete`/`write_eligible` 语义，以及公共静态入口和 session 保留边界不变。 |
| 现有回归 | 直接单测 `test_handpicked_final_basket_cannot_pass_path_budget` 固定不完整路径；`test_execution_arming.py`、`test_execution_recovery.py` 经 `reserve_ctp_execution_budget` 覆盖 write-eligible 完整包与 session 围栏。该直接集不足以单独保护状态扫描的所有失败分支。 |
| 拆分前补强 | 先新增纯本地行为测试：①完整 ordinary/recovery 路径的数值、state-cost 顺序、accepted/write-eligible；②重复/上下文不匹配/恢复不重叠等状态扫描失败的 reasons 首次出现顺序；③余额不足、未知估值、过期等后段 fail-closed 原因。测试只表达现有行为，不触碰 session、网络或真实 CTP。 |
| 实施结果 | 三条纯 fixture 契约先在原实现通过，随后只提取 `_evaluate_reachable_states`；入口保留顶层证据、PnL、资金、expiry、最终 accepted/write-eligible 与结果构造。AST span **170 → 128**，helper 为 61 行。 |
| 验证 | `test_ctp_execution_budget.py` **4 passed**；加 arming/recovery 调用链的定向并行回归 **133 passed、5.75s**；改动文件 Ruff/format、`git diff --check` 通过；新鲜根并行标记回归 **1338 passed、2 warnings、76.25s**。 |
| 边界 | 本批计为 Task15 的 **3/5**；主包 >150 行函数清单 **20 → 19**。没有改动 `BtApi`、`_ExecutionSession`、外部会话或预算保留写入；所有测试均为本地 fixture/marker 回归，不能证明 CTP/SimNow 连通性或交易可用性。 |

##### 第四项评审与实施结果（2026-09-20）

| 项 | 结论 |
|----|------|
| 候选 | `bt_api_py._normalization.normalize_event`（181 行）。 |
| 选择理由 | 它是由 `BtApi` 的已收集事件路径调用的纯归一化分发器，本身不发网络、不开线程、不写持久化状态。虽然完整 dispatcher 回归面较广，但 orderbook 分支只消费单一 row，具有现成的 `test_orderbook_sequence.py` 专项本地契约，适合将本批严格限制为一个独立职责簇。 |
| 风险分级 | **中等语义风险、低副作用风险**：函数还分发 order/trade/position/account/tick/bar/reconcile，故不得改动 kind 推断、其他分支或 `_finish_event` 边界；只抽取 orderbook fields，不能把整个 dispatcher 拆散。 |
| 职责清单 | 解析 payload/kind 并分发既有 mapper；orderbook 分支将显式或列表拆分的价量档位、sequence/previous sequence、snapshot/delta 与 continuity 状态归一化到 event 字段。 |
| 输入/输出契约 | 维持 `item`、`exchange_name`、`kind`、`symbol` 和 CTP parent-attestation 参数；orderbook 返回的 `bids`/`asks` 数值化、`quantity_unit`、sequence、previous sequence、snapshot/delta、continuity 语义及 `_finish_event` 补充字段不变。 |
| 已有回归 | `test_orderbook_sequence.py` 覆盖 `u`/命名 sequence/缺失 sequence、零 previous sequence、Binance WSS/REST 容器；`test_normalized_public_api.py` 的 polling/coalescing 路径与其他 mapper 测试覆盖 dispatcher 边界。 |
| 拆分前补强 | 增加纯事件测试：`bid_price_list`/`bid_volume_list` 与 ask 对应列表的 strict 配对、数值化；显式 `action=update` 形成 delta，未知 continuity 值仍归为 `unverified`。测试须先在旧实现通过，且不测试私有 helper。 |
| 实施结果 | 列表档位、strict 配对、`action=update` 和未知 continuity 的组合契约先在旧实现通过，随后只提取 `_orderbook_event_fields(row, exchange_name)`；`normalize_event` 保留 row/payload/kind 推断、基础 event、其他 kind 与 `_finish_event`。AST span **181 → 134**，helper 为 44 行。 |
| 验证 | orderbook 专项 **7 passed**；加 normalized public API、position mutation 与 CTP surface 的定向并行回归 **171 passed、7.16s**；改动文件 Ruff/format、`git diff --check` 通过；新鲜根并行标记回归 **1339 passed、2 warnings、75.51s**。 |
| 边界 | 本批计为 Task15 的 **4/5**；主包 >150 行函数清单 **19 → 18**。没有改动事件 dispatcher 的其他分支、外部交易所连接或 CTP/SimNow 会话；本地归一化测试不构成外部连通性证明。 |

##### 第五项评审与实施结果（2026-09-20）

| 项 | 结论 |
|----|------|
| 候选 | `bt_api_py.bt_api.BtApi.get_order_readiness`（157 行）。 |
| 选择理由 | 在剩余清单中，`_ctp_quote_v2_fields`、CTP recovery 授权和 close-plan evidence 均直接参与 fail-closed 执行资格或平仓证据判定，不应为完成数量仓促拆动。本函数是公开的**只读** readiness 编排边界，已有 Binance/OKX 的本地 fixture 契约；第五项只分离 Binance 分支，能把风险局限为现有读操作的委托。 |
| 风险分级 | **中等语义风险、受控外部读取风险**：它不提交订单、不开启会话或写持久化状态，但会依次读取账户、交易规格和可选账户数据。读取的顺序、`extra_data` 深拷贝、初始 definite failure 的短路和顶层错误归一化均是契约，不得改变。 |
| 责任边界 | `get_order_readiness` 保留 venue/ZMQ/参数/会话环境 guard、账户配置读取、OKX 分支和统一 `normalize_error` 外壳；私有实例 helper `_get_binance_order_readiness` 只承担 Binance 的交易规格、持仓模式、可选读取及两阶段 mapper 调用。 |
| 输入/输出契约 | 公共签名、返回键集、checks/reasons 顺序、`ready`/`definite_failure`/`execution_unproven` 语义不变；Binance 的读取顺序固定为 `get_exchange_info` → `get_position_mode` → 初始 mapper →（未短路时）`get_symbol_config` → `get_leverage_info` → `get_max_size`，每次读取均接收独立但值相等的 `extra_data` 副本。 |
| 拆分前补强 | 新增本地 fixture 契约，记录完整 Binance 分支的读取顺序、各读取参数和 `extra_data` 副本隔离；保留已有保证金/杠杆失败、非法数量短路、同步/异步共享契约。测试必须先在原实现通过。 |
| 测试先行 | 新增 `test_binance_readiness_success_preserves_read_order_parameters_and_extra_data_copies`，先在原实现以 **1 passed、4.99s** 通过；它锁定账户配置、交易规格、持仓模式、用户配置、杠杆和最大数量的顺序/参数，以及六份含嵌套对象的独立 `extra_data` 副本。 |
| 实施结果 | 只移动既有 Binance 分支至 `_get_binance_order_readiness`；读取条件、异常吞没范围、mapper 参数、OKX 分支和公共 API 未改变。公开入口 AST span **157 → 104**，helper 为 72 行。 |
| 验证 | 完整 `test_readonly_futures_metadata.py` **44 passed、5.27s**；改动文件 Ruff/format、`git diff --check` 通过；新鲜根并行标记回归 **1340 passed、2 warnings、76.11s**。 |
| 边界 | 本批完成 Task15 **5/5**；主包 >150 行函数清单 **18 → 17**。测试均为本地 fixture/marker 回归，未运行网络、CTP 或 SimNow，不能证明外部连通性或交易可用性。 |

#### Task 16：覆盖率提升 40% → 55%
- 内容：优先补高风险模块（`forwarding/`、`gateway/`、`brokers/`、`_execution_session.py`）的行为测试；
  严格遵循 `docs/code-quality-baseline.md` 的反"空壳测试"判据（禁止自指签名、单断言壳、源码文本断言）。
- 验证：`pytest tests -m "<CI 标记>" --cov=bt_api_py --cov-report=term-missing` ≥ 55%；
  新增测试通过反空壳复核（人工 + 既有判据）。
- 结果（2026-09-20）：未以历史百分比代替验收；使用 CI 同一 marker 和 `-n 8` 新鲜复算为 **66.45%**，
  **1340 passed、2 warnings、83.00s**，故本 Task 完成。该覆盖率仍是本地 marker 回归，不代表外部 CTP/SimNow 验收。

### M6 台账收口（P2）

#### Task 17：技术债登记册
- 内容：维护 `docs/TECH_DEBT.md`，登记：超长文件/函数、`ruff ignore` 的渐进项、
  遗留失败测试、`docs/plans/` 74 份计划现状（逐份标注 已落地/部分/未开工）；
  新增 `scripts/ci/render_tech_debt.py` 生成只读实时盘点。
- 结果（2026-09-21）：计划索引已覆盖并逐份复核 **74/74**；最终分布为已落地 61、已废弃/阻塞 8、部分/未开工 5、待确认 0；报告固定扫描根仓
  `bt_api_py`、`tests`、`scripts`、`examples` 的 2 条指定渐进规则；最近一次完整报告的历史证据为 **37** 项，Tasks37–41 未取得 fresh 全范围库存。
  `PERF402`/`S113`、`PERF403`、两个 `S112` 脚本命中和最后 7 个 `PERF401` 均已按离线契约清理并从相应根规则移除；`PERF401` 也已从测试路径例外移除。CTP vendor 的范围化 `S112` 例外不在本 Task 的清理范围。报告校验其余根配置及行内理由，不扫描子仓、不写文件、不自动修复；2026-09-20 对当时 `TC001`/`TC003` 的 **37/37** 项审计证明反射可见项须保持 `typing.get_type_hints` 可解析，不能以 `typing.*` 别名或全限定名伪清零。F-86 只处理此后新增且由无运行时用法审计或真实解析回归保护的命中；当前反射守卫尚未跟踪，须随最终交付显式纳入版本控制。
- 验证：历史 `make tech-debt-report`、最后 PERF401 的 1/2/1/26/8/48 passed 合同、最后 S110 的 12 个 fake-only 合同、63 目标 `typing.get_type_hints` 本地观察、F-86 四测试 **48 passed**、Task33 artifact-first **12 passed**、Task34 AST/假模块 **7 passed**、Task35 精确假模块 **4 passed**、Task36 生产监控/Hyperliquid 假模块 **7 passed**、Task37–41 联合目标 **203 passed**、风险限额 **34 passed** 与组合 **111 passed**、单规则扫描、PR 子模块元数据治理历史 **14 passed** 与根 marker 回归。工件闭包落地 root marker 为 **1499 passed、2 warnings、97.25s**；本批 Task41 后最终 marker 为 **1735 passed、2 warnings、88.69s**，`--cov-branch` 同范围证据为 **1735 passed、2 warnings、110.06s** / **67.27%**。历史技术债为 37；本批只有目标 TC 扫描无新增的证据，未重跑 fresh `tech-debt-report`。

#### Task 18：例外清单显式化与文档同步
- 内容：生成代码/测试桩等配置级例外写入 `pyproject.toml per-file-ignores` + 本迭代文档 + 台账三处一致；
  精确行级例外则固定为源码行 + 本迭代文档 + 台账，并由行为测试覆盖；
  更新 `docs/CODE_QUALITY.md`（当前基线、门槛、棘轮用法）。
- 结果（2026-09-20）：E-05、E-06 与 E-07 均已逐行登记；其中 E-07 的固定 Ruff argv、根 cwd、无 shell、配置/JSON/退出状态 fail-closed 均有行为测试，单脚本 Bandit 通过。
- 验证：三处例外清单一致；新人可按 `docs/CODE_QUALITY.md` 的命令复现当前指标。例外受约束不等于相关债务已清零。

#### Task 19：PR governance schema v2 与可信 base
- 内容：使用真实 `git diff --raw -z --no-renames --no-ext-diff --abbrev=40` 生成逐 path `gitlink_changes` 和 `collection_errors`；可信 base 由脚本与 Git 对象探针确定。校验器拒绝缺失/重复/错路径、短/零 SHA、未知 type-change，识别但拒绝 add/delete；legacy 全局字段不再授权。
- 结果：本地 collector、validator 与 workflow 结构契约合计 **46 passed**。`dev`、`master`、`code-optimization` 的两阶段 rollout 固定为 `strict=false → 观察 → strict`；三分支、fork 与 strict 在线运行均 **NOT_RUN**，故为 **PARTIAL / NO-GO**。

#### Task 20：SourceSupervisor 同键并发
- 内容：以 per-key `asyncio.Lock` 串行化 upstream start/stop await 与 refcount 更新；different-key 仍并行，空闲锁可回收。
- 结果：首次并发订阅、slow-stop/新订阅交错、顺序 refcount 与 double-close 的确定性本地契约 **5 passed**；未连接外部 feed。

#### Task 21：Docs CI fail-closed
- 内容：扩充文档触发 paths，删除安装 fallback、`--no-build-isolation`、`--no-deps` 和任何 `continue-on-error`；严格构建前执行 generator `--check` 与 docs contract，保留 Pages 结构。
- 结果：本地 YAML/结构契约 **4 passed**；GitHub/Pages **NOT_RUN**。

#### Task 22：Optional optimized 安全与 wheel smoke
- 内容：移除 Bandit/pip-audit/build/wheel smoke 的 fail-open 遮蔽，锁定本轮 wheel 的安装、导入与最小运行契约。
- 结果：本地结构契约 **5 passed**；真实 GitHub Bandit/pip-audit/build/wheel **NOT_RUN**，状态 **PARTIAL**。CTP `B507` 仍为安全 NO-GO。

#### Task 23：Same-candidate 正式发布链
- 内容：实现单运行 `build → publish-testpypi → smoke-install-testpypi → publish-pypi`，绑定 source SHA、version、wheel/sdist filename/size/SHA256、wheel-contract receipt、manifest 与 `SHA256SUMS`；smoke 精确下载候选并校验独立 wheel/sdist/manifest 摘要锚，生产复用原 artifact，有界重试且 fail closed。
- 结果：本地候选/工作流与既有发布资源定向合同 **54 passed**。manual 只构建；三个外部 job 统一要求 release + `ENABLE_PYPI_RELEASE=true`；TestPyPI wheel 校验后从本地安装，依赖仅走 PyPI；PyPI job 依赖并复验同一 artifact。GitHub hosted、TestPyPI/PyPI、OIDC/Environment/trusted publisher/Ruleset 与第三方 action 不可变 SHA 固定均 **NOT_RUN**；`release: published` 的元数据先于 smoke，故正式发布仍为 **PARTIAL / NO-GO**。

#### Task 24：Canonical 测试 runner fail-closed

- 内容：默认注入 `not ctp` marker；`--ctp` 仅解除该默认排除；`make test-ctp` 显式传递 `--ctp -m ctp`。`--cov` 增加 `--cov-branch`，并分别传播 pytest 与 `tee` 的退出码、保留失败摘要。
- 结果：runner/Makefile 合同 **23 passed**；最新 checkout-local 根 marker **1735 passed、2 warnings、88.69s**，branch coverage 同范围复跑 **1735 passed、2 warnings、110.06s** / **67.27%**。network/CTP/SimNow 均 **NOT_RUN**。

#### Task 25：Optional performance false-green 收紧

- 内容：只保留 `workflow_dispatch` opt-in，移除 schedule；缺少 `tests/performance/`、benchmark 报告、非空 `benchmarks` 或严格 artifact 均 fail closed。
- 结果（历史阶段）：optimized 合同累计 **8 passed**（其中 performance 新增 **3**），当时真实 benchmark **NOT_RUN**、能力未建立。该缺口已由 Task42 的本地 singleton 能力取代；GitHub hosted 和多 benchmark 聚合仍未完成，schedule 继续禁用。

#### Task 26：CI full-suite baseline coverage fail-closed

- 内容：`tests.yml` full-suite 使用 `python -m pytest tests -v -n 8 --cov-branch`；coverage threshold 仅经 step env 传入，通过 0..100 十进制数值白名单（允许小数）后以 Bash 数组逐 argv 传递，禁止命令替换与额外 pytest 参数注入。
- 结果：`coverage-full.xml` 与 `htmlcov/index.html` 非空验证及同一 artifact 严格归档已由本地结构合同 **20 passed** 锁定；Codecov 仍仅可选遥测。GitHub hosted **NOT_RUN**，故状态是 **PARTIAL**，不宣称在线 CI 通过。

#### Task 27：Execution journal 迁移结构与原子性

- 内容：先以失败注入锁定 publish-before-registry 与 COMMITTED transaction 写失败两条 rollback 路径，再把 `migrate_execution_journal` 的 source freeze、claim/quarantine、远端对账、registry authority、staging/cutover、source seal、commit 和 rollback 拆为私有 helper；用 frozen `_MigrationPreparation` 替代 9 元组。
- 结果：入口 AST **361 → 128**，公开签名与 `migrate_legacy_execution_journal` 别名身份不变。提交阶段改为先原子写入独立 committed payload，成功后才更新内存状态；失败时 source、destination/staging/sealed、transaction 和所有非空 registry manifest 均按旧值恢复。定向 **6 passed**、execution-session 文件 **161 passed**；不涉及网络、外部 registry、CTP 或 SimNow。

#### Task 28：Legacy analysis entrypoint 单一实现源

- 内容：保留 8 个 `scripts/analysis/` 历史 CLI/import 路径，但移除其中的实现副本；统一惰性加载顶层 `scripts/*.py` canonical 模块。import 模式转发 canonical 已有属性的读写，CLI 模式通过 `runpy` 保留 argv 尾部、退出码与 `SystemExit`。
- 结果：独立审查先发现首版只读 proxy 会让 `wrapper.CORE_DIR = value` 停留在 wrapper，补齐写转发后又发现 `patch.object` 退出阶段删除/恢复失败，以及 wrapper 本地 `Path` 遮蔽 canonical 同名全局。最终私有化基础设施名称，以持久 canonical 属性集合和私有 `ModuleType` 子类转发读取、写入及删除/恢复。6 组既有双路径合同、两组补齐 parity 与隔离 wrapper 合同共 **41 passed**；目标 Ruff/format/mypy、`git diff --check` 通过，顶层 8 个 canonical 文件 SHA256 前后不变，独立复审 **PASS**。未运行真实扫描、coverage 主入口、网络或报告写出。

#### Task 29：Funding snapshot coercion 职责拆分

- 内容：仅在干净的 `cross_venue.py` 与其既有契约测试内工作；保持 `coerce_funding_snapshot` 公共签名和错误优先级，将 freshness mapping 重建、`FundingSnapshot` mapping 重建与最终 availability/identity/expiry 校验拆为私有 helper。
- 结果：入口 AST **81 → 28**。独立审查发现首版会提前读取自定义 Mapping 的外层 `source`，改变读取次数并让构造阶段异常逃逸；改为按原顺序惰性读取并补状态化 Mapping 回归。dataclass 输入保持对象身份，完整 mapping 重建不发明默认值，bool/zero interval、缺失或 naive freshness、stale、exchange/symbol mismatch、过期与构造器类型错误均由 **17 passed** 锁定；目标 Ruff check、Ruff format、mypy 与 `git diff --check` 通过。不涉及交易所、网络、订单或外部时间源。

#### Task 30：Docstring 自动补全的类型与写入安全闭包

- 内容：只修改 canonical `scripts/fill_missing_docstrings.py` 与既有 docstring 工具测试。以 `FunctionNode` 联合类型和 `MissingDocstrings` `TypedDict` 精确描述 module/class/method/function 四类 AST 结果；不使用 `Any`、`cast`、`noqa`、`type: ignore` 或配置放宽。
- 结果：目标脚本 **20 → 0** 个 mypy 错误，精确 CI 范围 **77 → 57 errors（14 → 13 files）**。行为测试暴露并推动修复：多行 body docstring 落到定义外、同索引 action 丢失 module docstring、跨行 header 的 inline suite 被插入参数/基类列表、tab 混缩进，以及 UTF-8 文件的 PEP 263 cookie 被推到第三行。实现以 token 定位定义头冒号、复用原始行缩进、稳定合并同索引动作，并在任何写入前重新 parse 且确认目标 docstring 已形成；失败返回 `False` 且原文件不变。定向 **23 passed**，目标 Ruff/format/mypy、diff-check 与独立最终复审 **PASS**；完整根 marker **1725 passed、2 warnings、100.03s**，同范围 branch coverage **1725 passed、2 warnings、119.54s / 67.24%**。当前仅承诺 UTF-8 cookie 位置；极少见 split-line `async \\` / `def` 语法安全拒绝，未宣称支持。

#### Task 31：本批新增 TC003 的运行时兼容闭包

- 内容：只处理 F-85 后实时报告出现的 4 个新增 `TC003`。对三个测试文件先确认 `Path`/`ModuleType` 仅用于 future annotations，再移入 `TYPE_CHECKING`；对 `fill_missing_docstrings.py` 的公开 `Iterable[str]` 注解保留运行时可解析名称，并新增真实 `typing.get_type_hints` 结果断言。禁止改 ignore、基线、规则范围，禁止以 `typing.*` 对象替换或全限定名规避诊断。
- 结果：四测试定向 **48 passed**；五文件目标 mypy、Ruff、format、diff-check 与独立复审 **PASS**。`make tech-debt-report` 从本批新增后的 **40** 降至 **37**（TC001 22、TC003 15），只计 3 个测试命中的真实清理；脚本 `Iterable` 直接导入继续计债，不以别名或 ignore 隐藏。历史 37/37 反射审计保持为当时快照，余下库存仍须逐项证明运行时兼容或明确不进入反射面。

#### Task 32：Quality ratchet 强制更新防绕过

- 内容：收紧 `scripts/ci/check_quality_ratchet.py --force-update`。当 baseline 已存在时，任何 recorded scope 删除均在 Ruff 扫描前拒绝；真正的 scope expansion 必须额外扫描旧 recorded scope 并确认每条旧债不升；same-scope 复用首次扫描结果与旧 baseline 比较。普通 `--update` 的范围一致、只降不升语义保持不变。
- 结果：新增 scope shrink、扩张时旧范围债务回升、same-scope 回升三条 fail-closed 测试，均断言 baseline 字节不变；原安全扩张仍通过。两个目标测试文件合计 **31 passed**，目标 Ruff/format/mypy、`make quality-ratchet` 与 diff-check PASS；`--force-update --help` 不再宣称无条件写快照。

#### Task 33：Artifact-first mypy 边界收敛

- 内容：在 `offline_pip.py`、`submodule_validation.py` 与对应测试中精确建模三类边界。环境参数接受 `Mapping[str, str]`，先以真实运行时 guard 拒绝非映射输入，再复制并只重建四个受控 `PIP_*`；公开 `run_validation` 接受 `Path | str | None`，在创建 artifacts 前单次解析为内部 `Path | None`；`Distribution.locate_file()` 的 `SimplePath` 经 `Path(str(...)).resolve()` 明确转为本地文件系统路径，再执行 purelib/platlib 和文件存在性校验。
- 结果：目标 **12 passed**，四文件 mypy/Ruff/format、diff-check、`make tech-debt-report` 与独立复审 **PASS**；非映射输入在复制前拒绝，相对字符串和不存在的 `Path` 均在 artifacts 创建前失败，只读 `MappingProxyType` 输入不被修改，债务保持 **37**。Task30 的精确 256 文件结果仍为 **57 errors / 13 files**；本批按禁读边界排除两份测试后的可比 254 文件范围为 **52 errors / 11 files**，不表述为精确 CI 全量复跑。未使用 `Any`、`cast`、ignore、`noqa` 或配置放宽。完整根 marker **1731 passed、2 warnings、88.20s**，branch coverage **1731 passed、2 warnings、118.11s / 67.24%**。

#### Task 34：测试侧 AST 与假模块 mypy 边界收敛

- 内容：只处理 `tests/test_legacy_dex_example_retirement.py`、`tests/scripts/test_reconcile_tick_universe.py` 与 `tests/test_ctp_env_selector.py` 的 4 个 mypy 错误。DEX AST 扫描在访问行号与 helper 前显式窄化到 import 节点；PyArrow/YAML 替身使用精确 `ModuleType` 子类声明运行时属性，拒绝 Parquet 读取的桩以 `Callable[..., Never]` 表达既有行为。
- 结果：三文件目标 mypy **4 → 0**，定向 **7 passed**，Ruff/format、diff-check、`make tech-debt-report` 与两次独立复审 **PASS**；债务保持 **37**，本批未新增 `Any`、`cast`、ignore、`noqa`、配置放宽、`__dict__` 或 `setattr` 类型逃逸。可比 254 文件范围 **52 → 48 errors（11 → 8 files）**；Task30 的精确 256 文件证据仍为 **57 errors / 13 files**且未重跑。完整根 marker **1731 passed、2 warnings、92.95s**，branch coverage **1731 passed、2 warnings、135.56s / 67.24%**。这不构成真实 Parquet、CTP/SimNow、网络或发布验收。

#### Task 35：离线脚本测试 fake 模块 mypy 边界收敛

- 内容：仅以精确 `ModuleType` 子类表达两份离线脚本测试中 Playwright、browser-cookie3 与 requests/requests.exceptions 的真实运行时属性；保留 Chrome 读取失败、Firefox 触发离线请求异常、Safari 成功与 Edge 不调用的原行为。
- 结果：两文件目标 mypy **8 → 0**，定向 **4 passed**；不启动浏览器或网络。fresh `--no-incremental` 可比 254 文件快照为 **41 errors / 7 files**；该运行额外暴露一个既有注解错误，故不把 Task34 的 **48 errors / 8 files** 与本值写成直接因果差值。Task30 的精确 256 文件证据仍为 **57 errors / 13 files** 且未重跑。

#### Task 36：生产监控/Hyperliquid 离线测试 fake 模块 mypy 边界收敛

- 内容：为 logging/monitoring 与 Hyperliquid 的根包、feeds、functions、live feed、log message 与 config loader 建立精确 `ModuleType` 子类并赋真实属性。独立审查发现首版 `config_loader.load_exchange_config` 只有注解、运行时没有属性，却以 `raising=False` 临时创建假接口；fixture 改为先绑定安全失败占位函数，异常注入恢复默认 `raising=True`。
- 结果：两文件目标 mypy **18 → 0**，定向 **7 passed**；同一 fresh 254 文件范围 **41 → 23 errors（7 → 5 files）**。完整根 marker **1734 passed、2 warnings、91.45s**，branch coverage **1734 passed、2 warnings、119.06s / 67.27%**。这不构成真实服务、Hyperliquid、网络、CTP/SimNow 或发布验收。

#### Task 37：Execution session key/value 类型收敛

- 内容：在 `_execution_session.py` 中为字典 key/value 边界补充精确注解，不改变 execution session 的运行时状态、持久化或回滚语义。
- 结果：目标 mypy **1 → 0**，execution-session **164 passed**，`make type-check` 的 **128 source PASS**，独立复审 **PASS**。

#### Task 38：OKX WSS 测试 Event 联合类型收敛

- 内容：以精确 Event 联合描述 OKX WSS 测试中的事件变体，保持原有消息与断言行为。
- 结果：目标 mypy **5 → 0**，定向 **3 passed**，独立复审 **PASS**。Task37/38 后 fresh 254 文件范围为 **17 errors / 3 files**。

#### Task 39：Workflow YAML 运行时形状窄化

- 内容：在 workflow 测试中以路径化 runtime YAML guards 逐层窄化动态结构，并增加坏形状负例，不依赖 `Any`、`cast`、ignore 或配置放宽。
- 结果：目标 mypy **5 → 0**，定向 **21 passed**，独立复审 **PASS**。

#### Task 40：Websocket 基础设施 Protocol 边界收敛

- 内容：以 connection/server/socket Protocol 保持 websockets>=12 API shape，服务端使用 `port=0` 避免本机 Anki 占用 8765 导致的冲突。fresh 跨文件扫描又捕获 `_receive_messages: AsyncMock` consumer 1 个错误，以最小 receiver Protocol 修复，不改运行时 websocket 流程。
- 结果：目标 mypy **11 → 0**。websockets 15.0.1 下两文件 **8 passed**；独立复审在 13.1 与 15.0.1 下分别 **7 passed**。12.0 未安装执行，但官方 API shape 复核兼容，最终结论 **PASS**。

#### Task 41：Probe subprocess 类型收敛

- 内容：以 `list[str]` 与 `CompletedProcess[str]` 精确表达 probe 命令与文本输出，保持原有 subprocess 调用和错误处理语义。
- 结果：目标 mypy **1 → 0**，定向 **8 passed**，独立复审 **PASS**。Task37–41 联合目标 **203 passed**。排除两份禁读测试的 fresh `--no-incremental` 254 文件范围最终为 **0 errors / 0 files**，但精确 256 文件命令未重跑。根 marker **1735 passed、2 warnings、88.69s**，branch coverage **1735 passed、2 warnings、110.06s / 67.27%**。历史技术债仍为 **37**；本批仅目标 TC 扫描无新增，未重跑 fresh `tech-debt-report`。

#### Task 42：AC-19 singleton 性能安全上限

- 内容：新增纯离线静态 orderbook `normalize_event` benchmark；workflow 与 Make 固定唯一 nodeid 和 `-n 8`，schema v1 基线强制唯一 fullname、mean ≤ **0.0005s**、rounds ≥ **1000**。checker 对文件、JSON、重复项、名称集合、mean、iterations、rounds 和阈值全链 fail closed；Make 使用临时目录并清理报告，workflow 仅在 checker 成功后上传。
- 结果：定向 benchmark/checker/workflow 合同 **59 passed**，目标 Ruff/format、fresh 4 文件 mypy 与 diff-check PASS；7 次本地有效 JSON 均为 1/1，mean **8.096–32.879µs**、rounds **3613–14608**，门禁通过。最新完整根 marker **1796 passed、2 warnings、95.29s**，文档生成检查、文档合同和严格 MkDocs 构建均 PASS。7 条空闲 xdist worker warning 是当前 singleton 并行模式的已知噪声。0.5ms 只是灾难性退化上限；GitHub hosted、多 benchmark JSON 聚合与 schedule 恢复仍 **NOT_RUN / NO-GO**。

#### Task 43：OrderRequest 校验复杂度与边界合同

- 内容：先用 `dataclasses.replace` 驱动非法运行时值，锁定 17 项校验的异常类型、精确消息和跨段短路优先级；再把原顺序 1–8/9–17 机械移动到两个私有 helper。不得改变 frozen dataclass 字段/签名/反射、truthiness、不可哈希输入或 `replace`/`asdict` 行为，不新增 `Any`、`cast`、ignore、`noqa` 或配置例外。
- 结果：入口/core/intent C901 为 **1/9/10**，`models.py` C901 **3 → 2**、主包 **84 → 83**；两文件 **50 passed**、七文件直接消费者 **81 passed**，目标行/分支覆盖完整，128 source mypy、Ruff/format、diff-check 与独立复审 PASS。最新完整根 marker **1824 passed、2 warnings、127.85s**；本批未重跑 branch coverage。

## 3. 里程碑映射

| 里程碑 | Task | 出口判据 |
|--------|------|----------|
| M1 止血 | 1–4 | F821 = 0；账号泄漏 = 0；遗留失败已处置；配置债清零 |
| M2 堵漏 | 5–8, 32 | 门禁覆盖子仓；普通与强制更新均不能缩小既有范围或抬高旧债；覆盖率门槛同源 |
| M3 清存量 | 9–11 | 机械项清零；`S106` 收口；子仓规则统一 |
| M4 类型与文档 | 12–13, 30–31, 33–41 | 公开函数 docstring ≥85%；注解 ≥45%；L1 五仓入 mypy；精确/可比 mypy 范围与类型导入债按运行时行为契约逐步收敛 |
| M5 结构与覆盖率 | 14–16, 27–29, 43 | 5 份方案卡；主包 5 个计划内超长函数、1 个追加高风险迁移函数、1 个干净职责拆分及 1 个 C901 等价拆分切片完成；8 个 legacy analysis 副本收敛为单一实现源；覆盖率 ≥55% |
| M6 台账收口 | 17–18 | 台账对账通过；例外三处一致；文档可复现 |
| M7 运行与发布治理 | 19–26, 42 | 本地合同与 singleton benchmark 完成；GitHub/Pages/三分支 strict rollout/hosted benchmark/TestPyPI/PyPI 仍须取得外部证据 |

## 4. 验收映射

| AC（验收文档） | 覆盖 Task |
|----------------|-----------|
| AC-1 真实缺陷清零（F821） | 1 |
| AC-2 安全泄漏清零 | 2 |
| AC-3 遗留失败处置 | 3 |
| AC-4 配置债清零 | 4 |
| AC-5 门禁全量覆盖 | 5, 6, 11 |
| AC-6 棘轮有效（只降不升） | 7, 32 |
| AC-7 覆盖率门槛同源且达标 | 8, 16 |
| AC-8 机械 lint 债清零 | 9, 10 |
| AC-9 类型与文档目标达成 | 12, 13, 30, 33–41 |
| AC-10 结构治理产出 | 14, 15, 27, 28, 29, 43 |
| AC-11 债务台账可用 | 17, 18, 30, 31, 33–41 |
| AC-12 零回归 | 全部（每批测试全绿；Task37–41 联合目标 203 passed；Task43 七文件 81 passed） |
| AC-13 SourceSupervisor 并发 | 20 |
| AC-14 docs workflow | 21 |
| AC-15 PR governance v2 | 19 |
| AC-16 optimized 安全/wheel smoke | 22 |
| AC-17 same-candidate 发布 | 23 |
| AC-18 canonical 测试 runner | 24 |
| AC-19 optional performance | 25, 42 |
| AC-20 CI full-suite baseline coverage | 26 |

## 5. 已知限制

- **子仓协调成本**：15 个子仓各自 CI/分支/发版节奏，本期只保证"止血 + 门禁 + 机械清理"，
  类型与结构治理按仓排期，可能跨迭代。
- **覆盖率提升有天花板**：`bt_api.py`(8864 行) 等巨型文件难以在单迭代内有效覆盖，
  55% 是阶段性目标而非终局。
- **棘轮依赖扫描口径稳定**：若后续调整 `ruff` 规则集或扫描范围，需重建基线（属预期，需在 PR 中说明）。
- **CTP 生成代码**始终在例外内，不纳入任何指标计算。
- **`docs/plans/` 74 份计划**只做状态登记，不逐份执行；执行与否由 owner 按价值决定。
