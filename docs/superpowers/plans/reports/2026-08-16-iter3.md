# 迭代 3 报告：发布链路与工程化修复

> 日期：2026-08-17

## 目标回顾

让发布真正可执行、版本单一、CI 覆盖子模块、文档构建稳定（Spec D-01~D-15）。

## 任务完成情况

| 任务 | Finding | Commit | 状态 |
|------|---------|--------|------|
| 3.1 | D-01 | `b630c780` | complete（MIT LICENSE + PEP 639 license 表达式） |
| 3.2 | D-02 | `afd84386` | complete（版本单一源 importlib.metadata，删 _version.py，版本 0.15.3） |
| 3.3 | D-03 | `91cb74f2` | complete（publish 单 job + tag↔版本校验） |
| 3.4 | D-07 | `752f3ed0` | complete（删 setup.py/MANIFEST.in CTP/Cython 死代码） |
| 3.5 | D-09 | `560c7e88` | complete（classifiers 3.9-3.13，删 3.14） |
| 3.6 | D-08 | `01b17dfc` | complete（恢复 arg-type/attr-defined/call-arg/var-annotated，mypy 全绿） |
| 3.7 | D-13/14 | `7ed762f9` | complete（get_supported_ops 去 `or True`、CI 模板去 `|| true`、checkout v6） |
| 3.8 | D-04/B-19 | `5b4e05aa` | complete（install_and_test_all PASS/FAIL/SKIP + parallel + report，新建 submodule-tests.yml） |
| 3.9 | D-05/06/10/15 | `5e2d6103` | complete（changelog 合并、mkdocstrings paths: [.]、死引用批量修复、readthedocs strict） |
| 3.10 | D-11/12 | `3a5c1bb2` | complete（删 CTP 残留、pip-audit 真失败、兼容矩阵抽取 reusable workflow） |

## 验收清单结果

1. ✅ `python -m build && twine check dist/*` 通过（`bt_api_py-0.15.3-py3-none-any.whl` + sdist，twine check PASSED）
2. ✅ tag↔版本校验本地模拟：正确 tag（0.15.3 == 0.15.3）通过、错 tag（v0.9.9 ≠ 0.15.3）失败
3. ✅ `mkdocs build --strict` 绿（Documentation built in 3.84s，无 WARNING）
4. ⚠️ nightly 子模块 workflow `workflow_dispatch` 手动触发：需在 GitHub 上触发（本地无法执行），workflow 文件已就绪
5. ⚠️ code-reviewer agent 复审：当前环境无 reviewer 子 agent，controller self-review，无 CRITICAL/HIGH

## 关键设计决策

- **版本单一源**：`pyproject.toml` `version = "0.15.3"`（与 bt_api_base 对齐），`__init__.py` 用 `importlib.metadata` 读取，删除 `_version.py` 和 setup.py 的 `_load_version()`。Task 3.1 的 PEP 639 `license = "MIT"` 触发 setuptools 冲突，删除 License classifier 解决。
- **mypy 恢复错误码**：`disable_error_code` 移除 4 个核心码后暴露 10 个真实类型错误，全部修复（asdict cast/ignore、EntryPoints 兼容、Coroutine 签名、Literal 收窄、redundant-cast 删除）；`python_version` 3.9→3.12（numpy stubs 要求）。
- **兼容矩阵去重**：新建 `reusable-compat-matrix.yml`（workflow_call），tests.yml 与 optimized-tests.yml 各自调用，消除 18 job 重复。
- **死引用批量修复**：`bt_api_py.registry/event_bus/auth_config` → `bt_api_base.*`（点号+斜杠形式），涉及 27 个 .md 文件；`grep` 验收无残留。

## 遗留问题

1. **nightly workflow 未实际触发**：`workflow_dispatch` 需在 GitHub 手动触发一次验证（本地无法执行）。
2. **code-reviewer 复审未完成**：需在有 reviewer 子 agent 的环境补做。
3. 迭代 1/2/3 共 30+ commit 尚未 push（本次迭代 3 的 commit 在本地 master）。
4. `.joyincode/`、`docs/plans/`、`docs/superpowers/` 等工作流文件仍未入库（不入库）。
