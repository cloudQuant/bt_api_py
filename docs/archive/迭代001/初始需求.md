### 背景

现在bt_api_py已经相对成熟了，但是接入了很多的交易所，整个项目已经相对比较臃肿，现在希望bt_api_py只保留核心的部分，其他关于交易所相关的内容都分拆成不同的python包，使用pip install 安装之后，可以调用包，实现同样的内容

### 任务

1. 把ctp、ib_web、mt5，binance, okx等数字货币交易所，拆分出来，形成bt_api_ctp, bt_api_ib_web, bt_api_mt5, bt_api_binance, bt_api_okx等这种形式的包，放到一个文件夹中，后面我会把这些内容放到一个python包里面去上线。
2. bt_api_py这个包是需要重构的，一些基础的底层的代码，很多bt_api_xx交易所和bt_api_py会使用到的，放到bt_api_base包中，bt_api_xx正常应该只调用bt_api_base中的内容，bt_api_xx这个是直连交易所的。bt_api_py是对bt_api_xx的统一接口封装，并且实现了gateway和zmq行情交易转发。
3. bt_api_py里面的代码应该是越来越少的，
